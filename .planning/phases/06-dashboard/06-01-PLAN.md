---
phase: 06-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/dashboard/queries.ts
  - app/api/proposals/route.ts
  - app/api/decisions/route.ts
  - app/providers.tsx
  - app/layout.tsx
  - app/page.tsx
  - app/decisions/page.tsx
  - components/proposal-queue.tsx
  - components/decision-log.tsx
  - components/status-chip.tsx
  - components/nav.tsx
  - components/ui/table.tsx
  - components/ui/badge.tsx
  - components/ui/card.tsx
autonomous: true
requirements: [DSH-01, DSH-02, DSH-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Opening http://localhost:3002 lists every seeded Proposal with its status, its start time in Asia/Hong_Kong and its confidence (DSH-01, D-05, D-23)"
    - "Opening http://localhost:3002/decisions lists Decision rows including at least one ignored row with its confidence and its reason (DSH-02, D-06)"
    - "A Proposal row inserted directly into Postgres appears in an already-open dashboard tab within 5 seconds with no manual reload (DSH-03, D-08; polling only, no websocket or SSE per D-09)"
    - "Both views are reachable from each other through the two-link nav without typing a URL (D-06 separate view)"
    - "Every displayed time string is produced once, server-side, by the Phase 1 HKT formatter; no component and no route handler calls Intl or Date (D-20, D-23)"
    - "Neither list component nor the provider reads any environment value, config module or Prisma client; client components receive rows as props or as JSON from a route handler only (D-20)"
    - "No control anywhere in the dashboard mutates data: no approve, reject, edit or retry affordance exists on either view (D-07)"
    - "Two Proposals sharing the same created_at render as two separate rows in a deterministic order broken by id descending, never merged into one (edge DSH-01 adjacency, resolved-explicit)"
    - "An empty Proposal table renders exactly one empty-state row and an empty Decision table renders exactly one empty-state row; neither view renders a blank page, a bare table head, or throws (edge DSH-01 empty, resolved-explicit)"
    - "Two consecutive polls over unchanged data return rows in the identical order, because both queries order by a descending timestamp plus a descending id tiebreak (edge DSH-01 ordering, resolved-explicit)"
    - "A row whose confidence column is null renders an em dash, never NaN, null or a blank cell (edge DSH-01 empty, resolved-explicit)"
  artifacts:
    - path: "lib/dashboard/queries.ts"
      provides: "The single read path for both views and both transports, including one-time HKT formatting"
      exports: ["ProposalRow", "DecisionRow", "getProposalRows", "getDecisionRows"]
      contains: "prisma.proposal.findMany"
    - path: "app/api/proposals/route.ts"
      provides: "Parameterless GET returning ProposalRow list as JSON for the poll"
      exports: ["GET"]
    - path: "app/api/decisions/route.ts"
      provides: "Parameterless GET returning DecisionRow list as JSON for the poll"
      exports: ["GET"]
    - path: "app/page.tsx"
      provides: "Proposal queue first paint, replacing the Phase 1 Prisma-count placeholder"
      contains: "getProposalRows"
    - path: "app/decisions/page.tsx"
      provides: "Decision log first paint, the separate view D-06 requires"
      contains: "getDecisionRows"
    - path: "app/providers.tsx"
      provides: "QueryClientProvider holding one client in useState"
      exports: ["Providers"]
    - path: "components/proposal-queue.tsx"
      provides: "Client polling table for proposals"
      exports: ["ProposalQueue"]
      contains: "refetchInterval"
    - path: "components/decision-log.tsx"
      provides: "Client polling table for decisions"
      exports: ["DecisionLog"]
      contains: "refetchInterval"
    - path: "components/status-chip.tsx"
      provides: "Status and verdict mapped to a token plus a symbol, shared by both views"
      exports: ["StatusChip"]
    - path: "components/nav.tsx"
      provides: "Two-link nav between the queue and the Decision log"
      exports: ["Nav"]
  key_links:
    - from: "app/page.tsx"
      to: "lib/dashboard/queries.ts"
      via: "direct server-side call for first paint"
      pattern: "getProposalRows\\("
    - from: "app/api/proposals/route.ts"
      to: "lib/dashboard/queries.ts"
      via: "same function as first paint, wrapped in a JSON response"
      pattern: "getProposalRows\\("
    - from: "components/proposal-queue.tsx"
      to: "app/api/proposals/route.ts"
      via: "useQuery queryFn fetching the route handler every 4 seconds"
      pattern: "/api/proposals"
    - from: "components/decision-log.tsx"
      to: "app/api/decisions/route.ts"
      via: "useQuery queryFn fetching the route handler every 4 seconds"
      pattern: "/api/decisions"
    - from: "lib/dashboard/queries.ts"
      to: "utils/time.ts"
      via: "the Phase 1 HKT formatter, called once per row, server-side only"
      pattern: "utils/time"
    - from: "app/layout.tsx"
      to: "app/providers.tsx"
      via: "root layout wraps children in the QueryClientProvider"
      pattern: "Providers"
  prohibitions:
    - statement: "MUST NOT render any control that implies the dashboard can approve, reject, edit, retry or otherwise change a Proposal. Approval lives on the Slack card, and a dead control that looks actionable is worse than none."
      category: transparency
      requirement_id: DSH-01
      status: unverified
      flagged: true
    - statement: "MUST NOT filter, cap or hide rows to make the demo look tidier. Every ProposalStatus value including dismissed and already_scheduled, and both DecisionVerdict values, reach the view."
      category: transparency
      requirement_id: DSH-02
      status: unverified
      flagged: true
    - statement: "MUST NOT turn the Decision log into a surveillance surface. No per-person aggregation, counting, ranking, scoring or sentiment of colleagues' messages; it shows the agent's own decisions and nothing else."
      category: privacy
      requirement_id: DSH-02
      status: unverified
      flagged: true
    - statement: "MUST NOT present the model-generated reason as an unattributed statement of fact about a person. The column is labelled as the agent's reason wherever it is shown."
      category: transparency
      requirement_id: DSH-02
      status: unverified
      flagged: true
---

<objective>
The tracer slice for Phase 6: real seeded rows out of Postgres, through one shared query module, onto two Server-Component-rendered pages that keep updating by polling, end to end, before any theming happens.

**As a** presenter demoing the agent, **I want to** see the live proposal queue and the Decision log on a projector, **so that** the audience sees every pending proposal and the agent's considered-and-ignored reasoning without me touching the browser.

Purpose: prove the whole read path (Prisma to `lib/dashboard/queries.ts` to route handler and Server Component to client poll) before 06-02 re-values tokens and 06-03 critiques it. Covers DSH-01, DSH-02, DSH-03 and ROADMAP success criteria 1 and 2. Ownership follows D-01, D-02, D-03, D-04; the plan split follows D-17; data display follows D-05, D-06, D-07; live updates follow D-08, D-09; process follows D-14 and D-18; the impeccable order starts here per D-15; repo rules follow D-20, D-21, D-22; times follow D-23.

Output: `lib/dashboard/queries.ts`, two GET route handlers, two Server Component pages, four components, the QueryClientProvider, and three shadcn primitives.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard/06-CONTEXT.md
@.planning/phases/06-dashboard/06-RESEARCH.md
@.planning/phases/06-dashboard/06-PATTERNS.md
</context>

<ownership_note>
`lib/dashboard/` is a NEW directory created by this phase and owned by Phase 6 alone. It sits outside the ROADMAP's Phase 6 "Files owned" list (`app/**`, `components/**`, `app/globals.css`) but is not in the must-not-touch list either, and PROJECT.md's "thin route handlers, logic in lib/" rule puts it there. It has zero overlap with Phase 5's `lib/agent/**` and `lib/ai/**`, Phase 2's `lib/slack/**` or Phase 3's `lib/calendar/**` (D-02).

Read but never modified by this phase: `prisma/schema.prisma`, `types/`, `utils/time.ts`, `lib/db.ts`, `lib/config.ts`, and the `package.json` scripts. `components/ui/` is written by the shadcn CLI and then left alone (D-01). `package.json` and `bun.lock` should not change, because this phase installs no npm package (Phase 1 D-21 already installed TanStack Query and Zustand). If the shadcn add touches either file, do not hand-merge: regenerate with `bun install` at merge time (D-03).

Schema gate: no schema file is modified this phase (D-04, read-only), so there is deliberately no `prisma db push` task anywhere in Phase 6.
</ownership_note>

<assumption_delta_decision>
Noun under review: `Proposal` / `Decision`. Decision: **no-change**. Rationale: this is a read-only display phase. It adds no column, no relation, no identity key and no new entity; `ProposalRow` and `DecisionRow` are display projections of existing rows, not a second model. The detector fired on prose only ("Runs alongside", "also extend", "Second:", "Custom shadcn tokens").
</assumption_delta_decision>

<cross_phase_assumptions>
Contracts consumed from Phases 1 to 4. Each is confirmed by reading a real file listed in `read_first` before use; none is edited here.

1. `lib/db.ts` exports the Prisma singleton `prisma` (Phase 1 D-12). Only `lib/dashboard/queries.ts` imports it.
2. `utils/time.ts` exports the HKT formatter, contract name `formatHkt`, imported through the alias path (Phase 1 D-08; Phase 2 research orchestrator note 4). **ASSUMPTION: its parameter type is `Date`.** Read `utils/time.ts` first and call whatever it actually exports with the type it actually declares. Never fork it and never edit it, because Phase 5 appends to the same file in parallel.
3. Schema field names are snake_case with no `@map` (Phase 1 D-10). Proposal: id, team_id, dedupe_key, title, start, end, tz, status, organizer_user_id, calendar_event_id, source_channel, source_ts, confidence, card_channel, card_ts, calendar_html_link, meet_link, alternatives, created_at. Decision: id, team_id, source_ts, verdict, confidence, reason, source_channel, message_text, proposal_id.
4. **Decision has no `created_at`.** Order it by `source_ts` descending and derive its display time from `source_ts`, a Slack epoch-seconds string. Do not add a column.
5. Enums (Phase 1 D-11): ProposalStatus is pending, confirmed, dismissed, already_scheduled; DecisionVerdict is acted, ignored. Phase 4 never writes already_scheduled onto a still-pending Proposal, but the enum value exists, so the chip still maps it.
6. **ASSUMPTION: `confidence` may be Prisma Float or Decimal, and may be nullable.** Map it through `Number(...)` behind an explicit null guard in the query module so both transports carry a plain number or null.
7. `app/page.tsx` is a Phase 1 Prisma-count placeholder that this plan replaces (Phase 1 D-25). shadcn was init-only in Phase 1 (D-22): `components.json`, the `:root` and `@theme inline` blocks in `app/globals.css`, and `cn` in `lib/utils.ts`. Biome excludes `components/ui/` (Phase 1 D-23).
8. Phase 1 D-19 scripts include `dev` (plain `next dev`, never a bun-runtime flag) and `check` (biome).
</cross_phase_assumptions>

<flagged_assumptions>
Edge-probe rows that stayed unresolved and are carried forward explicitly rather than dropped. Three of the phase's eleven probe items; the other eight are authored as `must_haves.truths` across 06-01 and 06-02.

- **DSH-02, unclassified.** No probe category applies to "the dashboard shows the Decision log including ignored messages with confidence and reason". Reviewed manually: covered by the empty-state, null-confidence and unparseable-timestamp criteria in Task 2. Left unresolved because no category applies, not because it was skipped.
- **DSH-03, unclassified.** No category applies to "new rows appear without a manual reload". The observable is the five-second truth above.
- **DSH-04, unclassified.** Carried in 06-02 and recorded there in the same form.

Ledger for this plan: 5 probe items in scope (DSH-01 adjacency, empty, ordering; DSH-02 unclassified; DSH-03 unclassified) = 3 authored as truths (plus one extra auto-resolved display criterion) + 2 flagged above. No silent drops.
</flagged_assumptions>

<artifacts_produced>
## Artifacts this phase produces (06-01 slice)

| Artifact | Kind | Where |
|---|---|---|
| `ProposalRow` | exported interface: id, title, status, startHkt, confidence | `lib/dashboard/queries.ts` |
| `DecisionRow` | exported interface: id, verdict, whenHkt, confidence, reason, messageText, sourceChannel | `lib/dashboard/queries.ts` |
| `getProposalRows()` | exported async function returning `ProposalRow[]` | `lib/dashboard/queries.ts` |
| `getDecisionRows()` | exported async function returning `DecisionRow[]` | `lib/dashboard/queries.ts` |
| `GET` | route handler export | `app/api/proposals/route.ts`, `app/api/decisions/route.ts` |
| `ProposalQueue` | client component, query key `["proposals"]`, `refetchInterval: 4000` | `components/proposal-queue.tsx` |
| `DecisionLog` | client component, query key `["decisions"]`, `refetchInterval: 4000` | `components/decision-log.tsx` |
| `StatusChip` | presentational component mapping all six enum values to token plus symbol | `components/status-chip.tsx` |
| `Nav` | presentational two-link nav | `components/nav.tsx` |
| `Providers` | client QueryClientProvider | `app/providers.tsx` |
| `Table`, `TableHeader`, `TableBody`, `TableRow`, `TableHead`, `TableCell`, `Badge`, `Card` | shadcn-generated primitives, unmodified | `components/ui/` |

Produced later in the phase: the retro CSS tokens (`--elevated`, `--success`, `--success-foreground`, `--shadow-retro`, `--radius`, the re-valued shadcn token set, the `--font-mono` and `--font-sans` mappings) in 06-02, and the critique-driven revisions in 06-03.
</artifacts_produced>

<source_audit>
## Multi-Source Coverage Audit (whole Phase 6 plan set)

| SOURCE | ID | Feature / Requirement | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Dashboard shows live proposal and Decision data in the retro theme, accessible and unstyled-literal-free | 06-01 + 06-02 + 06-03 | COVERED |
| REQ | DSH-01 | Proposal queue with status, HKT time, confidence | 06-01 | COVERED |
| REQ | DSH-02 | Decision log including ignored rows with confidence and reason | 06-01 | COVERED |
| REQ | DSH-03 | TanStack Query polling, no manual reload | 06-01 | COVERED |
| REQ | DSH-04 | Theme as shadcn CSS vars plus `@theme inline`, zero colour literals | 06-02, gate re-run in 06-03 | COVERED |
| REQ | DSH-05 | Retro devices applied consistently | 06-02, polished in 06-03 | COVERED |
| REQ | DSH-06 | Amber and cyan contrast plus visible focus states | 06-02, re-checked in 06-03 | COVERED |
| RESEARCH | — | Two routes plus one shared query module; `initialData`, not HydrationBoundary | 06-01 | COVERED |
| RESEARCH | — | GET handlers dynamic by default; pages get `await connection()` | 06-01 | COVERED |
| RESEARCH | — | Format HKT once, server-side, in the query module (hydration-mismatch pitfall) | 06-01 | COVERED |
| RESEARCH | — | `refetchIntervalInBackground: true` (background-tab pause pitfall) | 06-01 | COVERED |
| RESEARCH | — | `Number(...)` mapping for confidence (Decimal vs Float serialization) | 06-01 | COVERED |
| RESEARCH | — | Re-value shadcn's paired token set; `--muted` is a surface, not the label colour | 06-02 | COVERED |
| RESEARCH | — | Dark text on every filled chip (white on amber is 1.71:1) | 06-01 chip map, 06-02 token values | COVERED |
| RESEARCH | — | `--ring` set to amber gives shadcn primitives a 9.85:1 focus ring | 06-02 | COVERED |
| RESEARCH | — | Table rows are not interactive: no manual tab stops | 06-01, gated in 06-02 | COVERED |
| RESEARCH | — | `--border` at 1.46:1 flagged to critique as a lighten candidate | 06-03 | COVERED |
| RESEARCH | — | Feed theme direction inline; no `/impeccable init`, no DESIGN.md | 06-01, 06-02, 06-03 | COVERED |
| RESEARCH | — | `/impeccable critique` ends in a required question; budget real minutes | 06-03 | COVERED |
| RESEARCH | — | Package Legitimacy Audit not applicable: zero new npm packages | 06-01 | COVERED |
| CONTEXT | D-01 to D-04 | Ownership, overlaps, read-only schema | 06-01 ownership note | COVERED |
| CONTEXT | D-05, D-06, D-07 | Row fields, separate Decision view, read-only | 06-01 | COVERED |
| CONTEXT | D-08, D-09 | Polling shape, no websockets or SSE | 06-01 | COVERED |
| CONTEXT | D-10 to D-13 | Tokens, palette, retro devices, contrast and focus | 06-02 | COVERED |
| CONTEXT | D-14 | Port 3002, plain dev script, one build pair per save | 06-01 | COVERED |
| CONTEXT | D-15 | Impeccable order: layout, then typeset and colorize, then critique and polish | 06-01, 06-02, 06-03 | COVERED |
| CONTEXT | D-16 | `bg-elevated` smoke check in the first minutes of the theme work | 06-02 | COVERED |
| CONTEXT | D-17 | Suggested plan split | 06-01 objective | COVERED |
| CONTEXT | D-18 | Biome after each shadcn add and before done | 06-01, 06-03 | COVERED |
| CONTEXT | D-19 | Cut order under time pressure | 06-02, 06-03 | COVERED |
| CONTEXT | D-20 to D-23 | RSC default, folder rules, naming and TSDoc and no tests, HKT everywhere | 06-01 | COVERED |

No MISSING rows. Deferred ideas (impeccable audit in Phase 10, dashboard mutations in Phase 8, CopilotKit, in-dashboard graph render, real extracted rows in Phase 7) are out of scope per CONTEXT.md and are not gaps.
</source_audit>

<!-- planner-discipline-allow: process\.env|@/lib/config|@/lib/db -->
<!-- planner-discipline-allow: @/lib/db -->
<!-- planner-discipline-allow: @/lib/config -->
<!-- planner-discipline-allow: process\.env -->
<!-- planner-discipline-allow: #[0-9a-fA-F]{3,8} -->

<tasks>

<task type="tracer">
  <name>Task 1: Seeded proposals on screen, polling, end to end</name>
  <reversibility rating="costly">The ProposalRow and DecisionRow shapes and the lib/dashboard location are the display contract both transports and, later, Phase 8's CopilotKit surface read; renaming a field afterwards touches the query module, both route handlers and both list components.</reversibility>
  <precondition>Phase 1 and Phase 4 are merged into this workspace: app/layout.tsx, app/globals.css with the shadcn-init token blocks, components.json, lib/db.ts, lib/utils.ts, utils/time.ts and prisma/schema.prisma all exist; prisma/generated has been produced by bun install; Postgres is up on :5432; and prisma/seed.ts has been run so at least one Proposal and at least one ignored Decision exist in the database.</precondition>
  <files>lib/dashboard/queries.ts, app/api/proposals/route.ts, app/providers.tsx, app/layout.tsx, app/page.tsx, components/proposal-queue.tsx, components/status-chip.tsx, components/ui/table.tsx, components/ui/badge.tsx, components/ui/card.tsx</files>
  <read_first>
    - prisma/schema.prisma: exact Proposal and Decision field names, the enum names, which columns are nullable, and whether confidence is Float or Decimal
    - lib/db.ts: the exported Prisma singleton name and its import path
    - utils/time.ts: the exact exported HKT formatter name and its parameter type; never edit this file
    - app/layout.tsx: what Phase 1's scaffold already put there (font block, metadata, body classes) before adding anything
    - app/page.tsx: the Phase 1 Prisma-count placeholder this task replaces
    - app/globals.css: the token names shadcn init generated, so component classes only use tokens that already exist
    - package.json: the exact dev and check script bodies, read only, never modified
    - components/ui/table.tsx, components/ui/badge.tsx, components/ui/card.tsx: after the shadcn add, for the real exported names and prop shapes
  </read_first>
  <action>
Preflight first, then the single path. Do not write a component before step 1 passes.

Step 1, port and Fast Refresh preflight (D-14, research correction 1). Start the dev server in the background with Phase 1's dev script and read its banner. If the banner does not show port 3002, stop it and restart as `bun run dev -p 3002` (bun forwards the flag through) or with PORT set in the invoking environment rather than in a dotenv file, which Next cannot read for the port. Never edit the shared dev script, because main uses :3000, and never force bun's own runtime for it. Then save one file once and confirm the terminal prints exactly one compile-and-ready pair for that save; repeated pairs mean a bun-runtime flag leaked into the invocation.

Step 2, shadcn primitives (D-01, D-18). Run `bunx shadcn@4.21.0 add table badge card` with the version pinned, never a floating tag. Immediately run Phase 1's biome script; if that script is check-only, run `bunx @biomejs/biome check --write .` instead. Do not pass a bun-runtime flag to biome either: PROJECT.md forbids it repo-wide. Leave every generated file under components/ui unmodified. Then run `git status --porcelain package.json bun.lock` and record the result in the SUMMARY; if the CLI touched either file, note that it is regenerated with bun install at merge and never hand-merged (D-03). No npm package is installed in this phase.

Step 3, lib/dashboard/queries.ts, the single read path. Export `interface ProposalRow` with id string, title string, status string, startHkt string, confidence number or null. Export `interface DecisionRow` with id string, verdict string, whenHkt string, confidence number or null, reason string, messageText string, sourceChannel string. Export `async function getProposalRows(): Promise<ProposalRow[]>` reading `prisma.proposal.findMany` with orderBy `[{ created_at: "desc" }, { id: "desc" }]`; the id tiebreak is what makes two rows with an identical created_at order deterministically instead of arbitrarily. Export `async function getDecisionRows(): Promise<DecisionRow[]>` reading `prisma.decision.findMany` with orderBy `[{ source_ts: "desc" }, { id: "desc" }]`, because Decision has no created_at column and none may be added. Map every returned field explicitly, one by one; never spread a Prisma row into the return value, so no credential or internal column can reach the wire. Format Proposal.start through the Phase 1 HKT formatter imported from the utils/time alias path. For whenHkt, convert Decision.source_ts (Slack epoch seconds as a string) to a Date by multiplying the parsed number by 1000; if the parsed number is not finite, emit an em dash instead of an invalid date. Map both confidence columns as: null stays null, otherwise pass through `Number(...)`, so a Prisma Decimal and a Float both cross the JSON boundary and the RSC prop boundary as a plain number. This module is the only file in the phase that touches the Prisma singleton and the only place a date is ever formatted. TSDoc on both interfaces and both functions, no `any` (D-22).

Step 4, app/api/proposals/route.ts. Export one `async function GET()` returning a JSON response built from `await getProposalRows()`, and nothing else. No route segment config: GET handlers have been dynamic by default since Next 15, so a force-dynamic export would be wrong here. Keep it parameterless, with no query string, no filtering and no request argument, which removes the entire injection surface (threat T-06-04).

Step 5, app/providers.tsx. A "use client" component exporting `Providers`, holding one QueryClient created inside `useState` using the lazy initializer form so it is not recreated on re-render, and returning QueryClientProvider around its children. This is library provider injection, not app state, so the repo's no-Context-for-app-state rule does not apply (D-20, D-21). It reads no environment value and imports no config or database module.

Step 6, app/layout.tsx. Wrap the existing children in Providers. Change nothing else in this task: leave Phase 1's metadata, html and body structure and any existing font block exactly as they are, because 06-02 replaces the font block.

Step 7, components/status-chip.tsx. Export `StatusChip` taking a single status string prop and mapping all six enum values to a token class plus a symbol: pending uses the primary token with a filled dot; confirmed uses the success token with a check; dismissed uses the muted token with a cross; already_scheduled uses the secondary token with a right arrow; acted uses the success token with a check; ignored uses the muted token with an em dash. Any unrecognised value falls back to the muted token with a question mark plus the raw value, so an unknown status is visible rather than blank. Render through the shadcn Badge primitive with the label in uppercase. Every filled chip pairs its fill token with its own foreground token, never the general foreground token, because white text on amber measures 1.71:1 and on cyan 1.67:1 (research contrast findings). Use token utility classes only, no colour literal of any kind (DSH-04). The symbol is what makes status readable without colour (DSH-05, D-12).

Step 8, components/proposal-queue.tsx. A "use client" component exporting `ProposalQueue` with an `initialData: ProposalRow[]` prop. Import the row type with a type-only import from the query module so the server module never enters the client bundle (threat T-06-02). Call useQuery with queryKey `["proposals"]`, a queryFn that fetches `/api/proposals`, throws on a non-ok response, and parses the body as the row array, plus initialData, `refetchInterval: 4000`, and `refetchIntervalInBackground: true`; the background flag is what keeps the poll alive while the presenter is in Slack triggering the demo row. Render a shadcn Table inside a Card with columns STATUS, TITLE, START (HKT) and CONFIDENCE. Put a data-status attribute carrying the row status on each TableRow so row state is inspectable and greppable in the served HTML. Render startHkt verbatim, never re-parsed and never re-formatted, and never call Intl or Date in this file. Render a null confidence as an em dash. When the list is empty render exactly one row spanning the table reading NO PROPOSALS YET; when the query is in an error state render exactly one row reading FEED UNAVAILABLE beside the last known rows. No button, link, checkbox, row click handler or any other control: the dashboard is read-only and approval belongs to the Slack card (D-07).

Step 9, app/page.tsx. Replace the Phase 1 placeholder entirely with an async Server Component whose first statement awaits `connection()` imported from next/server, giving a request-time render so a production build never prerenders frozen seed rows, then awaits getProposalRows, then renders ProposalQueue with the rows as initialData. The page passes rows as props; it never fetches its own API route.
  </action>
  <verify>
    <automated>bunx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line containing "error TS" is printed</fails_when>
    <automated>bun run check</automated>
    <fails_when>non-zero exit, or the biome summary line reports one or more errors</fails_when>
    <automated>curl -sf http://localhost:3002/api/proposals | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const a=JSON.parse(s);if(!Array.isArray(a)||a.length===0)process.exit(1);const r=a[0];if(typeof r.startHkt!=="string"||r.startHkt.length===0)process.exit(1);if(!(r.confidence===null||typeof r.confidence==="number"))process.exit(1);console.log("proposals ok",a.length)})'</automated>
    <fails_when>non-zero exit, or the line starting "proposals ok" is not printed, which is what happens when the endpoint 404s, returns an empty array, returns a raw date instead of a pre-formatted string, or serializes confidence as a string</fails_when>
    <automated>curl -sf http://localhost:3002/ | grep -q 'data-status='</automated>
    <fails_when>non-zero exit, because the first-paint HTML carries no proposal row and the Server Component path is not rendering rows</fails_when>
    <automated>! grep -rEn "process\.env|@/lib/config|@/lib/db" components app/providers.tsx --include="*.tsx" --exclude-dir=ui</automated>
    <fails_when>non-zero exit, because grep printed a file-and-line match: a client component is reaching for an environment value, the config module or the database client</fails_when>
    <human-check>With the dashboard tab open at http://localhost:3002, insert one Proposal row directly into Postgres with psql or a one-off script and watch the browser without touching it: the new row appears within about 5 seconds with no reload. This is the DSH-03 half of the phase exit criterion.</human-check>
  </verify>
  <acceptance_criteria>
    - `bunx tsc --noEmit` exits 0 and prints no line containing "error TS".
    - The proposals endpoint assertion above prints a line starting "proposals ok" with a count of at least 1.
    - `curl -sf http://localhost:3002/ | grep -c 'data-status='` returns 1 or more.
    - `grep -c 'import type' components/proposal-queue.tsx` returns 1 or more.
    - `grep -c 'refetchIntervalInBackground' components/proposal-queue.tsx` returns exactly 1.
    - `grep -rn "dangerouslySetInnerHTML" app components --exclude-dir=ui` prints nothing.
    - The `git status --porcelain package.json bun.lock` result is recorded in the SUMMARY, either unchanged or noted for bun install regeneration at merge.
  </acceptance_criteria>
  <done>Seeded proposals are visible at http://localhost:3002 with status, HKT start time and confidence, the page is server-rendered, and the client list keeps polling the proposals endpoint every 4 seconds.</done>
</task>

<task type="auto">
  <name>Task 2: Decision log as its own view, plus the nav between them</name>
  <files>app/api/decisions/route.ts, app/decisions/page.tsx, components/decision-log.tsx, components/nav.tsx, app/layout.tsx</files>
  <read_first>
    - lib/dashboard/queries.ts: the DecisionRow shape and getDecisionRows exactly as Task 1 wrote them
    - components/proposal-queue.tsx: the table, empty-state, error-state and polling shape this view mirrors
    - components/status-chip.tsx: the verdict values it already maps
    - app/layout.tsx: the current structure after Task 1's Providers wrap
    - app/api/proposals/route.ts: the thin handler shape to mirror
  </read_first>
  <action>
Mirror the proven slice onto the Decision log. No new pattern is invented here.

Step 1, app/api/decisions/route.ts. The same thin shape as the proposals handler, wrapping getDecisionRows. Parameterless GET, no route segment config.

Step 2, components/decision-log.tsx. A "use client" component exporting `DecisionLog` with an `initialData: DecisionRow[]` prop, a type-only import of the row type, and useQuery with queryKey `["decisions"]`, a queryFn fetching `/api/decisions` that throws on a non-ok response, initialData, `refetchInterval: 4000` and `refetchIntervalInBackground: true`. Render a shadcn Table inside a Card with columns VERDICT, WHEN (HKT), CONFIDENCE, AGENT REASON and MESSAGE. The reason column header says AGENT REASON rather than REASON: the text is the model's stated reason about someone's message, and labelling it as the agent's is what keeps it from reading as an unattributed judgement of a colleague. Put a data-verdict attribute carrying the row verdict on each TableRow. Render reason and messageText as plain JSX text children so React escapes them, which is the entire XSS control for this Slack-sourced and model-sourced text (threat T-06-01); never render either through a raw-HTML prop. Constrain long message text with a CSS truncation utility, never with a JavaScript string slice, so multi-byte and emoji text can never be severed mid-character (edge DSH-05 encoding). Render a null confidence and an unparseable timestamp as an em dash. An empty list renders exactly one row reading NO DECISIONS YET; an error state renders exactly one row reading FEED UNAVAILABLE. No controls of any kind.

Step 3, app/decisions/page.tsx. The same Server Component shape as the queue page: await connection first, then await getDecisionRows, then render DecisionLog with the rows as initialData.

Step 4, components/nav.tsx. Export `Nav` with two next/link links: the root route labelled QUEUE and the decisions route labelled DECISIONS. These two links are the only genuinely interactive elements on either page, so give each an explicit visible focus treatment with the focus-visible outline utilities bound to the ring token: outline width 2, outline offset 2, outline colour from the ring token. The offset is what stops the ring visually merging with an adjacent hard border (edge DSH-06 adjacency). Do not add manual tab-stop attributes to table rows anywhere: rows are not interactive in a read-only dashboard, and adding stops would put dead entries in the tab order (research correction 5). DOM order is the tab order, so do not set any positive tab index value (edge DSH-06 ordering).

Step 5, app/layout.tsx. Render Nav above the children inside the Providers wrap so both views share it. Still no font changes in this task.
  </action>
  <verify>
    <automated>bunx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line containing "error TS" is printed</fails_when>
    <automated>curl -sf http://localhost:3002/api/decisions | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const a=JSON.parse(s);if(!Array.isArray(a))process.exit(1);const ig=a.find(r=>r.verdict==="ignored");if(!ig)process.exit(1);if(typeof ig.reason!=="string"||ig.reason.length===0)process.exit(1);if(typeof ig.whenHkt!=="string"||ig.whenHkt.length===0)process.exit(1);console.log("decisions ok",a.length)})'</automated>
    <fails_when>non-zero exit, or the line starting "decisions ok" is not printed, which is what happens when the endpoint is missing, no ignored row is returned, or the reason or HKT string is empty</fails_when>
    <automated>curl -sf http://localhost:3002/decisions | grep -q 'data-verdict="ignored"'</automated>
    <fails_when>non-zero exit, because the server-rendered decisions page contains no ignored row</fails_when>
    <automated>! grep -rEn "\.slice\(0, *[0-9]+\)" components/decision-log.tsx</automated>
    <fails_when>non-zero exit, because grep printed a match: message text is being truncated in JavaScript instead of with a CSS utility</fails_when>
    <human-check>Load http://localhost:3002, click DECISIONS in the nav, then click QUEUE: both views render their own table, neither view shows the other's rows, and no approve, reject or edit control appears anywhere on either page.</human-check>
  </verify>
  <acceptance_criteria>
    - The decisions endpoint assertion prints a line starting "decisions ok".
    - `curl -sf http://localhost:3002/decisions | grep -c 'data-verdict="ignored"'` returns 1 or more.
    - `grep -c 'AGENT REASON' components/decision-log.tsx` returns exactly 1.
    - `grep -rn "onClick" components --exclude-dir=ui` prints nothing.
    - `grep -c 'refetchIntervalInBackground' components/decision-log.tsx` returns exactly 1.
    - Both `/` and `/decisions` return HTTP 200 from the running dev server.
  </acceptance_criteria>
  <done>The Decision log is a separate route showing verdict, HKT time, confidence, the agent's reason and the message, including at least one ignored row, and the two views are linked by the nav.</done>
</task>

<task type="auto">
  <name>Task 3: Impeccable layout pass on the two views</name>
  <files>app/page.tsx, app/decisions/page.tsx, app/layout.tsx, components/proposal-queue.tsx, components/decision-log.tsx, components/nav.tsx, components/status-chip.tsx</files>
  <read_first>
    - The theme_direction block below, which is the brand and design context fed inline to every impeccable call this phase
    - components/proposal-queue.tsx and components/decision-log.tsx as written in Tasks 1 and 2
    - app/layout.tsx: the current shell that layout changes apply to
  </read_first>
  <action>
Run the structure half of the impeccable sequence while the structure is fresh (D-15: layout first, typeset and colorize in 06-02, critique and polish in 06-03).

Invoke the impeccable skill with the layout command against the dashboard surface, naming the two page files as the target. There is no PRODUCT.md and no DESIGN.md in this repo and no `/impeccable init` is run, deliberately (D-15, PROJECT.md out of scope), so paste the theme_direction block below into the invocation as the brand and design context it would otherwise read from a design file. If the skill's context launcher reports that context loading did not run, continue: that is the documented fallback, not a blocker.

Apply only the structural recommendations it returns: spacing scale, alignment, column order and widths, page shell and heading hierarchy, micro-label placement, table density, and empty-state composition. Explicitly out of scope for this task, and to be declined if the tool proposes them: any colour literal, any new npm dependency, any mutation or control, any new route, any change to the query module or the route handlers, any font change (that is 06-02's typeset step), and any edit to files under components/ui. Legibility on a projector outranks density: if a recommendation makes text smaller or lower contrast, decline it.

Then run Phase 1's biome script to normalise formatting, and re-run the type check.

Time box: if this task has not converged within roughly five minutes, keep what has been applied and move on. Structure is the cheapest thing to fix later; the theme and the critique-polish pair are what must survive (D-19).
  </action>
  <verify>
    <automated>bunx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line containing "error TS" is printed</fails_when>
    <automated>bun run check</automated>
    <fails_when>non-zero exit, or the biome summary line reports one or more errors</fails_when>
    <automated>! grep -rEn "#[0-9a-fA-F]{3,8}" app components lib/dashboard --include="*.tsx" --include="*.ts" --exclude-dir=ui</automated>
    <fails_when>non-zero exit, because grep printed one or more file-and-line matches: a colour literal reached a component or the query module</fails_when>
    <automated>curl -sf http://localhost:3002/ | grep -q 'data-status='</automated>
    <fails_when>non-zero exit, because the layout pass broke the server-rendered proposal rows</fails_when>
    <automated>curl -sf http://localhost:3002/decisions | grep -q 'data-verdict='</automated>
    <fails_when>non-zero exit, because the layout pass broke the server-rendered decision rows</fails_when>
    <human-check>Both views still show their rows after the layout pass, the columns read left to right in a sensible order, and nothing overlaps or overflows at a projector-sized window.</human-check>
  </verify>
  <acceptance_criteria>
    - The hex-literal gate above prints nothing and exits 0.
    - Both pages still return HTTP 200 and still contain their row marker attributes.
    - `git status --porcelain components/ui package.json bun.lock` prints nothing beyond what Task 1 already recorded for the shadcn add (no layout-pass edits reached those paths).
    - The SUMMARY records which layout recommendations were applied and which were declined, with the reason.
  </acceptance_criteria>
  <done>The queue and the Decision log have a deliberate, projector-legible structure, with no colour or font work done yet and no colour literal anywhere.</done>
</task>

</tasks>

<theme_direction>
## Theme direction fed inline to every impeccable call (D-11, D-12, D-15)

Retro, PostHog-spirited, dark, and legible from the back of a room on a projector. Not neo-brutalist parody, not terminal-cosplay: a dense operator console that a developer would trust.

Palette, as CSS custom properties in `app/globals.css` only (values are the D-11 starting point; impeccable may propose stronger values as long as they stay dark, retro and projector-legible):

| Token | Value | Role |
|---|---|---|
| background | `#0B0B0C` | page ground |
| card | `#141416` | panel surface |
| elevated | `#1C1C20` | raised surface, row hover |
| border | `#2E2E33` | hard structural border |
| foreground | `#EDEDEF` | primary text |
| muted label colour | `#8A8A93` | secondary text and micro-labels |
| primary | `#F2A93B` | amber, pending and focus ring |
| secondary | `#4EC9E0` | cyan, already-scheduled |
| success | `#46C07A` | confirmed and acted |
| destructive | `#E5544B` | error states |

Devices, applied consistently: 1 to 2 pixel hard borders; 4px 4px 0 offset shadows with no blur; 2 to 4 pixel radii; monospace with tabular numerals for data, timestamps, confidence, chips and table columns; proportional sans for prose; uppercase letter-spaced micro-labels; status encoded in form (chip, stripe or symbol) as well as colour. No gradients, no glass, no soft elevation, no animation.

Accessibility facts already computed (WCAG relative luminance, research contrast findings): amber on the ground is 9.85:1, cyan 10.08:1, success 8.51:1, destructive 5.34:1, all passing. White text on a filled amber chip is 1.71:1 and on filled cyan 1.67:1, both failing badly, so every filled chip uses dark text. The border token against the ground is 1.46:1, which is fine for decorative dividers but is a candidate to lighten if a stronger palette is proposed.
</theme_direction>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres to Next.js server | Row content originating in Slack messages and model output crosses into the render path |
| Next.js server to browser | Route-handler JSON and RSC props cross into an untrusted client bundle |
| Browser to Next.js route handler | Unauthenticated GET requests on the workspace dev port |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering / Information Disclosure | `components/decision-log.tsx`, `components/proposal-queue.tsx` | medium | mitigate | Render `reason`, `message_text` and `title` as plain JSX text children so React escapes them; no raw-HTML prop anywhere. Gated by the `dangerouslySetInnerHTML` grep in Tasks 1 and 2. |
| T-06-02 | Information Disclosure | `components/*.tsx`, `app/providers.tsx` | high | mitigate | Client components import no environment value, no config module and no database client; the row type crosses as a type-only import. Gated by the negative grep in Task 1. |
| T-06-03 | Information Disclosure | `lib/dashboard/queries.ts` | medium | mitigate | Every returned field is mapped explicitly; no Prisma row is spread into a response, so no credential column (for example a stored refresh token on a related record) can ever reach the wire. |
| T-06-04 | Tampering | `app/api/proposals/route.ts`, `app/api/decisions/route.ts` | low | mitigate | Both handlers are parameterless GETs with no query-string filtering, which removes the injection surface entirely (ASVS V5). |
| T-06-05 | Information Disclosure | Both route handlers | medium | accept | The dashboard has no auth anywhere in this build; it serves localhost on a workspace dev port for a single-laptop demo, and PROJECT.md scopes authentication out. Accepted with the field projection of T-06-03 limiting what is exposed. |
| T-06-06 | Denial of Service | Polling plus unbounded findMany | low | accept | Four-second polling of a seeded demo table on one laptop. Accepted for the window; a row cap would be the fix if the table ever grew. |
| T-06-SC | Tampering | Supply chain | low | mitigate | Zero npm packages are installed this phase, so the package legitimacy gate does not apply. The shadcn CLI is invoked at the pinned 4.21.0 and generates source files rather than resolving a new dependency; `git status` on package.json and bun.lock is checked after the add. |

Spoofing, Repudiation and Elevation of Privilege have no surface in this phase: there is no identity, no session, no write path and no privileged operation.
</threat_model>

<verification>
- Dev server is on :3002 and a single save produces a single build pair (D-14).
- `bunx tsc --noEmit` clean; Phase 1's biome check clean (D-18).
- The proposals endpoint returns a non-empty array of pre-formatted rows; the decisions endpoint returns at least one ignored row with a non-empty reason.
- Both pages return 200 and carry their row marker attributes in the server-rendered HTML.
- A hand-inserted Proposal row appears within about 5 seconds with no reload.
- No colour literal in any `.ts` or `.tsx` file under app, components or lib/dashboard.
- No client file reaches for an environment value, the config module or the database client.
- No control on either view mutates anything.
</verification>

<success_criteria>
DSH-01, DSH-02 and DSH-03 are demonstrably satisfied against real seeded data, and ROADMAP success criteria 1 and 2 pass by hand in under a minute each. The theme is deliberately not done yet: that is 06-02.
</success_criteria>

<output>
Create `.planning/phases/06-dashboard/06-01-SUMMARY.md` when done. Record: the port the dev server actually used and how it was set; whether package.json or bun.lock changed after the shadcn add; the exact exported name and parameter type of the HKT formatter as found in utils/time.ts; whether confidence is Float or Decimal and whether it is nullable; and which impeccable layout recommendations were applied versus declined.
</output>
