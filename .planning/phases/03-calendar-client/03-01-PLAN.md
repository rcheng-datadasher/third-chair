---
phase: 03-calendar-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/calendar/google-client.ts
  - lib/calendar/smoke.ts
  - lib/calendar/hkt-rfc3339.ts
  - lib/calendar/freebusy.ts
  - lib/config.ts
autonomous: true
requirements:
  - CAL-01

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Running `bun lib/calendar/smoke.ts` exchanges A's stored refresh token under bun and prints `[scope-check] PASS` with the granted scope list only, before any Calendar call (D-01, D-13)"
    - "A token whose scopes cannot serve both freebusy and events.insert/get (for example `calendar.events` alone) prints `[scope-check] FAIL`, exits non-zero, and makes no Calendar call (D-01)"
    - "`checkConflicts(userId, startIso, endIso)` keeps Phase 1's exact signature and returns A's real busy blocks from `freebusy.query` on the primary calendar, with no pending-Proposal union (D-05)"
    - "The logged freebusy request shows `timeMin`/`timeMax` ending in `+08:00` with `timeZone` `Asia/Hong_Kong`, and the logged busy blocks are re-rendered with `+08:00` offsets whatever format Google returns (D-04, success criterion 1)"
    - "`getCalendarClient` for a user with no `google_refresh_token` throws an explicit error naming that user id and never falls back to another user's token (D-15)"
    - "A freebusy response carrying per-calendar `errors`, or no calendar entry, throws instead of returning an empty conflict list, so a failed lookup never reads as free (D-05)"
    - "Google OAuth client id/secret come only from `lib/config.ts`, Postgres is only read, and only `lib/calendar/**` plus an extend-only `google` section of `lib/config.ts` change; no schema push (D-02, D-13, D-14, D-16, D-17)"
  artifacts:
    - path: "lib/calendar/google-client.ts"
      provides: "OAuth2 client and Calendar v3 client for a user, built from that user's stored refresh token"
      exports: ["getGoogleAuth", "getCalendarClient"]
    - path: "lib/calendar/hkt-rfc3339.ts"
      provides: "RFC3339 formatter with an explicit +08:00 offset"
      exports: ["toHktRfc3339"]
    - path: "lib/calendar/freebusy.ts"
      provides: "Real freebusy.query conflict lookup on the organizer's primary calendar"
      exports: ["checkConflicts"]
      contains: "freebusy.query"
    - path: "lib/calendar/smoke.ts"
      provides: "One-command live check: token scope first, then freebusy as A"
      contains: "[scope-check]"
  key_links:
    - from: "lib/calendar/freebusy.ts"
      to: "lib/calendar/google-client.ts"
      via: "getCalendarClient(userId) builds the authenticated Calendar client"
      pattern: "getCalendarClient\\("
    - from: "lib/calendar/google-client.ts"
      to: "lib/db.ts"
      via: "prisma user lookup selecting google_refresh_token"
      pattern: "google_refresh_token"
    - from: "lib/calendar/google-client.ts"
      to: "lib/config.ts"
      via: "OAuth client id/secret from the config google section"
      pattern: "config\\.google"
    - from: "lib/calendar/freebusy.ts"
      to: "lib/calendar/hkt-rfc3339.ts"
      via: "toHktRfc3339 normalizes the request window and re-renders busy blocks"
      pattern: "toHktRfc3339\\("
    - from: "lib/calendar/smoke.ts"
      to: "lib/calendar/freebusy.ts"
      via: "live call for the Thu 17 Sep 2026 HKT window"
      pattern: "checkConflicts\\("
  prohibitions:
    - statement: "MUST NOT write to Google Calendar or Postgres on the availability-check path: getGoogleAuth, getCalendarClient and checkConflicts only read (one user row, one freebusy.query), so checking availability can never create, move or delete anything on anyone's calendar"
      status: "resolved"
      verification: "judgment"
---

<objective>
Stand up the real Google Calendar client for user A and deliver CAL-01: a token scope check, then a live `freebusy.query` with explicit `+08:00` offsets, filled into Phase 1's `checkConflicts` stub.

Phase goal (ROADMAP, verbatim): "Calendar client creates a real event with a Meet link, invites the participant, and is idempotent on retry". This plan is the first half of the D-02 split (03-01: OAuth client, scope check, freebusy with HKT offsets. 03-02: insert with Meet link, deterministic id, 409 fallback, demo tag, invite). MVP note: the ROADMAP Goal line is not written as a user story; no story is invented here (run `/gsd mvp-phase 03` if one is wanted).

Purpose: D-01 makes one real OAuth2 client and one real `freebusy.query` under bun the literal first move. A runtime problem, an expired token, or a too-narrow scope then surfaces in minutes and is never debugged as calendar logic. 03-02 builds `events.insert` on this plan's `getCalendarClient` and `toHktRfc3339`.

Output: `lib/calendar/google-client.ts`, `lib/calendar/hkt-rfc3339.ts`, a real `checkConflicts` body in `lib/calendar/freebusy.ts`, and `lib/calendar/smoke.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-calendar-client/03-CONTEXT.md
@.planning/phases/03-calendar-client/03-RESEARCH.md
@.planning/phases/03-calendar-client/03-PATTERNS.md
@CLAUDE.md

<interfaces>
Contracts fixed by Phase 1 (consume exactly as written on disk; the 03-RESEARCH.md sketches are guesses, including its `@/prisma/generated/prisma/client` import path):

- `lib/calendar/freebusy.ts` stub: `checkConflicts(userId: string, startIso: string, endIso: string): Promise<ConflictSlot[]>` (returns `[]` today). Signature is LOCKED. Only the body changes.
- `types/agent.ts`: `interface ConflictSlot { startIso: string; endIso: string; reason: string }`. Import it, never redeclare it.
- `lib/db.ts`: `export const prisma` (Prisma 7 client with `@prisma/adapter-pg`, globalThis singleton). Never construct a second PrismaClient.
- `lib/config.ts`: `export const config`, one Zod parse of the env with sections `slack`/`google`/`ai`/`db`/`trigger`/`agent`/`graph`. It is the only env reader.
- Prisma `User` columns (snake_case, no `@map`): `id`, `team_id`, `slack_user_id`, `email`, `google_refresh_token` (nullable), `tz`.
- Seed (Phase 1 D-13/D-14): user A has a refresh token, user B has none, and one pending Proposal sits at Thu 17 Sep 2026 15:00 HKT with B as participant.

Downstream consumers of this plan's exports (keep compatible):
- Phase 7 (CFL-01) calls `checkConflicts` and unions pending Proposals itself.
- Phase 10 (10-RESEARCH.md Open Question 1) reuses `getCalendarClient(userId)` for demo cleanup.
- 03-02 imports `getCalendarClient` and `toHktRfc3339`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "A's token works for Calendar under bun": scope check, then one live freebusy.query (one path only)</name>
  <precondition>Phase 1 artifacts exist: `lib/calendar/freebusy.ts` stub, `lib/db.ts`, `lib/config.ts`, and a `User` row with non-null `google_refresh_token` (seeded A); the worktree root `.env` parses through `lib/config.ts`.</precondition>
  <files>lib/calendar/google-client.ts, lib/calendar/smoke.ts, lib/config.ts</files>
  <read_first>
    - lib/config.ts (does a `google` section with the OAuth client id/secret already exist, and under what names)
    - lib/db.ts (exact `prisma` export and the generated-client import path)
    - lib/calendar/freebusy.ts and lib/calendar/create-event.ts (Phase 1 stubs: import style, `@/` alias vs relative)
    - prisma/schema.prisma (User model field names; read only)
    - tsconfig.json (confirm the `@/*` path alias) and package.json (confirm `googleapis` 180.0.0 is installed)
    - .planning/phases/03-calendar-client/03-RESEARCH.md §"Pitfall A" and §"Code Examples" google-client.ts (read the orchestrator correction block)
  </read_first>
  <action>
Build the thinnest real path DB → config → OAuth2 → tokeninfo → freebusy.query, run under bun (D-01). Follow the repo rules: kebab-case files, named exports, TSDoc on every function, Biome only, no new dependencies.

1. Read the real Phase 1 files listed above first. Match their export names, import style and generated Prisma client path exactly.

2. Config (D-13, D-17). If `config.google` already exposes the OAuth client id and secret, change nothing and use those names. Otherwise extend `lib/config.ts` additively. Add env keys `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET` (both `z.string().min(1)`) to the existing schema and map them to `config.google.clientId` and `config.google.clientSecret`. Do not touch other sections and never add a second env reader.

3. Create `lib/calendar/google-client.ts` with two named async exports:
   - `getGoogleAuth(userId: string)`:
     - Read the token with `prisma.user.findUniqueOrThrow({ where: { id: userId }, select: { google_refresh_token: true } })`.
     - If the token is null or empty, throw `new Error` with the message "User <userId> has no Google refresh token; cannot call Google Calendar" (D-15). Never fall back to another user's token.
     - Construct `new google.auth.OAuth2(clientId, clientSecret)` from the config values, call `setCredentials({ refresh_token })`, and return the client.
     - Type the client via inference or googleapis' re-exported `Auth.OAuth2Client`. Do not import `google-auth-library` directly: it is transitive and not in package.json.
   - `getCalendarClient(userId: string)`: return `google.calendar({ version: "v3", auth: await getGoogleAuth(userId) })`, typed `calendar_v3.Calendar`. Phase 10 reuses this export.

   No module-level cache. Never log the token.

4. Create `lib/calendar/smoke.ts`, a script run directly with `bun lib/calendar/smoke.ts` (bun loads the root `.env`). Its file-header TSDoc says it calls live Google APIs as user A. It has an async `main()`:
   a. Find A with `prisma.user.findFirstOrThrow({ where: { google_refresh_token: { not: null } } })`.
   b. Scope check, before any Calendar call:
      - `const auth = await getGoogleAuth(A.id)`, then `const { token } = await auth.getAccessToken()`. Throw if `token` is empty.
      - `const info = await auth.getTokenInfo(token)`. Log ONLY `info.scopes` joined by spaces.
      - Freebusy-capable means `info.scopes` includes one of `https://www.googleapis.com/auth/calendar`, `https://www.googleapis.com/auth/calendar.readonly`, `https://www.googleapis.com/auth/calendar.freebusy` or `https://www.googleapis.com/auth/calendar.events.freebusy`.
      - Insert/get-capable means it includes `https://www.googleapis.com/auth/calendar` or `https://www.googleapis.com/auth/calendar.events`.
      - Both satisfied: print `[scope-check] PASS scopes=<list>`.
      - Otherwise: print `[scope-check] FAIL missing=<freebusy and/or insert> scopes=<list>`, set `process.exitCode = 1`, and return before any Calendar call. `calendar.events` alone is a FAIL.
   c. First live call. Get `getCalendarClient(A.id)` and call `calendar.freebusy.query` with requestBody `{ timeMin: "2026-09-17T00:00:00+08:00", timeMax: "2026-09-18T00:00:00+08:00", timeZone: "Asia/Hong_Kong", items: [{ id: "primary" }] }`. Log `JSON.stringify(res.data.calendars)`, then print `[smoke] freebusy ok`. Keep that exact tag in every later version of this script.
   d. Wrap `main()` with a catch that prints `[smoke] FAILED` plus only `err?.response?.status ?? err?.status` and `err?.message`, and sets `process.exitCode = 1`. Never print the error object itself: a GaxiosError's `config` can carry the Authorization header. Add a finally that awaits `prisma.$disconnect()`, so the pg pool does not keep the process alive.

5. Run `bunx --bun @biomejs/biome check --write lib/calendar lib/config.ts`, then run the smoke script.

6. Triage by signal:
   - An error naming a missing Node built-in, or a module-resolution failure, is a runtime problem, not calendar logic (D-01). Retry once with `bunx tsx --env-file=.env lib/calendar/smoke.ts`, per Phase 1 D-18's fallback.
   - `invalid_grant` means A's refresh token expired or was revoked. That is a pre-window checklist miss.
   - `[scope-check] FAIL`, or a 403 with `insufficientPermissions`, is a scope problem.
   - For `invalid_grant` or a scope FAIL: STOP and raise a dynamic `checkpoint:human-action` (auth gate). The user re-consents A with `https://www.googleapis.com/auth/calendar`, updates A's `User.google_refresh_token` (Phase 1 `bun run db:seed` with the new token in `.env`), and then the smoke run is repeated. Never change code to work around a scope or token failure.
  </action>
  <verify>
    <automated>bun lib/calendar/smoke.ts</automated>
    <fails_when>non-zero exit; or output contains `[scope-check] FAIL`, `invalid_grant`, `insufficientPermissions` or `[smoke] FAILED`; or no `[scope-check] PASS scopes=https://www.googleapis.com/auth/` line; or no `[smoke] freebusy ok` line; or output contains a substring starting `ya29.` or `1//` (a leaked access or refresh token)</fails_when>
    <automated>bunx --bun @biomejs/biome check lib/calendar lib/config.ts</automated>
    <fails_when>non-zero exit, or the summary reports one or more errors</fails_when>
  </verify>
  <acceptance_criteria>
    - `lib/calendar/google-client.ts` exports `getGoogleAuth` and `getCalendarClient`, each taking `userId: string` and each with a TSDoc block
    - `lib/calendar/google-client.ts` contains `findUniqueOrThrow`, `google_refresh_token`, `setCredentials(` and `throw new Error(`
    - `grep -rn "process\.env" lib/calendar/ | grep -v -E ":[0-9]+:\s*(//|\*)"` prints nothing (config goes through `lib/config.ts` only)
    - `lib/calendar/smoke.ts` contains `getTokenInfo(`, `[scope-check] PASS`, `[scope-check] FAIL`, `[smoke] freebusy ok` and `$disconnect(`
    - Smoke output shows `[scope-check] PASS scopes=` followed by at least one `https://www.googleapis.com/auth/calendar` URL, then `[smoke] freebusy ok`, and the process exits 0 without hanging
    - If `lib/config.ts` changed, `git diff "$(git merge-base HEAD develop)" -- lib/config.ts` (substitute `main` if no `develop` branch exists) shows only added lines (the Google keys) and no removed lines
  </acceptance_criteria>
  <done>A's refresh token exchanges under bun. Its granted scopes cover freebusy and insert/get, and only the scope list is logged. One live `freebusy.query` for Thu 17 Sep 2026 HKT returns through `getCalendarClient`.</done>
</task>

<task type="auto">
  <name>Task 2: Real checkConflicts with +08:00 request and HKT-rendered response logging (CAL-01)</name>
  <files>lib/calendar/hkt-rfc3339.ts, lib/calendar/freebusy.ts, lib/calendar/smoke.ts</files>
  <read_first>
    - lib/calendar/freebusy.ts (Phase 1 stub: the signature is a locked contract)
    - types/agent.ts (`ConflictSlot` shape)
    - lib/calendar/google-client.ts and lib/calendar/smoke.ts (from Task 1)
    - utils/time.ts (Phase 1 display formatter: do not modify it and do not reuse it for RFC3339; it is outside lib/calendar ownership)
    - .planning/phases/03-calendar-client/03-RESEARCH.md §"Pitfall B" and §"Code Examples" hkt-rfc3339.ts / freebusy.ts
  </read_first>
  <action>
1. Create `lib/calendar/hkt-rfc3339.ts` exporting `toHktRfc3339(d: Date): string`, with TSDoc.
   - If `Number.isNaN(d.getTime())`, throw `new Error` with the message "toHktRfc3339: invalid Date". This one guard covers every caller, including 03-02's insert.
   - Otherwise add 8 hours to the timestamp and relabel the ISO string's trailing milliseconds-and-Z suffix as `+08:00`. A fixed offset is correct because Asia/Hong_Kong has no DST. Use stdlib only, no date library (Phase 1 D-08).
   - `2026-09-17T07:00:00.000Z` must become `2026-09-17T15:00:00+08:00`.
   - It lives in lib/calendar, not utils/time.ts (D-16 ownership).

2. In `lib/calendar/freebusy.ts`, replace only the body of `checkConflicts`. The name, parameter list, return type and the `ConflictSlot` import from types/agent stay exactly as Phase 1 wrote them. New body:
   - Compute `timeMin = toHktRfc3339(new Date(startIso))` and `timeMax = toHktRfc3339(new Date(endIso))`. Every request then carries `+08:00` whatever offset the caller passed (D-04), and an unparseable input throws before any network call.
   - `const calendar = await getCalendarClient(userId)`. Build requestBody `{ timeMin, timeMax, timeZone: "Asia/Hong_Kong", items: [{ id: "primary" }] }`. Log `[freebusy] request` with `JSON.stringify(requestBody)`.
   - `const res = await calendar.freebusy.query({ requestBody })`. Read the single calendar entry as `Object.values(res.data.calendars ?? {})[0]`: one calendar is queried, so do not depend on the response key.
   - If the entry is missing or `entry.errors` is non-empty, throw `new Error` naming the userId and including `JSON.stringify` of the errors. A failed lookup must never read as free.
   - Map every busy block that has both `start` and `end` to `{ startIso: toHktRfc3339(new Date(start)), endIso: toHktRfc3339(new Date(end)), reason: "Busy on calendar" }`.
   - Log `[freebusy] response raw` (the busy array as Google returned it) and `[freebusy] response hkt` (the mapped slots). Google may answer in `Z`; the hkt line is what success criterion 1 checks (Pitfall B).
   - Return the slots. There is no pending-Proposal union (that is CFL-01 in Phase 7, D-05), no Postgres access and no retry.

3. Update `lib/calendar/smoke.ts`:
   - Right after finding A, add a self-check: `toHktRfc3339(new Date("2026-09-17T07:00:00.000Z"))` must equal `2026-09-17T15:00:00+08:00`. If not, print `[smoke] hkt-rfc3339 FAILED` and set `process.exitCode = 1`.
   - Keep the scope check unchanged.
   - Replace the raw `calendar.freebusy.query` call with `checkConflicts(A.id, "2026-09-17T00:00:00+08:00", "2026-09-18T00:00:00+08:00")` and print `[smoke] freebusy ok slots=<count>`.
   - An empty slot list is valid here: A's calendar may be free that day. 03-02 re-checks the rendering against a real busy block once the seeded event exists.
   - Keep the demo slots (Fri 18 Sep 2026 11:00 and 10:30 HKT) out of every window used here.

4. Run `bunx --bun @biomejs/biome check --write lib/calendar`, then the verify commands below.
  </action>
  <verify>
    <automated>bun lib/calendar/smoke.ts</automated>
    <fails_when>non-zero exit; or output contains `[smoke] FAILED` or `[smoke] hkt-rfc3339 FAILED`; or the `[freebusy] request` line lacks `"timeMin":"2026-09-17T00:00:00+08:00"` or `"timeZone":"Asia/Hong_Kong"`; or no `[freebusy] response hkt` line; or no `[smoke] freebusy ok slots=` line</fails_when>
    <automated>bunx --bun @biomejs/biome check lib/calendar</automated>
    <fails_when>non-zero exit, or the summary reports one or more errors</fails_when>
    <automated>bunx tsc --noEmit</automated>
    <fails_when>any `error TS` line whose file path starts with `lib/calendar/`; or output contains `This is not the tsc command you are looking for` (typescript not installed, so nothing was checked). Errors only in other paths predate this phase: record them in the SUMMARY, do not fix them.</fails_when>
  </verify>
  <acceptance_criteria>
    - `lib/calendar/hkt-rfc3339.ts` exports `toHktRfc3339` taking `d: Date` and returning `string`, and contains `+08:00` and `invalid Date`
    - `git diff "$(git merge-base HEAD develop)" -- lib/calendar/freebusy.ts` (substitute `main` if no `develop` branch exists) shows no change to the `checkConflicts` name, parameter list (`userId: string, startIso: string, endIso: string`) or return type (`Promise<ConflictSlot[]>`)
    - `lib/calendar/freebusy.ts` contains `freebusy.query(`, `Asia/Hong_Kong`, `[freebusy] request`, `[freebusy] response raw`, `[freebusy] response hkt` and `errors`
    - `grep -rn "interface ConflictSlot" lib/calendar/` prints nothing (the type is imported from types/agent, never redeclared)
    - `lib/calendar/freebusy.ts` has exactly three import statements: `ConflictSlot` (types/agent), `getCalendarClient` (./google-client) and `toHktRfc3339` (./hkt-rfc3339)
    - Smoke output's `[freebusy] request` line contains `+08:00` twice (timeMin and timeMax); every `startIso`/`endIso` on the `[freebusy] response hkt` line ends in `+08:00`
  </acceptance_criteria>
  <done>`checkConflicts` returns A's real busy blocks as `ConflictSlot[]` from `freebusy.query`, and logs the `+08:00` request and an HKT-rendered response. Its Phase 1 signature is unchanged and it throws on a failed lookup.</done>
</task>

</tasks>

## Edge Coverage (spec-less probe fallback)

| Probe item | Status | Disposition |
|---|---|---|
| CAL-01 unclassified ("review manually") | unresolved | Flagged assumption FA-1 below (never auto-resolved) |

No-silent-drop equality: 1 surfaced = 0 authored into `must_haves` + 1 flagged assumption.

Prohibition recall (Stage 1 → Stage 2): one kept prohibition, a read-only availability check (in `must_haves.prohibitions`, descriptor-less, flagged-unverified). Canon referral: leaking OAuth tokens or the client secret through logs is canon secret handling. It is covered by T-03-01 below and `/gsd-secure-phase` and is not minted here.

## Flagged assumptions

- **FA-1 (CAL-01 probe, unresolved):** the offset format of Google's freebusy `busy[].start/end` is undocumented (research A4). The plan logs a locally re-rendered `+08:00` copy, so success criterion 1 does not depend on it. The raw line may legitimately show `Z`.
- **FA-2 (research A3):** the seeded token's scope is unknown until Task 1 runs. A FAIL is a re-consent auth gate, not a code change.
- **FA-3:** the freebusy `calendars` map is read by taking its single value, not the `primary` key. This is correct whichever key Google echoes.

## Artifacts this phase produces

| Artifact | Kind | Plan |
|---|---|---|
| `lib/calendar/google-client.ts` | new file | 03-01 |
| `getGoogleAuth(userId: string)` returning the OAuth2 client | new export | 03-01 |
| `getCalendarClient(userId: string)` returning `calendar_v3.Calendar` | new export (Phase 10 reuses it) | 03-01 |
| `lib/calendar/hkt-rfc3339.ts` | new file | 03-01 |
| `toHktRfc3339(d: Date): string` | new export | 03-01 |
| `checkConflicts(userId, startIso, endIso): Promise<ConflictSlot[]>` real body | Phase 1 stub filled, signature unchanged | 03-01 |
| `lib/calendar/smoke.ts` | new file, script, no exports | 03-01, extended in 03-02 |
| Log tags `[scope-check] PASS`, `[scope-check] FAIL`, `[smoke] freebusy ok`, `[smoke] FAILED`, `[smoke] hkt-rfc3339 FAILED`, `[freebusy] request`, `[freebusy] response raw`, `[freebusy] response hkt` | log contract used by verify commands | 03-01 |
| `config.google.clientId`, `config.google.clientSecret` from env `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET` | conditional additive config keys (only if Phase 1 lacks them) | 03-01 |
| `lib/calendar/event-id.ts`, `deriveEventId(proposalId: string): string` | new file and export | 03-02 |
| `createCalendarEvent(proposal, organizerUserId?: string): Promise<{ eventId: string; meetLink: string; htmlLink: string }>` | Phase 1 stub filled; new optional param `organizerUserId`, new return field `htmlLink` | 03-02 |
| Module-private `extractMeetLink(event)`, `isConflict(err)` | internal helpers in create-event.ts | 03-02 |
| Error names `MissingOrganizerError`, `CancelledEventError` | thrown Error `name` values | 03-02 |
| Log tags `[create-event] inserted id=`, `[create-event] 409 -> events.get`, `[create-event] no attendees`, `[smoke] eventId=`, `[smoke] idempotent OK`, `[smoke] busy-block OK` | log contract used by verify commands | 03-02 |
| `.planning/phases/03-calendar-client/COVERAGE.md` | API coverage matrix (Calendar v3) | planning |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres → lib/calendar | A's refresh token is read from `User.google_refresh_token` (plaintext at rest, Phase 1) |
| lib/calendar → Google OAuth/Calendar APIs | Bearer access token over TLS; responses are untrusted input |
| process → terminal logs | Anything printed may be screen-shared at the demo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `smoke.ts` scope check and error logging | high | mitigate | Log only `info.scopes`. The error handler prints status and message, never the error object (a GaxiosError config can hold the Authorization header). Task 1's verify fails on any `ya29.` or `1//` substring in output |
| T-03-02 | Elevation of Privilege | `getGoogleAuth(userId)` | high | mitigate | Resolves the token for exactly the requested user id. A missing token throws an explicit error (D-15); there is no fallback to any other user's token |
| T-03-03 | Information Disclosure | refresh token at rest in Postgres | medium | accept | Inherited Phase 1 debt (01-CONTEXT specifics, `/ponytail-debt` candidate), one-laptop demo; this plan only reads it |
| T-03-04 | Tampering | `checkConflicts` date inputs | low | mitigate | `toHktRfc3339` throws on unparseable dates before the request is built (ASVS V5) |
| T-03-05 | Denial of Service | freebusy quota (self-inflicted) | low | mitigate | One `freebusy.query` per call, no retry loop |
| T-03-06 | Tampering | freebusy response with per-calendar errors read as "free" | medium | mitigate | Throw when the entry is missing or `errors` is non-empty, so a failed lookup never yields an empty conflict list |

No package installs in this plan (googleapis 180.0.0 was installed in Phase 1; research's legitimacy audit approved it), so no supply-chain row applies.
</threat_model>

<assumption_delta_decision>
noun: Proposal (idempotency key, later mapped to one Google Calendar event id)
decision: no-change
rationale: The detector's weak signals ("runs alongside", "409→events.get fallback", "a second run", "custom event id") are scheduling and idempotency words. One Proposal still maps to exactly one calendar event, so there is no singular-to-plural identity change.
</assumption_delta_decision>

<verification>
- `bun lib/calendar/smoke.ts` exits 0 and prints `[scope-check] PASS`, `[freebusy] request` (with `+08:00`), `[freebusy] response hkt` and `[smoke] freebusy ok slots=`
- `bunx --bun @biomejs/biome check lib/calendar` exits 0
- `bunx tsc --noEmit` reports no `error TS` under `lib/calendar/`
- Only `lib/calendar/**` (plus, if needed, additive lines in `lib/config.ts`) changed; `prisma/schema.prisma`, `lib/slack/**`, `app/**`, `components/**` and `lib/agent/**` are untouched (D-16)
</verification>

<success_criteria>
- Success criterion 1 (request half): a logged `freebusy.query` for a known HKT window shows `+08:00` offsets in the request and an HKT-rendered response. The busy-block half is re-asserted in 03-02 against the seeded event.
- A's token scope is proven sufficient for freebusy and insert/get before 03-02 starts.
- `checkConflicts` is real, signature-compatible for Phase 7, and `getCalendarClient` is exported for 03-02 and Phase 10.
</success_criteria>

<output>
Create `.planning/phases/03-calendar-client/03-01-SUMMARY.md` when done. Record the logged scope list (scopes only), whether `lib/config.ts` was extended, and any pre-existing `tsc` errors outside `lib/calendar/`.
</output>
