---
phase: 04-approval-bridge
plan: 01
type: execute
wave: 2
depends_on:
  - "04-02"
files_modified:
  - lib/slack/approve.ts
  - lib/slack/handlers/approve-proposal.ts
  - lib/slack/handlers/reject-proposal.ts
  - lib/slack/blocks.ts
  - package.json
  - bun.lock
autonomous: false
requirements:
  - APR-01
  - APR-02
  - APR-03
  - APR-04

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "[D-01, D-02] Phase 2's and Phase 3's branches are ancestors of `main` through `develop`; `bun.lock` was regenerated by `bun install` after resolving `package.json` by hand, never hand-merged, and no conflict marker survives anywhere in the tree"
    - "[D-02] `bunx prisma db push` ran if and only if `prisma/schema.prisma` differs between the pre-merge `main` sha and the merged `main`; when it is unchanged no push happens at all"
    - "[D-01] `/ponytail-review` ran on the merged Wave A diff and its findings are recorded in the SUMMARY (DMO-05 discipline; the requirement is signed off at Phase 7)"
    - "[D-03, D-21] Exactly one Bolt process runs across every worktree after the merge, started from the merged tree with the runtime 02-01-SUMMARY.md recorded"
    - "[APR-01, D-04, D-06] Clicking Approve on a real card runs only ordinary backend code in `lib/slack/approve.ts`: read row, token guard, organizer claim, `createCalendarEvent`, persist, `updateProposalCard`. No Next.js route, no Trigger.dev task, no graph resume and no model call is involved"
    - "[APR-04, D-10] The card updates in place to a confirmed chip carrying the Google Calendar link and the Meet link, on the stored `card_channel`/`card_ts`, and no second Slack message appears"
    - "[APR-02, D-07] The organizer claim is the locked conditional UPDATE run through a `prisma.$queryRaw` tagged template with bound parameters; its SQL text is unchanged from CONTEXT.md D-07, with no extra condition and no subquery"
    - "[APR-02, D-08, success criterion 4] A click by a user with no `google_refresh_token` returns `not_organizer` before the claim is attempted, so no Google Calendar call and no database write happen on that path"
    - "[APR-03, D-09, success criterion 2] A second Approve click on a confirmed proposal re-renders the same confirmed card and returns `not_pending`; the calendar still holds exactly one event for that proposal"
    - "[APR-03, D-09] A Slack redelivery of the same click behaves identically to a double click: the `status !== \"pending\"` early return plus Phase 3's deterministic event id and 409 fallback mean exactly one calendar event exists"
    - "[D-07] When the claim returns no row and the current `organizer_user_id` is a different user, the clicker gets ephemeral feedback only, the Proposal row is not written at all, and the outcome is `already_scheduled`"
    - "[D-07] When the claim returns no row but the current `organizer_user_id` is the clicker's own `User.id`, the flow continues into the idempotent `createCalendarEvent` path instead of stopping"
    - "[APR-04] A confirmed proposal whose `meet_link` or `calendar_html_link` is an empty string renders the confirmed card without throwing and without printing an empty link element"
    - "[APR-05 boundary, D-05] An unknown or malformed proposal id from the button `value` resolves to `not_found` through `findUnique`, produces ephemeral feedback and never throws"
    - "[D-11] Reject sets `status = dismissed` only while the row is still pending and updates the card to a dismissed chip; no Calendar call happens on the reject path"
    - "[D-11] Reject clicked on a row that is already confirmed or dismissed changes no status and re-renders the card in its current state"
    - "[D-06] A proposal whose `card_channel` or `card_ts` is missing still completes the approve sequence and logs that the card update was skipped, instead of throwing"
    - statement: "[APR-03 concurrency] Two Approve clicks racing for the same pending proposal produce exactly one calendar event: Postgres serialises the conditional UPDATE so one clicker claims the row, and the loser returns `already_scheduled` with no write"
      verification: backstop
    - statement: "[APR-03 interruption] If the process dies after Google commits the insert but before the Prisma persist, the next Approve click by the same organizer re-enters through the lost-claim same-user branch, hits Phase 3's 409 fallback and persists the same event id"
      verification: backstop
    - "[D-18, D-19, D-20] Only `lib/slack/approve.ts`, `lib/slack/handlers/approve-proposal.ts`, `lib/slack/handlers/reject-proposal.ts` and `lib/slack/blocks.ts` change; `lib/agent/graph.ts`, `app/**`, `components/**`, `lib/calendar/**` and `prisma/schema.prisma` are untouched, and Phase 2's builders and registrations are extended, never restructured"
  artifacts:
    - path: "lib/slack/approve.ts"
      provides: "Bolt-free approve and reject domain logic reused unchanged by Phase 7's choose_alt handler"
      exports: ["approveProposal", "rejectProposal", "ApproveOutcome", "RejectOutcome"]
      contains: "organizer_user_id IS NULL"
    - path: "lib/slack/handlers/approve-proposal.ts"
      provides: "Thin block_actions listener: ack, read the button value, call approveProposal, render ephemeral feedback"
      exports: ["handleApproveProposal"]
      contains: "approveProposal("
    - path: "lib/slack/handlers/reject-proposal.ts"
      provides: "Thin block_actions listener calling rejectProposal"
      exports: ["handleRejectProposal"]
      contains: "rejectProposal("
    - path: "lib/slack/blocks.ts"
      provides: "buildConfirmedBlocks extended with the calendar link and the Meet link (APR-04)"
      contains: "calendar_html_link"
  key_links:
    - from: "lib/slack/handlers/approve-proposal.ts"
      to: "lib/slack/approve.ts"
      via: "listener passes action.value and body.user.id into the domain function"
      pattern: "approveProposal\\("
    - from: "lib/slack/approve.ts"
      to: "lib/calendar/create-event.ts"
      via: "createCalendarEvent(proposal, clicker.id) after a won or self-owned claim"
      pattern: "createCalendarEvent\\("
    - from: "lib/slack/approve.ts"
      to: "lib/slack/update-proposal-card.ts"
      via: "chat.update on the stored card_channel/card_ts, never the click payload"
      pattern: "updateProposalCard\\("
    - from: "lib/slack/approve.ts"
      to: "lib/db.ts"
      via: "the shared Prisma singleton; the raw conditional claim runs through $queryRaw"
      pattern: "\\$queryRaw"
    - from: "lib/slack/blocks.ts"
      to: "Proposal columns"
      via: "confirmed chip renders calendar_html_link and meet_link"
      pattern: "meet_link"
  prohibitions:
    - statement: "MUST NOT attempt any Google Calendar call for a clicker whose User row has no Google refresh token"
      status: resolved
      verification: test
    - statement: "MUST NOT write any status value onto a still-pending Proposal when the conditional claim returns no row"
      status: resolved
      verification: test
    - statement: "MUST NOT take the channel or message timestamp for the card update from the interaction payload instead of the stored columns"
      status: resolved
      verification: test
    - statement: "MUST NOT alter the locked claim SQL text, add a condition to its WHERE clause, or build it by string concatenation instead of a bound tagged template"
      status: resolved
      verification: test
    - statement: "MUST NOT import Bolt types or the Bolt entry module into the approve domain module"
      status: resolved
      verification: test
    - statement: "MUST NOT edit lib/agent/graph.ts internals, app/**, components/** or prisma/schema.prisma in this plan"
      status: resolved
      verification: judgment
    - statement: "MUST NOT place any secret, token or connection string in source; every value comes from the config module"
      status: resolved
      verification: test
---

**Phase goal (ROADMAP, verbatim):** Approving a proposal from Slack writes a real Calendar event and updates the card, with a working local/Trigger.dev transport switch.

**User story:** not framed. The ROADMAP Goal line for Phase 4 is not in "As a / I want to / so that" form, and MVP mode forbids inventing one. Surfaced to the orchestrator; run `/gsd mvp-phase 4` to author a story.

<objective>
Merge Wave A onto `main`, then make the Approve button real. An Approve click on a live Slack card reads the Proposal row, refuses a clicker who holds no Google token, claims the organizer with the locked conditional UPDATE, creates the real Calendar event through Phase 3's idempotent wrapper, persists the event id and links, and updates the same card in place to a confirmed chip carrying both links. Reject dismisses the row and updates the chip without touching Google.

Decisions implemented:
- **Merge window:** D-01 (Phase 2 and Phase 3 into `develop`, then `main`, `/ponytail-review` on the merged diff), D-02 (never hand-merge the lockfile; conditional schema push), D-03 (one Bolt, restarted from the merged tree).
- **Approve:** D-04 (ordinary backend code in `lib/`), D-05 (real proposal id from the button value), D-06 (read, claim, create, persist, update the stored card), D-07 (byte-identical claim SQL), D-08 (token-holder guard before any Calendar write), D-09 (exactly one event under double click or redelivery), D-10 (confirmed card shows both links).
- **Reject:** D-11 (dismissed status, dismissed chip, no Calendar call).
- **Ownership and rules:** D-18, D-19, D-20 (extend Phase 2's handlers and builders, never restructure; graph internals, `app/**` and `components/**` untouched), D-21 (Bolt and Postgres only), D-22 (shared Prisma singleton, config-only env, TSDoc, Biome, no test files).

Purpose: this is the product's whole promise — nothing reaches the calendar until a human clicks, and the click that reaches it is the token holder's. Everything downstream (Phase 7's `choose_alt`, the rehearsal, the recording) runs through this function.

Output: `lib/slack/approve.ts` (new), real listener bodies in `lib/slack/handlers/approve-proposal.ts` and `lib/slack/handlers/reject-proposal.ts`, and `buildConfirmedBlocks` extended with both links.
</objective>

<assumption_delta_decision>
primary_noun: Proposal (organizer = the token-holding User claimed on the row)
decision: no-change
rationale: the detector fired on scheduling and timeline prose ("Runs alongside", "2nd slot", "Second:"), not on an identity-model change. One Proposal still has exactly one organizer and at most one calendar event.
detector_hits: "Runs alongside", "2nd slot", "Second:"
</assumption_delta_decision>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-approval-bridge/04-CONTEXT.md
@.planning/phases/04-approval-bridge/04-RESEARCH.md
@.planning/phases/04-approval-bridge/04-PATTERNS.md
@CLAUDE.md

## Interfaces consumed (read the real merged files; the research sketches are the contract only until the merge lands)

- `lib/calendar/create-event.ts` (Phase 3, COMMITTED contract): `createCalendarEvent(proposal, organizerUserId?: string): Promise<{ eventId: string; meetLink: string; htmlLink: string }>`. The organizer resolves as `organizerUserId ?? proposal.organizer_user_id`; with neither it throws an Error named `MissingOrganizerError` before any Google or database call. It is 409-safe through a deterministic event id plus `events.get`, and `meetLink`/`htmlLink` may be `""`. `organizerUserId` is the internal `User.id`. Do not edit any file under `lib/calendar/`.
- `lib/slack/update-proposal-card.ts` (Phase 2): `updateProposalCard(channel, ts, proposal): Promise<void>` — three arguments; it picks the status-appropriate blocks from `proposal.status` itself. Keep the signature.
- `lib/slack/blocks.ts` (Phase 2): `buildApprovalBlocks`, `buildConfirmedBlocks`, `buildDismissedBlocks`, and whatever else 02-02 shipped (an already-scheduled builder may exist). Extend `buildConfirmedBlocks` additively; never change action ids or button values.
- `lib/slack/handlers/approve-proposal.ts` / `reject-proposal.ts` (Phase 2): export `handleApproveProposal` / `handleRejectProposal`; `await ack()` is the literal first awaited statement. `lib/slack/bolt.ts` is registration-only (Phase 2 D-17) and needs no edit in this plan.
- `lib/db.ts`: `export const prisma` — one client per process behind a `globalThis` guard (D-22). Never construct a client in a listener.
- Prisma schema (Phase 1, snake_case, no `@map`): `Proposal` id, team_id, dedupe_key, title, start, end, tz, status (`pending|confirmed|dismissed|already_scheduled`), organizer_user_id, calendar_event_id, calendar_html_link, meet_link, card_channel, card_ts, confidence, alternatives, source_channel, source_ts, created_at. `User` id, team_id, slack_user_id, email, google_refresh_token, tz, with `@@unique([team_id, slack_user_id])` exposed as the compound key `team_id_slack_user_id`.
- Conventions: `@/` tsconfig alias imports, kebab-case files, named exports, no `any`, TSDoc on every function, Biome only.

## Downstream contract (keep compiling; additive only)

- Phase 7's `choose_alt` handler calls `approveProposal` directly after rewriting `start`/`end` from the stored alternatives. It must pass `body.user.id` as the **second** argument: the required second parameter turns a one-argument call into a type error rather than a silent bypass of the token guard (Phase 7's research assumed a one-argument call).
- Phase 10's reset script deletes events tagged `extendedProperties.private.demo = "true"`; this plan writes no calendar tags of its own.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave A merge — Phase 2 and Phase 3 into develop, develop into main, conditional schema push, ponytail-review, single Bolt restarted</name>
  <sync_note>
**Orchestrator sync note (2026-09-12 13:35 — read before Step 1).** Reality differs from the text below; the text's intent stands, the branch names and the already-merged set do not:
- Phase 3 is ALREADY on `main` (`1b7a618`, branch `worktree-agent-aa4dd6a82adc92e03`). Phase 5 is ALREADY on `main` (`4c5cc68`). Only Phase 2 remains to merge.
- Phase 2's branch is `worktree-agent-a8631508a07016037` (no `gsd/phase-2-slack-surface` exists). Its 02-02 plan is being executed by ANOTHER session right now.
- **Gate:** `git show worktree-agent-a8631508a07016037:.planning/phases/02-slack-surface/02-02-SUMMARY.md` must succeed before you merge. If it does not exist yet, STOP at a `checkpoint:human-verify` ("02-02 not finished — say 'go' when its SUMMARY is committed") and do nothing else in this plan.
- **Known conflicts** (dry-run `git merge-tree --write-tree main worktree-agent-a8631508a07016037`): `lib/slack/blocks.ts` and `lib/slack/bolt.ts` — Phase 5's edit-approve additions (`buildEditApproveBlocks`, `EDIT_APPROVE_MODAL_CALLBACK_ID`, `app.action("edit_approve_proposal")`, `app.view(...)`) vs 02-02's status chips and Reject registration. Resolve by keeping BOTH sides' additions — both tracks were told to extend, never restructure. Possibly `lib/config.ts` (02-01 vs 04-02): keep both. Prefer asking the 02-02 session to merge `main` into its branch first if it is still open; otherwise resolve here.
- Replace `gsd/phase-2-slack-surface` with `worktree-agent-a8631508a07016037` and `gsd/phase-3-calendar-client` with `worktree-agent-aa4dd6a82adc92e03` in every command and verify line below. `develop` may not exist — create it from `main`.
- The one Bolt rule still applies: 02-02's session and the Phase 6 session (`feature/dashboard-layout` worktree, no Bolt) — kill any Bolt found before Step 6.
  </sync_note>
  <precondition>The branches `gsd/phase-2-slack-surface` and `gsd/phase-3-calendar-client` exist locally with their phase work committed, and `02-01-SUMMARY.md` records the winning Bolt runtime (`bun` or `tsx`).</precondition>
  <reversibility rating="costly">Merging two tracks into `main` is recoverable with `git revert`/`git reset`, but every later phase branches from the result, so an unwound merge means re-merging by hand under the clock. D-01 locks the order; no decision checkpoint.</reversibility>
  <files>package.json, bun.lock</files>
  <read_first>
    - .planning/phases/02-slack-surface/02-01-SUMMARY.md and 02-02-SUMMARY.md (winning Bolt runtime, any D-06 cut, the real file layout Phase 2 shipped)
    - .planning/phases/03-calendar-client/03-01-SUMMARY.md and 03-02-SUMMARY.md (the real createCalendarEvent signature, whether the D-18 invite cut was taken, pre-existing tsc errors)
    - .planning/phases/04-approval-bridge/04-CONTEXT.md D-01, D-02, D-03
    - .planning/ROADMAP.md §"File Ownership Matrix" (the `package.json` + `bun.lock` row) and §"Phase 4"
    - package.json and prisma/schema.prisma on `main` before the merge
  </read_first>
  <action>
**Step 1 — kill every Bolt first (D-03).** Run `ps aux | grep -i bolt` and `pkill -f 'slack/bolt.ts'`. Every worktree shares this WSL process table, and Socket Mode load-balances across every open connection on the app token, so a stray listener silently steals the clicks Task 2 depends on. Nothing may hold the app token until Step 6.

**Step 2 — record the pre-merge baseline.** Capture the current `main` sha into a shell variable (`PRE_MERGE_SHA`, from `git rev-parse main`) and note it in the SUMMARY. Step 5's schema decision compares against it.

**Step 3 — merge into `develop`, then into `main` (D-01).** Check out `develop` (create it from `main` if Phase 1 did not), merge `gsd/phase-2-slack-surface`, then `gsd/phase-3-calendar-client`, then check out `main` and merge `develop`.
- Phase 2 and Phase 3 own disjoint directories, so the only realistic conflicts are `package.json`, `bun.lock`, and possibly `types/` or `lib/config.ts` if either track extended them.
- **Never hand-merge the lockfile (D-02).** Resolve `package.json` by taking the union of both sides' dependency entries with every existing pin unchanged, then discard the conflicted lockfile and regenerate it with `bun install` (which also reruns the `postinstall` `prisma generate`), then stage the regenerated lockfile.
- For a `types/` or `lib/config.ts` conflict, keep both sides' additions — both tracks were told to extend, never fork.
- No new dependency is introduced by this phase; if the merged `package.json` gained a package neither Phase 2's nor Phase 3's SUMMARY mentions, stop and report it rather than installing it.

**Step 4 — prove the merge is clean.** No conflict marker may survive in any tracked file, and the two branch tips must both be ancestors of `main`.

**Step 5 — conditional schema push (D-02). [BLOCKING for every later task in this phase.]** Compare `prisma/schema.prisma` between `PRE_MERGE_SHA` and the merged `HEAD` with `git diff --quiet <PRE_MERGE_SHA> HEAD -- prisma/schema.prisma`. Only when that reports a difference, run `bunx prisma db push`. When the schema is unchanged, run nothing and say so in the SUMMARY. No database-touching verification anywhere in this phase may run before this step has been decided.

**Step 6 — continue on the phase branch and restart the single Bolt (D-03, D-21).** Confirm the tree you will write Phase 4 code in carries the merged Wave A code (`lib/slack/blocks.ts`, `lib/slack/update-proposal-card.ts` and `lib/calendar/create-event.ts` all exist). If `gsd/phase-4-approval-bridge` does not exist, create it from the merged `main`; if it does, merge the merged `main` into it. Then start exactly one Bolt with the runtime `02-01-SUMMARY.md` recorded (`bun lib/slack/bolt.ts`, or `bunx tsx lib/slack/bolt.ts` if tsx won), teeing output to `/tmp/ai-secretary-bolt.log`. Never `--bun`, never Node type stripping.

**Step 7 — ponytail review (D-01).** Run `/ponytail-review` over the merged Wave A diff (`git diff <PRE_MERGE_SHA>..HEAD`). Record its findings verbatim in the SUMMARY. Apply only findings that are one-line and risk-free inside the files this phase already owns; defer everything else to the SUMMARY as a note — this is the DMO-05 discipline pass, not a refactor window. DMO-05 itself is signed off at Phase 7.

**Step 8 — record.** The SUMMARY names: `PRE_MERGE_SHA`, whether the schema push ran, the conflicted files and how each was resolved, the winning Bolt runtime, any cut either Wave A phase recorded, and any pre-existing `tsc` error inherited from the merge.
  </action>
  <verify>
    <automated>git merge-base --is-ancestor gsd/phase-2-slack-surface HEAD; a=$?; git merge-base --is-ancestor gsd/phase-3-calendar-client HEAD; b=$?; test "$a$b" = "00"</automated>
    <fails_when>non-zero exit — `$a` or `$b` is non-zero, so one of the two Wave A branch tips is not an ancestor of the working branch and that track's work is not actually merged (a status above 1 means git itself failed, which is also a failure, never a pass)</fails_when>
    <automated>test -z "$(grep -rln -E '^(<{7}|={7}|>{7})' --include='*.ts' --include='*.tsx' --include='*.json' --include='*.prisma' lib types app components prisma/schema.prisma package.json 2>/dev/null)"</automated>
    <fails_when>non-zero exit, and the same command without `-z` prints the paths — a conflict marker survived the merge. An empty result with a missing directory is not a pass on its own: `lib`, `types`, `app`, `components`, `prisma/schema.prisma` and `package.json` must all exist after the merge, which the next criterion checks</fails_when>
    <automated>test -e bun.lock && test ! -e package-lock.json</automated>
    <fails_when>non-zero exit — the regenerated bun lockfile is missing, or an npm lockfile was written into a bun-only repo</fails_when>
    <automated>bunx tsc --noEmit</automated>
    <fails_when>any `error TS` line. An error inherited from a Wave A branch is recorded in the SUMMARY and fixed only if it blocks Task 2; an error that names a symbol from both tracks at once is a real merge defect and must be fixed here</fails_when>
    <automated>bunx biome check lib</automated>
    <fails_when>non-zero exit, or the summary line reports one or more errors</fails_when>
    <automated>test "$(pgrep -f 'slack/bolt' | wc -l)" = 1</automated>
    <fails_when>the count is 0 (no Bolt is running, so no click can be received) or 2 or more (a second Socket Mode connection will steal clicks — kill it before continuing)</fails_when>
  </verify>
  <acceptance_criteria>
    - Both Wave A branch tips are ancestors of the current `HEAD` (first automated command exits 0)
    - No conflict marker exists in any tracked `.ts`, `.tsx`, `.json`, `.prisma` or `.md` file (second command exits 0)
    - `bun.lock` exists and `package-lock.json` does not
    - `lib/slack/blocks.ts`, `lib/slack/update-proposal-card.ts`, `lib/slack/handlers/approve-proposal.ts` and `lib/calendar/create-event.ts` all exist in the working tree
    - The SUMMARY states the pre-merge `main` sha and, in one line, either that `prisma/schema.prisma` was unchanged and no push ran, or that it changed and `bunx prisma db push` succeeded
    - The SUMMARY contains the `/ponytail-review` findings on the merged diff
    - `pgrep -f 'slack/bolt'` matches exactly one process
  </acceptance_criteria>
  <done>Phase 2's and Phase 3's work is on `main` through `develop` with a regenerated lockfile and no conflict markers, the schema push decision is made and recorded, the merged diff has been ponytail-reviewed, and exactly one Bolt process is running from the merged tree.</done>
</task>

<task type="tracer" tdd="false" gate="blocking-human">
  <name>Task 2 (tracer): One Approve click end to end — Slack card, token guard, organizer claim, real Calendar event, confirmed card with both links</name>
  <precondition>User A's Google refresh token still exchanges for a fresh access token (the pre-window checklist item), A is the Slack user who will click, and the Slack app can post to the watched channel.</precondition>
  <reversibility rating="costly">`approveProposal(proposalId, clickerSlackUserId)` is the exported seam Phase 7's `choose_alt` handler plans against. Changing the name, the argument order or the outcome union later means editing Phase 7's plan as well. The two-argument shape is fixed here deliberately.</reversibility>
  <files>lib/slack/approve.ts, lib/slack/handlers/approve-proposal.ts, lib/slack/blocks.ts</files>
  <read_first>
    - lib/slack/handlers/approve-proposal.ts (Phase 2's real body — the ack-first shape and the BlockButtonAction narrowing to keep)
    - lib/slack/blocks.ts (the real `buildConfirmedBlocks` Phase 2 shipped: its parameter, its existing element order, and any already-scheduled builder)
    - lib/slack/update-proposal-card.ts (confirm the three-argument signature and that it picks blocks from `proposal.status`)
    - lib/calendar/create-event.ts (the real exported signature and return shape; read only, never edit)
    - lib/db.ts (the `prisma` export and the generated-client import path)
    - lib/agent/graph.ts (read only, never edit — confirm the stub sets `team_id` and a Participant on the Proposals it creates, and that it stores `card_channel`/`card_ts`)
    - prisma/schema.prisma (Proposal and User column names; read only, never edited or pushed)
    - .planning/phases/04-approval-bridge/04-RESEARCH.md §"Approve handler: organizer claim + token guard", §"Pitfall 1", §"Pitfall 5", §"Pitfall 6"
    - .planning/phases/04-approval-bridge/04-PATTERNS.md §"lib/slack/approve.ts" and §"Shared Patterns"
    - .planning/phases/01-foundation-hardcoded-round-trip/01-CONTEXT.md D-02, D-03 (action id and button value conventions)
  </read_first>
  <action>
Wire ONE path all the way through: a real Approve click by A on a real card becomes a real Google Calendar event and a confirmed chip on the same Slack message. No other click path is built in this task.

**1. Create `lib/slack/approve.ts` (D-04).** It is plain backend code: it imports the Prisma singleton from `@/lib/db`, `createCalendarEvent` from `@/lib/calendar/create-event` and `updateProposalCard` from `@/lib/slack/update-proposal-card`, and nothing from the Slack framework or the Bolt entry module — Phase 7 calls this function directly, and the Trigger.dev import-graph rule forbids that dependency anywhere near it. TSDoc on every function.

Export a string-union type `ApproveOutcome` with the members `confirmed`, `not_found`, `not_pending`, `not_organizer` and `already_scheduled`, and an async function `approveProposal(proposalId: string, clickerSlackUserId: string): Promise<ApproveOutcome>`. The team id is read from the row (`proposal.team_id`); there is no third parameter. Body, in this order (D-06):

   a. `prisma.proposal.findUnique({ where: { id: proposalId } })`. A null row returns `not_found` — an unknown or hand-crafted button value is a miss, never a throw.

   b. When `proposal.status` is not `pending`, call `updateProposalCard(proposal.card_channel, proposal.card_ts, proposal)` to re-render whatever state the row already holds and return `not_pending`. This single branch is what makes a double click and a Slack redelivery both harmless (D-09).

   c. Look the clicker up with the compound unique key: `prisma.user.findUnique({ where: { team_id_slack_user_id: { team_id: proposal.team_id, slack_user_id: clickerSlackUserId } } })`. Reading `team_id` off the row is what keeps the signature at two parameters. If `lib/agent/graph.ts` turns out not to populate `team_id` on the rows it creates, fall back to the configured team id from `@/lib/config` and record that finding in the SUMMARY.

   d. **Token guard (D-08), before the claim.** When there is no clicker row, or its `google_refresh_token` is null or empty, return `not_organizer` immediately. Nothing is written and no Google call is made on this path — this is the whole of success criterion 4, and it is a pre-read rather than an extra SQL condition precisely so that step (e) stays byte-identical.

   e. **The organizer claim (D-07, APR-02).** Run the locked conditional UPDATE through `prisma.$queryRaw` as a tagged template typed `<{ id: string }[]>`: set `organizer_user_id` to the clicker's `User.id` where the id matches and `organizer_user_id` is null, returning the id. Interpolate the clicker id and the proposal id as template expressions so Prisma binds them as parameters. Do not extend the WHERE clause with any further condition or nested query, do not switch to the unsafe raw variant, and do not fold the status transition into this statement — the status change goes through the ordinary Prisma update in step (g), which handles the enum for you.

   f. When the claim returns zero rows, re-read the row and compare its `organizer_user_id` to the clicker's `User.id`. A different user means someone else owns it: return `already_scheduled`. That branch performs no Prisma write of any kind and leaves the row's status exactly as it found it — writing a terminal status onto a still-pending Proposal would lock out a pre-set organizer who has not clicked yet, and the real organizer's click would then hit the step (b) early return and never create an event. The same user means this is the clicker's own retry: fall through to step (g).

   g. Call `createCalendarEvent(proposal, clicker.id)` and destructure `eventId`, `meetLink` and `htmlLink`. Passing the claimed organizer's `User.id` explicitly avoids a stale-row read of the column just written. Then `prisma.proposal.update` the row with `calendar_event_id`, `calendar_html_link`, `meet_link`, `organizer_user_id` set to the clicker id and `status` set to confirmed, and keep the updated row.

   h. Call `updateProposalCard(updated.card_channel, updated.card_ts, updated)` with the **stored** columns. Never a channel or timestamp taken from an interaction payload, and never the short-lived response webhook: a permanent state transition belongs to `chat.update` on the original message (Pitfall 5). When either stored column is missing, skip the card update, log `[approve] no stored card; skipping update proposal=<id>` and still return `confirmed`.

   i. Return `confirmed`. Add no try/catch wrapper: `createCalendarEvent` already owns its 409 fallback and throws explicitly on a missing organizer, and Bolt's own error handler logs anything that escapes.

**2. Wire the listener in `lib/slack/handlers/approve-proposal.ts` (D-05).** Keep `await ack()` as the literal first awaited statement, keep Phase 2's narrowing of the payload to a block-button action, then read the proposal id from `actions[0].value` and the clicker from `body.user.id`, call `approveProposal`, and switch on the returned outcome to deliver ephemeral feedback through the middleware's `respond` helper with an ephemeral response type and without replacing the original message:
   - `confirmed`: no ephemeral message — the card itself is the feedback.
   - `not_organizer`: tell the clicker that only the connected calendar owner can approve, and that nothing was written to Google Calendar.
   - `already_scheduled`: tell the clicker that another user already claimed this proposal.
   - `not_pending`: tell the clicker it was already handled and the card has been refreshed.
   - `not_found`: tell the clicker that proposal no longer exists.
   Delete whatever placeholder body Phase 1/2 left; the listener now holds no database call, no status write and no Calendar call of its own. Do not touch `lib/slack/bolt.ts` — the registration Phase 2 wrote already points here.

**3. Extend `buildConfirmedBlocks` in `lib/slack/blocks.ts` (APR-04, D-10, D-20).** Add the calendar link and the Meet link to the confirmed chip, reading `calendar_html_link` and `meet_link` off the proposal it already receives. Both may be an empty string (Phase 3 returns `""` when conference creation has not settled, and the column may be empty on an older row): render a link element only when its value is non-empty, and never emit an element with an empty URL — Slack rejects that and the whole update fails. Reuse Phase 2's existing escaping helper for any text you interpolate. Change nothing else in the file: not `buildApprovalBlocks`, not the action ids, not the button values.

**4. Format and check.** Run `bunx --bun @biomejs/biome check --write lib/slack`, then the verify commands.

**5. Fire the one path by hand.** Post a real top-level message in the watched channel so Phase 1's stub `runAgent` creates a Proposal and posts its card (the hand-seeded Thu 17 Sep row from `prisma/seed.ts` has no `card_channel`/`card_ts`, so it has no card to click — the stub-produced card is the clickable target). Then hand off to the human check below.

Two expected, non-failure observations: if Phase 3's smoke script already inserted an event for the proposal you target, the log shows the 409 fallback line rather than a fresh insert, and that is success by design; and if the conference link is still settling, the Meet element may be absent on the first render while the calendar link is present.
  </action>
  <verify>
    <automated>bunx tsc --noEmit</automated>
    <fails_when>any `error TS` line whose file path starts with `lib/slack/`, in particular one saying `createCalendarEvent` expects a different argument count or that `team_id_slack_user_id` is not a known unique input. Errors in other paths inherited from the merge are recorded in the SUMMARY, not fixed here</fails_when>
    <automated>bunx biome check lib/slack</automated>
    <fails_when>non-zero exit, or the summary line reports one or more errors</fails_when>
    <automated>grep -q 'UPDATE "Proposal" SET organizer_user_id' lib/slack/approve.ts && grep -q 'organizer_user_id IS NULL' lib/slack/approve.ts && grep -q 'RETURNING id' lib/slack/approve.ts && grep -q '\$queryRaw' lib/slack/approve.ts</automated>
    <fails_when>non-zero exit — the locked conditional claim is missing, or its SET/WHERE/RETURNING text drifted from the string CONTEXT.md D-07 fixes</fails_when>
    <automated>test "$(grep -vE '^[[:space:]]*(\*|//|/\*)' lib/slack/approve.ts | grep -cE '\$queryRawUnsafe|EXISTS[[:space:]]*\(|status:[[:space:]]*"already_scheduled"|@slack/bolt|slack/bolt|body\.|process\.env')" = 0</automated>
    <fails_when>non-zero exit, meaning a non-comment line of the approve module does one of: builds the claim with the unsafe raw variant, folds a subquery into the claim, writes a terminal status on the lost-claim branch, depends on the Slack framework or the Bolt entry module, reads an interaction payload, or reads the environment directly instead of through the config module</fails_when>
    <automated>test "$(grep -c 'updateProposalCard(proposal.card_channel' lib/slack/approve.ts)" -ge 1 && test "$(grep -vE '^[[:space:]]*(\*|//|/\*)' lib/slack/handlers/approve-proposal.ts | grep -cE 'prisma\.|createCalendarEvent\(')" = 0</automated>
    <fails_when>non-zero exit — the card update does not use the stored channel column, or the listener still carries a database or Calendar call instead of delegating to the approve module</fails_when>
    <human-check>
      <test>With exactly one Bolt process running and signed in to Slack as user A: (1) post a top-level message in the watched channel and wait for the stub card; (2) click Approve once; (3) open the calendar link that appears on the confirmed card</test>
      <expected>(2) The same Slack message updates in place to a confirmed chip — no new message is posted — and the chip carries a Google Calendar link and, once conference creation settles, a `meet.google.com` link. (3) The event opens on A's calendar at the proposal's HKT time with the participant listed as a guest. If the Meet link is missing, reload the card's event once after a few seconds before calling it a failure. If the Bolt log shows the 409 fallback line instead of a fresh insert, that is the deterministic-id path doing its job, not an error.</expected>
      <why_human>Only a real Slack user can produce the interaction payload, and an insert returns HTTP 200 even when the Meet link was silently dropped — the event has to be opened (ROADMAP Phase 4 success criterion 1)</why_human>
    </human-check>
  </verify>
  <!-- planner-discipline-allow: @slack/bolt -->
  <!-- planner-discipline-allow: slack/bolt -->
  <!-- planner-discipline-allow: body. -->
  <!-- planner-discipline-allow: process.env -->
  <!-- planner-discipline-allow: already_scheduled -->
  <!-- planner-discipline-allow: prisma. -->
  <!-- planner-discipline-allow: createCalendarEvent( -->
  <!-- planner-discipline-allow: EXISTS -->
  <!-- planner-discipline-allow: $queryRawUnsafe -->
  <acceptance_criteria>
    - `lib/slack/approve.ts` exports `approveProposal` and `ApproveOutcome`, and `approveProposal` declares exactly two parameters, the second being the clicker's Slack user id
    - `lib/slack/approve.ts` contains `UPDATE "Proposal" SET organizer_user_id`, `organizer_user_id IS NULL`, `RETURNING id` and `$queryRaw` (third automated command exits 0)
    - In `lib/slack/approve.ts` the `google_refresh_token` check appears on an earlier line than the first `$queryRaw` occurrence, and the first `createCalendarEvent(` occurrence is later than both
    - `lib/slack/approve.ts` contains `team_id_slack_user_id` and `createCalendarEvent(proposal, clicker.id)`
    - The fourth automated command exits 0: no non-comment line of `lib/slack/approve.ts` matches the unsafe raw variant, a subquery in the claim, a terminal-status write, a Slack-framework dependency, an interaction-payload read, or a direct environment read
    - `lib/slack/handlers/approve-proposal.ts` contains `approveProposal(` and `respond(`, keeps `await ack()` as its first awaited statement, and contains no `prisma.` or `createCalendarEvent(` call (fifth automated command exits 0)
    - `lib/slack/blocks.ts` contains `calendar_html_link` and `meet_link`, and `lib/slack/bolt.ts` is byte-identical to its state after Task 1
    - `bunx tsc --noEmit` reports no `error TS` under `lib/slack/`, and `bunx biome check lib/slack` exits 0
    - Slack: one Approve click updates the same message to a confirmed chip carrying the calendar link, and the event opens on A's calendar
  </acceptance_criteria>
  <done>A real Approve click by the token-holding user turns a live card into a real Google Calendar event and a confirmed chip on the same Slack message showing both links, with the organizer claimed by the locked conditional UPDATE.</done>
</task>

<task type="auto">
  <name>Task 3: Expand from the proven slice — non-organizer and already-claimed feedback, double-click and redelivery idempotence, and the real Reject path</name>
  <files>lib/slack/approve.ts, lib/slack/handlers/reject-proposal.ts</files>
  <read_first>
    - lib/slack/approve.ts (from Task 2 — the branch structure the reject helper sits beside)
    - lib/slack/handlers/reject-proposal.ts (Phase 2's real body: ack-first shape, narrowing, and what it currently writes)
    - lib/slack/handlers/approve-proposal.ts (the ephemeral feedback shape to mirror)
    - lib/slack/blocks.ts (the dismissed builder Phase 2 shipped, and any already-scheduled builder)
    - .planning/phases/04-approval-bridge/04-CONTEXT.md D-11 and the "Claude's Discretion" list
    - .planning/phases/04-approval-bridge/04-RESEARCH.md §"Pitfall 5" and §"Pitfall 6"
  </read_first>
  <action>
The tracer proved the happy path. This task builds out the branches that share it, changing no part of the proven sequence.

**1. Reject (D-11, SLK-05/SLK-06 contract).** In the same `lib/slack/approve.ts`, export a `RejectOutcome` union with the members `dismissed`, `not_found` and `not_pending`, and an async `rejectProposal(proposalId: string): Promise<RejectOutcome>`. It needs no clicker identity: dismissing costs nobody a calendar write, so the token guard does not apply here. Body: a single `prisma.proposal.updateMany` scoped to the id with a status filter of pending, setting status to dismissed; then one `findUnique` for the current row. A null row returns `not_found`. Otherwise call `updateProposalCard` with the row's stored channel and timestamp and return `dismissed` when the update count was one, or `not_pending` when it was zero — in the second case the card is simply re-rendered in whatever state it already holds. No Calendar call appears on this path at all, and no other module is imported for it.

**2. Point the reject listener at it.** In `lib/slack/handlers/reject-proposal.ts`, keep `await ack()` first and Phase 2's narrowing, read the proposal id from the button value, call `rejectProposal`, and mirror the approve listener's ephemeral feedback: nothing on `dismissed`, a short note on `not_pending` and on `not_found`. Remove any status write the placeholder body performed directly. Do not touch `lib/slack/bolt.ts`.

**3. Confirm the branch behaviours the tracer did not exercise.** Re-read the approve function and make each of these true by inspection rather than by adding new machinery — every one of them is already a branch written in Task 2, and none of them earns a new abstraction:
   - a click by a user whose row carries no Google refresh token returns before the claim, leaving both the row and Google untouched;
   - a second click on a confirmed row re-renders the card and returns without reaching the claim;
   - a lost claim owned by a different user returns with no write of any kind;
   - a lost claim owned by the clicker continues into the idempotent create;
   - an unknown proposal id returns the miss outcome without throwing;
   - a row with no stored card channel or timestamp logs the skip and still completes.
   If any of these is not already true, fix it inside the existing branch structure. Add no new table, no lock and no in-memory set: the conditional claim, the pending-status check and Phase 3's deterministic event id are the entire concurrency story (APR-03).

**4. Format and check.** Run `bunx --bun @biomejs/biome check --write lib/slack`, then the verify commands, then hand off to the human check.

**Cut order (ROADMAP).** If the phase is overrunning, nothing in this task is cut: the organizer claim and the duplicate-event guard are named as never-cut, and Reject is a table-stakes button a judge probes first. The `inline` transport double-check in plan 04-02 is the cut, not this.
  </action>
  <verify>
    <automated>bunx tsc --noEmit</automated>
    <fails_when>any `error TS` line whose file path starts with `lib/slack/`</fails_when>
    <automated>bunx biome check lib/slack</automated>
    <fails_when>non-zero exit, or the summary line reports one or more errors</fails_when>
    <automated>grep -q 'rejectProposal' lib/slack/approve.ts && grep -q 'updateMany' lib/slack/approve.ts && test "$(grep -vE '^[[:space:]]*(\*|//|/\*)' lib/slack/handlers/reject-proposal.ts | grep -cE 'prisma\.|createCalendarEvent\(')" = 0</automated>
    <fails_when>non-zero exit — the reject helper is missing or does not use the conditional bulk update, or the reject listener still writes to the database itself</fails_when>
    <automated>test "$(grep -c 'createCalendarEvent(' lib/slack/approve.ts)" = 1</automated>
    <fails_when>the count is 0 (the approve path lost its Calendar call) or 2 or more (a second call site can create a second event — the reject path must never reach Google)</fails_when>
    <human-check>
      <test>With one Bolt running: (1) click Approve a second time on the card confirmed in Task 2; (2) as user B (who holds no Google token), post a new watched-channel message and click Approve on the card it produces; (3) check A's calendar for the day of the card in step 2; (4) post another watched-channel message and click Reject on its card; (5) click Reject again on that same card</test>
      <expected>(1) Nothing changes on the card and no second event appears on A's calendar; an ephemeral note says it was already handled. (2) B sees an ephemeral note that only the connected calendar owner can approve; the card still shows Approve and Reject, unchanged. (3) No event was created for that proposal — this is success criterion 4. (4) The card updates in place to a dismissed chip with no calendar activity. (5) Nothing changes beyond the same chip re-rendering.</expected>
      <why_human>Success criteria 2 and 4 are judged on the live card and on A's real calendar, and only a second real Slack user can produce B's click</why_human>
    </human-check>
  </verify>
  <!-- planner-discipline-allow: prisma. -->
  <!-- planner-discipline-allow: createCalendarEvent( -->
  <acceptance_criteria>
    - `lib/slack/approve.ts` exports `rejectProposal` and `RejectOutcome`, and `rejectProposal` declares exactly one parameter
    - `lib/slack/approve.ts` contains exactly one `createCalendarEvent(` call site (fourth automated command exits 0)
    - `lib/slack/handlers/reject-proposal.ts` contains `rejectProposal(`, keeps `await ack()` as its first awaited statement, and contains no `prisma.` or `createCalendarEvent(` call
    - `lib/slack/approve.ts` still passes every Task 2 gate unchanged: the locked claim text, and zero non-comment matches for the unsafe raw variant, a claim subquery, a terminal-status write, a Slack-framework dependency, an interaction-payload read or a direct environment read
    - `bunx tsc --noEmit` reports no `error TS` under `lib/slack/`, and `bunx biome check lib/slack` exits 0
    - Slack: a second Approve click changes nothing and creates no second event; B's Approve click creates no event at all; Reject dismisses the card in place and a second Reject changes nothing
  </acceptance_criteria>
  <done>Every click path the demo can produce is handled: the token holder approves, B is refused before any Calendar call, a repeat click is a no-op, and Reject dismisses the card in place without touching Google.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slack workspace to Bolt (Socket Mode) | The interaction payload — proposal id in the button value, clicking user id, team id — is untrusted input from any channel member |
| Bolt to Postgres | The approve module reads the Proposal row, the clicking User row and its Google refresh token, and writes the organizer claim and the confirmed status |
| Approve module to Google Calendar | The only outbound write in this plan; it creates a real event and sends real invitation emails |
| Two Wave A branches to `main` | Merged third-party dependency state (`package.json` + regenerated lockfile) crosses into the tree everything else runs from |
| Process to terminal log | Log lines persist in scrollback and `/tmp` and may be screen-shared at the demo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | `approveProposal` organizer resolution | high | mitigate | The clicker's `google_refresh_token` is read and checked before the claim is attempted; the `not_organizer` branch returns before any Google call and before any write. Gated by the ordering acceptance criterion (token check line precedes the first `$queryRaw`, which precedes the first `createCalendarEvent(`) |
| T-04-02 | Tampering | duplicate calendar event from a double click or a Slack redelivery | high | mitigate | The `status !== "pending"` early return short-circuits a repeat click, and Phase 3's deterministic event id plus 409 fallback makes even a racing insert resolve to the same event. Exactly one `createCalendarEvent(` call site is grep-enforced |
| T-04-03 | Tampering | SQL injection through the raw organizer claim | high | mitigate | The claim is a `$queryRaw` tagged template whose interpolations are bound parameters; the unsafe raw variant and string concatenation are grep-forbidden in the module |
| T-04-04 | Denial of Service | a lost claim writing a terminal status onto a still-pending row, locking out the real organizer | medium | mitigate | The lost-claim branch performs no Prisma write; grep-forbidden in the module. The clicker is told ephemerally instead |
| T-04-05 | Spoofing | a hand-crafted or stale button value naming another team's proposal | medium | mitigate | The id is resolved with `findUnique` (miss returns an outcome, never a throw), and the clicking user is looked up by the compound team-scoped key taken from the row, so a clicker outside the proposal's team never resolves to a token-holding user |
| T-04-06 | Information Disclosure | ephemeral feedback and log lines leaking row contents | medium | mitigate | Ephemeral texts state an outcome and name no token, email or link; log lines carry ids only |
| T-04-07 | Information Disclosure | `google_refresh_token` read in plaintext from Postgres | medium | accept | Inherited Phase 1 debt for a one-laptop demo; this plan only reads the column and never logs or returns it |
| T-04-08 | Repudiation | no record of who approved a proposal | low | accept | The claimed `organizer_user_id` on the row is the record; a full audit log is out of scope for the demo |
| T-04-SC | Tampering | supply chain via the merged `package.json` and the regenerated lockfile | medium | mitigate | No package is installed in this phase: Phase 1 installed and audited every core-window dependency at exact pins (04-RESEARCH.md §Package Legitimacy Audit, no `[SLOP]`, no new `[SUS]`). The merge resolves `package.json` to the union of two already-audited sides with pins unchanged, and Task 1 stops and reports any package neither Wave A SUMMARY mentions rather than installing it. No blocking legitimacy checkpoint is required because no install occurs |
</threat_model>

## Edge Coverage (spec-less probe fallback, §C)

| Probe edge | Status | Disposition |
|---|---|---|
| Double click on Approve | resolved (explicit) | truth: second click re-renders and returns `not_pending`; one event |
| Slack redelivery of the same click | resolved (explicit) | truth: identical to a double click via the pending check plus the 409 fallback |
| Non-organizer / no-token click | resolved (explicit) | truth + prohibition: returns before the claim, no Calendar call, no write |
| Already claimed by another user | resolved (explicit) | truth + prohibition: `already_scheduled`, no status write |
| Empty Meet or calendar link | resolved (explicit) | truth: link element rendered only when non-empty; card does not throw |
| Unknown proposal id | resolved (explicit) | truth: `not_found` through `findUnique`, no throw |
| Reject on a non-pending row | resolved (explicit) | truth: no status change, card re-rendered |
| Concurrent Approve clicks | resolved (backstop) | truth: Postgres serialises the claim; loser writes nothing |
| Interruption between Google commit and Prisma persist | resolved (backstop) | truth: same-user lost-claim branch re-enters the 409 fallback |
| Missing stored card channel or timestamp | resolved (explicit) | truth: skip logged, approve still completes |
| Trigger transport unavailable | deferred | owned by plan 04-02 (the `inline` branch) |

No-silent-drop equality: 11 surfaced = 10 authored into `must_haves.truths` + 1 deferred to 04-02 with its owner named.

Prohibition recall: seven prohibitions authored descriptor-less in `must_haves.prohibitions`. Canon referral: leaking a secret into logs is canon (OWASP logging) and is covered by T-04-06 and `/gsd-secure-phase`, so no separate prohibition is minted for it.

## Flagged assumptions

- **FA-1:** Phase 1's stub `runAgent` populates `team_id` and a Participant on the Proposals it creates and stores `card_channel`/`card_ts`. Task 2 reads `lib/agent/graph.ts` to confirm; if `team_id` is absent the fallback is the configured team id, recorded in the SUMMARY. `lib/agent/**` is never edited (D-19).
- **FA-2:** The hand-seeded Thu 17 Sep 2026 Proposal has no card, so the clickable target is a card the stub posts from a real watched-channel message. If Phase 3's smoke script already created that proposal's event, the 409 fallback line is expected output, not a failure.
- **FA-3:** Phase 2's `buildConfirmedBlocks` takes the Proposal row (or a shape carrying its columns) and can reach `calendar_html_link`/`meet_link` without a signature change. If it does not, the extension is additive on whatever parameter it does take; `updateProposalCard`'s three-argument signature stays fixed either way.
- **FA-4:** Bolt 5.1.0's action middleware provides a `respond` helper on a `block_actions` payload. If Phase 2's handler shape exposes it differently, use the equivalent ephemeral post — the choice of ephemeral surface is explicitly Claude's Discretion in 04-CONTEXT.md.
- **FA-5:** `prisma.$queryRaw` on Prisma 7's engine-free client with the pg driver adapter parameterises tagged-template interpolations exactly as on Prisma 6. Detection: a claim that silently matches nothing, or a driver error naming a bind parameter, on the first Approve click.

## Artifacts this phase produces

| Artifact | Symbol(s) / value | Plan |
|---|---|---|
| `lib/slack/approve.ts` | new file — Bolt-free approve/reject domain module | 04-01 |
| `approveProposal(proposalId: string, clickerSlackUserId: string): Promise<ApproveOutcome>` | new export; Phase 7 must pass `body.user.id` as the second argument | 04-01 T2 |
| `ApproveOutcome` | new exported union: `confirmed`, `not_found`, `not_pending`, `not_organizer`, `already_scheduled` | 04-01 T2 |
| `rejectProposal(proposalId: string): Promise<RejectOutcome>` | new export | 04-01 T3 |
| `RejectOutcome` | new exported union: `dismissed`, `not_found`, `not_pending` | 04-01 T3 |
| `lib/slack/handlers/approve-proposal.ts` | `handleApproveProposal` — real body delegating to `approveProposal` | 04-01 T2 |
| `lib/slack/handlers/reject-proposal.ts` | `handleRejectProposal` — real body delegating to `rejectProposal` | 04-01 T3 |
| `lib/slack/blocks.ts` | `buildConfirmedBlocks` extended with `calendar_html_link` and `meet_link` | 04-01 T2 |
| Log tags `[approve] no stored card; skipping update proposal=` | log contract | 04-01 T2 |
| `trigger.config.ts` | new root config file (`dirs: ["./lib/agent/tasks"]`, `prismaExtension({ mode: "modern" })`, Node runtime) | 04-02 T1 |
| `lib/agent/tasks/run-agent.ts` | new file — `runAgentTask`, task id `run-agent` | 04-02 T1 |
| `lib/agent/dispatch.ts` | `dispatchAgentRun` gains the `trigger` branch (signature unchanged) | 04-02 T2 |
| Config keys used | `AGENT_TRANSPORT` (`trigger`\|`inline`, already declared), the Trigger.dev secret key via the config module, and the Trigger.dev project ref read by `trigger.config.ts` | 04-02 |
| `.planning/phases/04-approval-bridge/COVERAGE.md` | API coverage matrix (Slack Web API, Google Calendar via Phase 3, Trigger.dev SDK) | planning |

## Source Coverage Audit (phase 04 plan set)

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Approving a proposal from Slack writes a real Calendar event and updates the card | 04-01 T2/T3 | COVERED |
| GOAL | — | Working local/Trigger.dev transport switch | 04-02 | COVERED |
| REQ | APR-01 | Approve is ordinary backend code — no graph resume, no model call | 04-01 T2 | COVERED |
| REQ | APR-02 | Conditional organizer claim; no row back is a UI state, not an error | 04-01 T2 | COVERED |
| REQ | APR-03 | Double click / redelivery creates exactly one event | 04-01 T2/T3 | COVERED |
| REQ | APR-04 | Confirmed card shows the calendar link and the Meet link | 04-01 T2 | COVERED |
| REQ | APR-05 | `AGENT_TRANSPORT` switches with env + restart only | 04-02 T2/T3 | COVERED |
| REQ | AGT-11 | Trigger.dev task wraps `runAgent`, idempotency key from team+channel+ts, plain WebClient | 04-02 T1/T2 | COVERED |
| RESEARCH | — | Token guard as a pre-read, claim SQL untouched (Pitfall 1) | 04-01 T2 | COVERED |
| RESEARCH | — | `chat.update` for state transitions, ephemeral only for the non-organizer notice (Pitfall 5) | 04-01 T2/T3 | COVERED |
| RESEARCH | — | Idempotent handler covers both redelivery and double click (Pitfall 6) | 04-01 T2/T3 | COVERED |
| RESEARCH | — | Compound `team_id_slack_user_id` key for the clicker lookup | 04-01 T2 | COVERED |
| RESEARCH | — | `prismaExtension({ mode: "modern" })` with no other options; generate is manual (Pitfall 2) | 04-02 T1 | COVERED |
| RESEARCH | — | CLI started from the repo root so `.env` loads (Pitfall 3) | 04-02 T1 | COVERED |
| RESEARCH | — | Import-graph hazard: no Bolt anywhere in the task's transitive graph | 04-02 T1/T2 | COVERED |
| RESEARCH | — | Deleted-event id retention | — | Excluded: explicitly flagged forward to Phase 10, not actionable here |
| CONTEXT | D-01, D-02, D-03 | Merge window, lockfile discipline, single Bolt | 04-01 T1 | COVERED |
| CONTEXT | D-04..D-11 | Approve sequence, claim, token guard, dup guard, links, reject | 04-01 T2/T3 | COVERED |
| CONTEXT | D-12..D-17 | Task wrapper, idempotency key, prismaExtension, transport flag, CLI, both-ways check | 04-02 | COVERED |
| CONTEXT | D-18, D-19, D-20, D-21, D-22 | Ownership, no-touch list, named overlaps, processes, repo rules | 04-01, 04-02 | COVERED |

## Requirement note

Reject carries no APR id of its own: it is locked by **D-11** (CONTEXT.md) and rides on SLK-05/SLK-06, which Phase 2 owns. It is built here because Phase 2 left the listener body a placeholder (D-20, sequential ownership).

<verification>
- Task 1: both Wave A branch tips are ancestors of `HEAD`, no conflict markers, `bun.lock` present and `package-lock.json` absent, the schema-push decision recorded, `/ponytail-review` findings recorded, one Bolt process
- Tasks 2 and 3: `bunx tsc --noEmit` reports no `error TS` under `lib/slack/`, `bunx biome check lib/slack` exits 0, and every grep gate exits 0
- Human (Task 2): one Approve click confirms the card in place with both links, and the event opens on A's calendar with the participant invited
- Human (Task 3): a second Approve click changes nothing and creates no second event; B's click creates nothing; Reject dismisses in place
- Only `lib/slack/approve.ts`, `lib/slack/handlers/approve-proposal.ts`, `lib/slack/handlers/reject-proposal.ts`, `lib/slack/blocks.ts` and (in the merge) `package.json`/`bun.lock` changed; `lib/agent/graph.ts`, `app/**`, `components/**`, `lib/calendar/**`, `lib/slack/bolt.ts` and `prisma/schema.prisma` are untouched by Tasks 2 and 3
</verification>

<success_criteria>
ROADMAP exit criterion (this plan's half), binary: a proposal with a live card → Approve → a real event with a Meet link and the participant invited → the same Slack card confirmed in place showing both links.
- Success criterion 1: approving produces exactly one calendar event with a Meet link and an invite, confirmed by opening the event
- Success criterion 2: the card updates to a confirmed chip with both links, and a second Approve click changes nothing further
- Success criterion 4: a click by a user who is not the token-holding organizer attempts no Calendar write
- Phase 7's `choose_alt` handler can call `approveProposal(proposalId, clickerSlackUserId)` without refactoring it
</success_criteria>

<output>
Create `.planning/phases/04-approval-bridge/04-01-SUMMARY.md` when done. Record: the pre-merge `main` sha, whether `bunx prisma db push` ran and why, the conflicted files and their resolutions, the `/ponytail-review` findings, the winning Bolt runtime, the FA-1 finding about `team_id` on stub-created Proposals, both human-check outcomes, and any `tsc` error inherited from the merge.
</output>
