---
phase: 02-slack-surface
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - lib/slack/blocks.ts
  - lib/slack/post-proposal-card.ts
  - lib/slack/update-proposal-card.ts
  - lib/slack/handlers/reject-proposal.ts
  - lib/slack/handlers/approve-proposal.ts
  - lib/slack/resolve-email.ts
  - lib/slack/bolt.ts
autonomous: false
requirements:
  - SLK-04
  - SLK-05
  - SLK-06
  - SLK-08
user_setup:
  - service: slack
    why: "users.info returns 200 without the users:read scope and simply omits profile.email, so a missing scope looks like a user with no email rather than an error (Research Pitfall 5, Orchestrator Note 7)"
    dashboard_config:
      - task: "Confirm BOTH users:read and users:read.email are on the installed (reinstalled) app, not just in the manifest"
        location: "api.slack.com/apps -> this app -> OAuth & Permissions -> Bot Token Scopes"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "[SLK-05, D-08, D-09, success criterion 2] The posted card shows the proposal title, the start time formatted in HKT, the duration in minutes, the participants and the confidence, and carries exactly two buttons: Approve with action_id `approve_proposal` and Reject with action_id `reject_proposal`, each with `value` set to the proposal id"
    - "[SLK-06, D-11, success criterion 3] Clicking Approve or Reject edits the same message through chat.update addressed by Proposal.card_channel and Proposal.card_ts read from the row; the buttons are gone, a status chip stands in their place, and no new message appears in the channel"
    - "[SLK-06, D-11, Research Pitfall 4] Every chat.update and every card post in lib/slack passes BOTH `text` and `blocks`; passing text without blocks deletes the card's blocks, so neither field is ever omitted"
    - "[SLK-04, D-07, D-12] `await ack()` is the first awaited statement of both click handlers; the proposal id is read from `action.value` immediately after, and neither handler reads the channel or the timestamp out of the click payload"
    - "[D-10] The card's time string comes from Phase 1's utils/time.ts HKT formatter; lib/slack gains no second formatter and no date-library import"
    - "[D-13, SLK-08] resolveParticipantEmail(slackUserId) in lib/slack/resolve-email.ts calls users.info and returns user.profile.email; posting a card logs one `participant email resolved` line for every participant carrying a slack_user_id"
    - "[Phase 1 contract, D-19] postProposalCard(proposal) and updateProposalCard(channel, ts, proposal) keep Phase 1's parameter order and arity; updateProposalCard chooses its blocks from proposal.status (pending -> approval card, confirmed -> confirmed chip, dismissed -> dismissed chip, already_scheduled -> chip) so Phases 4, 5 and 7 call it unchanged"
    - "[D-19] This plan is 02-02 of the two-plan split: the real Block Kit card, the chat.update status chip and users.info email resolution. 02-01 owns the runtime, the watched-channel filter and the secondary triggers; nothing 02-01 built is re-registered or restructured here"
    - "[D-12, D-15] Neither click handler writes to Google Calendar, claims an organizer, or posts a second message; Phase 4 owns the real approve body and both handlers stay thin enough for it to replace them"
    - "[D-14, D-15, D-16] Only files under lib/slack/ change; lib/agent/**, lib/ai/**, lib/calendar/**, app/**, components/** are untouched, types/ and lib/config.ts are not edited at all, prisma/schema.prisma is not edited and no db push runs, and no package is installed"
    - "[SLK-06 empty] updateProposalCard called with a null or empty channel or ts logs `card coordinates missing proposal_id=<id>` and returns before any Slack call, so the click handler neither throws nor posts anything; a Proposal with zero Participant rows still posts a card whose participants field renders as a single em dash; a Proposal with an empty title renders the header as `(untitled proposal)` rather than sending an empty plain_text block, which Slack rejects"
    - "[SLK-06 encoding] The title renders inside a plain_text header, which Slack does not parse as mrkdwn, truncated to 150 characters; every proposal-derived string rendered into an mrkdwn field first passes through escapeMrkdwn, which replaces &, < and > with their HTML entities, so a title containing `<!channel>`, a user reference or pipe-delimited link markup cannot broadcast, mention or spoof a link; each mrkdwn field is truncated to 2000 characters. The live injection path only becomes reachable in Phase 5, when the title stops being hardcoded"
    - "[SLK-06 idempotency] Both click handlers transition the row with a conditional update whose where clause requires status `pending`; when the affected count is 0 the handler logs `already decided proposal_id=<id> status=<status>` and makes no chat.update call, so a second Approve click from a stale render, or Reject after Approve, leaves the first chip in place"
    - "[SLK-08 empty] A participant with no slack_user_id is skipped without a users.info call; a users.info response carrying no profile.email (bot users, and humans who never set one) logs `participant email resolved slack_user_id=<id> email=none`; a users.info failure logs `email resolution failed slack_user_id=<id> code=<slack error code>` and the card still posts with mention labels, so a missing scope is visible in the log instead of silently degrading the whole demo"
    - statement: "[SLK-06 concurrency] Two users clicking the two different buttons within the same second race on that one conditional status update; Postgres lets exactly one of them match status `pending`, so exactly one chat.update runs, the chip shows exactly one terminal status, and the loser is a logged no-op"
      verification: backstop
  artifacts:
    - path: "lib/slack/blocks.ts"
      provides: "SLK-05 pure Block Kit builders plus the mrkdwn escaper; no DB call, no Slack call, no email anywhere in it"
      exports: ["escapeMrkdwn", "buildApprovalBlocks", "buildConfirmedBlocks", "buildDismissedBlocks", "buildAlreadyScheduledBlocks"]
      contains: "reject_proposal"
    - path: "lib/slack/post-proposal-card.ts"
      provides: "SLK-05 real card post (signature frozen) plus the additive participant-label loader shared with the update path"
      exports: ["postProposalCard", "loadParticipantLabels"]
    - path: "lib/slack/update-proposal-card.ts"
      provides: "SLK-06 status-driven in-place update; null-coordinate guard; both text and blocks on every call"
      exports: ["updateProposalCard"]
      contains: "card coordinates missing"
    - path: "lib/slack/handlers/reject-proposal.ts"
      provides: "SLK-04/SLK-06 ack-first Reject handler with the conditional pending-only status transition"
      exports: ["handleRejectProposal"]
    - path: "lib/slack/resolve-email.ts"
      provides: "SLK-08 users.info wrapper with an in-process cache, additive export for Phase 4's invite"
      exports: ["resolveParticipantEmail"]
      contains: "users.info"
    - path: "lib/slack/bolt.ts"
      provides: "One added registration line, still registration-only"
      contains: 'app.action("reject_proposal", handleRejectProposal)'
  key_links:
    - from: "lib/slack/bolt.ts"
      to: "lib/slack/handlers/reject-proposal.ts"
      via: "the only reject_proposal registration, added beside 02-01's approve registration"
      pattern: 'app\.action\("reject_proposal", handleRejectProposal\)'
    - from: "lib/slack/handlers/reject-proposal.ts"
      to: "lib/slack/update-proposal-card.ts"
      via: "the frozen three-argument call using the stored card coordinates"
      pattern: 'updateProposalCard\('
    - from: "lib/slack/update-proposal-card.ts"
      to: "lib/slack/blocks.ts"
      via: "status switch picking the chip builder"
      pattern: 'buildDismissedBlocks\('
    - from: "lib/slack/post-proposal-card.ts"
      to: "lib/slack/blocks.ts"
      via: "approval card construction"
      pattern: 'buildApprovalBlocks\('
    - from: "lib/slack/blocks.ts"
      to: "utils/time.ts"
      via: "the single HKT formatter, reused not forked (D-10)"
      pattern: '@/utils/time'
    - from: "lib/slack/post-proposal-card.ts"
      to: "lib/slack/resolve-email.ts"
      via: "SLK-08 email resolution during participant-label loading"
      pattern: 'resolveParticipantEmail\('
    - from: "lib/slack/post-proposal-card.ts"
      to: "lib/db.ts"
      via: "participant rows for the card, read by proposal id"
      pattern: 'prisma\.participant\.findMany\('
  prohibitions:
    - requirement_id: SLK-06
      category: safety
      status: resolved
      verification: test
      statement: "MUST NOT post a new message as a fallback when the in-place update cannot be performed: the update path never calls the post-message Web API method, and a missing or null stored card coordinate MUST NOT be substituted with the channel or timestamp carried by the click payload"
    - requirement_id: SLK-05
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT render proposal-derived text in a way that can notify or mislead the channel: no proposal-derived string may reach an mrkdwn field unescaped, so a title cannot broadcast to the channel, mention a user who is not a participant, or present a spoofed link as if it were part of the card's own copy"
    - requirement_id: SLK-08
      category: privacy
      status: resolved
      verification: test
      statement: "MUST NOT place a resolved email address into card text or any other Slack-visible surface; resolved addresses live only in the process log line and the Participant row, and participants are shown to the channel as Slack mentions"
---

**Phase goal (ROADMAP, verbatim):** Real watched-channel Slack listener posts and updates a real Block Kit approval card.

**User story:** not framed. The ROADMAP Goal line is not in "As a / I want to / so that" form, and MVP mode forbids inventing one. Already surfaced to the orchestrator by 02-01; run `/gsd mvp-phase 2` to author a story.

<objective>
Replace Phase 1's hardcoded one-button card with the real approval surface. A proposal posts as a Block Kit card showing title, HKT time, duration, participants and confidence, with Approve and Reject. Clicking either edits that same message into a status chip using the channel and timestamp stored on the row — no new message, no second card. A participant's email is resolved from their Slack profile, proving both `users.info` scopes are on the installed token before Phase 4's calendar invite depends on them.

Everything still runs against Phase 1's hardcoded `runAgent` stub. No LLM call, no Calendar call, no organizer claim.

Decisions implemented:
- **Ack:** D-07 (ack is the first statement of both click handlers).
- **Card:** D-08 (title, HKT time, duration, participants, confidence, Approve + Reject), D-09 (action ids and button `value`, inherited from Phase 1 D-03), D-10 (the one HKT formatter).
- **Update:** D-11 (chat.update on the stored `card_channel`/`card_ts`, buttons replaced by a chip) and D-12 (proposal id from `action.value`, no approve body yet).
- **Email:** D-13 (`users.info`, never cut).
- **Ownership and shape:** D-14/D-15/D-16 (lib/slack only), D-17 (bolt.ts gains one registration line and nothing else), D-19 (this is plan 2 of 2).

Purpose: SLK-05 and SLK-06 are the approval gate itself — the product's promise is that nothing reaches the calendar without a click, and that the click is visibly recorded in place.
Output: a real `lib/slack/blocks.ts`, real bodies behind Phase 1's frozen `postProposalCard`/`updateProposalCard` signatures, `lib/slack/handlers/reject-proposal.ts`, `lib/slack/resolve-email.ts`, and one new registration line in `lib/slack/bolt.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-slack-surface/02-CONTEXT.md
@.planning/phases/02-slack-surface/02-RESEARCH.md
@.planning/phases/02-slack-surface/02-PATTERNS.md
@.planning/phases/02-slack-surface/02-01-PLAN.md
@lib/slack/blocks.ts
@lib/slack/post-proposal-card.ts
@lib/slack/update-proposal-card.ts
@lib/slack/handlers/approve-proposal.ts
@lib/slack/client.ts
@lib/db.ts
@utils/time.ts
@prisma/schema.prisma

## Interfaces consumed (Phase 1 contracts; confirm against the real files, change nothing)

- `postProposalCard(proposal): Promise<{ channel: string; ts: string }>` — signature FROZEN. Phases 5, 7 and 8 call it unchanged; this phase only fills in the body.
- `updateProposalCard(channel, ts, proposal): Promise<void>` — signature FROZEN, three arguments in that order. Phases 4, 5 and 7 call `updateProposalCard(proposal.card_channel, proposal.card_ts, proposal)` after setting `status`, so the function picks its blocks from `proposal.status` internally.
- `buildApprovalBlocks(p)` and `buildConfirmedBlocks(p)` exist as Phase 1 stubs — names frozen, bodies replaced. `buildDismissedBlocks`, `buildAlreadyScheduledBlocks` and `escapeMrkdwn` are additive new exports. There is no `buildConflictBlocks`; Phase 7 adds it.
- `slackClient` from `lib/slack/client.ts`, `prisma` from `lib/db.ts`, the HKT formatter from `utils/time.ts` (use whatever export name Phase 1 shipped).
- `handleApproveProposal` in `lib/slack/handlers/approve-proposal.ts`, moved there by 02-01 Task 2 and registered there. It is extended here, never re-registered.
- Prisma field names are snake_case: `card_channel`, `card_ts`, `confidence`, `start`, `end`, `tz`, `source_channel`, `source_ts`. `ProposalStatus` is the enum `pending | confirmed | dismissed | already_scheduled`. `Participant` carries `proposal_id`, `user_id?`, `slack_user_id?`, `email`, `role`, `response`.

## Planning assumptions

- `card_channel` and `card_ts` may be declared nullable in Phase 1's schema. Do not change the schema; instead `updateProposalCard`'s `channel`/`ts` parameters are widened to accept null. **This is the one additive contract change in this plan** — widening an input type keeps every existing three-argument call site compiling, and it puts the guard in the one place all of Phases 4, 5 and 7 already route through.
- `confidence`'s scale is Phase 1's choice. Read the column type in `prisma/schema.prisma` and the value the stub writes in `lib/agent/graph.ts` (read only, never edit) before choosing whether to multiply by 100.
- **Flagged assumption, unresolved:** the spec-less edge probe returned SLK-05 / `unclassified` as "review manually" with no predicate text. Nothing defensible could be derived from it, so per the probe protocol it is surfaced here rather than silently resolved. SLK-05's own acceptance is carried by success criterion 2 and the Task 3 hand check; if the unclassified row meant something else, it surfaces as a Phase 4 or Phase 5 finding.
- Canon referral: generic injection hardening and secret-leakage-into-logs are canon security concerns covered by `/gsd-secure-phase`; only the Slack-specific mrkdwn broadcast/spoof case is minted as a prohibition here.
</context>

<tasks>

<!-- planner-discipline-allow: chat.postMessage -->

<task type="tracer" gate="blocking-human">
  <name>Task 1 (tracer): Real card with Approve + Reject, one click path end to end to a chip in place</name>
  <reversibility rating="costly">The card's block layout, the status-to-blocks switch inside updateProposalCard and the widened channel/ts parameter are what Phases 4, 5 and 7 build against; changing them later means editing three other phases' plans.</reversibility>
  <precondition>02-01 is applied in this worktree: lib/slack/bolt.ts is registration-only, lib/slack/handlers/approve-proposal.ts exists, and exactly one Bolt process is running on the runtime 02-01 recorded in its SUMMARY</precondition>
  <files>lib/slack/blocks.ts, lib/slack/post-proposal-card.ts, lib/slack/update-proposal-card.ts, lib/slack/handlers/reject-proposal.ts, lib/slack/bolt.ts</files>
  <read_first>
    - lib/slack/blocks.ts, lib/slack/post-proposal-card.ts, lib/slack/update-proposal-card.ts (Phase 1's stub bodies and their exact exported signatures)
    - lib/slack/handlers/approve-proposal.ts (the type annotation, the logging style and the statement order to mirror)
    - lib/slack/client.ts (the WebClient singleton) and lib/db.ts (the Prisma singleton)
    - utils/time.ts (the HKT formatter's real export name — D-10, never add a second one)
    - prisma/schema.prisma (Proposal and Participant column names and nullability, the ProposalStatus enum)
    - .planning/phases/02-slack-surface/02-RESEARCH.md: §"Code Examples" for the card and the reject handler, §"Pitfall 4", and §"Orchestrator Review Notes" items 3 and 4 — the Notes OVERRIDE the Code Examples where they disagree, in particular the three-argument updateProposalCard
    - .planning/phases/02-slack-surface/02-PATTERNS.md §"lib/slack/blocks.ts", §"post-proposal-card.ts / update-proposal-card.ts", §"reject-proposal.ts"
    - .planning/phases/01-foundation-hardcoded-round-trip/01-RESEARCH.md §"Interfaces Before Implementation", §"approve_proposal action handler", §"Minimal Block Kit approval card"
  </read_first>
  <action>
**Step 1: scope probe before anything is built on it (Research Orchestrator Note 7).** Make one real `users.info` call against user B's Slack id using the installed bot token, and look at the response body, not the status code. `user.profile.email` must be present. If it is absent or the call returns `missing_scope`, stop and hand off: the installed app is missing `users:read` or `users:read.email`, which is a Slack dashboard fix, not a code fix. Record the outcome in 02-02-SUMMARY.md. A 200 with no email field is the failure mode this probe exists to catch.

**Step 2: real `lib/slack/blocks.ts` (SLK-05, D-08, D-09).** Pure builders only — no Slack call, no database call, and no address-shaped data anywhere in the file.
- `escapeMrkdwn(text: string): string` replaces `&`, `<` and `>` with their HTML entities, in that order, so nothing proposal-derived can become a channel broadcast, a user reference or pipe-delimited link markup once Phase 5 stops hardcoding titles.
- Export a card-input type: Phase 1's Proposal row shape plus a `participants: string[]` array of already-built display labels. Builders never load their own data.
- `buildApprovalBlocks(p)` returns, in order: a `header` block whose `plain_text` is the title truncated to 150 characters, falling back to `(untitled proposal)` when the title is empty (Slack rejects an empty plain_text block); a `section` block with four `mrkdwn` fields labelled When, Duration, Participants and Confidence; and an `actions` block with `block_id` `proposal_actions`.
  - When: the HKT string from `utils/time.ts` applied to `start`.
  - Duration: whole minutes between `start` and `end`.
  - Participants: the labels joined with a comma, or a single em dash when the array is empty.
  - Confidence: a percentage; render an em dash when the column is null.
  - Every one of those four field strings passes through `escapeMrkdwn` and is truncated to 2000 characters.
  - The actions block holds exactly two buttons: Approve, `action_id` `approve_proposal`, `style` `primary`; and Reject, `action_id` `reject_proposal`, `style` `danger`. Both carry `value` set to the proposal id (Phase 1 D-03, restated as D-09).
- `buildConfirmedBlocks(p)`, `buildDismissedBlocks(p)` and `buildAlreadyScheduledBlocks(p)` each return the same header block plus one `context` block holding the status chip, and no actions block — removing the buttons is what stops a second click. Chip copy is your call; keep the three visually distinct.
- Export one helper that maps a status value to the right builder, or write the switch inside `update-proposal-card.ts` — either is fine, but there is exactly one such switch in the repo.
- TSDoc on every exported function. `@/` imports. No `any`.

**Step 3: real `lib/slack/post-proposal-card.ts` (SLK-05).** Keep `postProposalCard(proposal)` exactly as Phase 1 declared it, returning `{ channel, ts }`.
- Add one additive export, `loadParticipantLabels(proposalId: string): Promise<string[]>`. It reads Participant rows for that proposal id through the `prisma` singleton and returns one label per row: a Slack mention built from `slack_user_id` when the row has one, otherwise the literal `unknown participant`. It never returns an address — participants are shown to the channel as mentions (prohibition SLK-08/privacy). Task 2 adds the email resolution inside this function.
- `postProposalCard` calls `loadParticipantLabels`, builds the approval blocks, and posts through the `slackClient` singleton with BOTH a `blocks` array and a `text` fallback string (the title plus a short status word). It returns the channel and `ts` from the API response so the caller can store them on the row; Phase 1's stub already stores them.
- Log one line naming the proposal id and the returned `ts`. Never log the card body.

**Step 4: real `lib/slack/update-proposal-card.ts` (SLK-06, D-11, Research Pitfall 4).** `updateProposalCard(channel, ts, proposal)` keeps three parameters in that order.
- Widen the `channel` and `ts` parameter types to also accept null, so a caller can pass the row's columns straight through.
- First statement: if either is missing or empty, log `card coordinates missing proposal_id=<id>` and return. No Slack call happens, nothing is posted anywhere else, and the caller does not throw. This guard is the reason no other phase needs its own.
- Then switch on `proposal.status`: `pending` rebuilds the full approval card (loading labels through `loadParticipantLabels`, which is why that export exists); `confirmed`, `dismissed` and `already_scheduled` each select their chip builder. An unrecognised status logs and returns rather than guessing.
- The single update call passes BOTH `text` and `blocks`, always. Text alone deletes the card's blocks — that is the documented behaviour, not a theory (Research Pitfall 4).
- This file never posts a new message under any branch. A card that cannot be updated stays as it was.

**Step 5: `lib/slack/handlers/reject-proposal.ts` (SLK-04, SLK-06, D-07, D-11, D-12).** Mirror the approve handler's file layout exactly; Phase 4 replaces both bodies, so keep this thin.
- Export `handleRejectProposal`. Type it `AllMiddlewareArgs & SlackActionMiddlewareArgs<BlockButtonAction>` using Bolt's exported types; if that does not type-check against `app.action` on the first attempt, copy the approve handler's annotation verbatim rather than inventing a third style. No `any`.
- `await ack()` is the literal first statement (D-07). Nothing awaited precedes it, including a try block.
- Next, narrow the payload with the `BlockButtonAction` cast the approve handler already uses and take the proposal id out of the first action's `value`. It is optionally typed: return when it is absent. Never take the channel or the timestamp from the click payload — the row is the only source (D-11).
- Read the Proposal row by id; log and return when it is missing.
- Transition the status with a conditional update whose where clause requires both the id and status `pending`, setting `dismissed`. When the affected count is 0, log `already decided proposal_id=<id> status=<status>` and return without touching Slack — that one clause is what makes a double click and a two-user race harmless.
- Otherwise call `updateProposalCard` with the row's stored card coordinates and the row spread with its new status, so the function sees the status it should render rather than the one it had a moment ago. **This is the trap:** passing the pre-update row re-renders the approval card with its buttons intact.
- One log line naming the proposal id and the new status. TSDoc with summary, @param, @returns, @throws.

**Step 6: register in `lib/slack/bolt.ts` (D-17).** Add exactly one import and exactly one line: the `reject_proposal` action registration, beside 02-01's approve registration. No other edit to this file. Approve is already registered; do not register it twice.

**Step 7: restart and hand off.** Kill the running Bolt process and start exactly one on the runtime 02-01 recorded, teeing output to `/tmp/ai-secretary-bolt.log`. Then hand off to the human check. Leave the process running for Task 2.
  </action>
  <verify>
    <automated>bunx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any `error TS` line in the output</fails_when>
    <automated>bunx biome check lib/slack</automated>
    <fails_when>non-zero exit, or the summary line reports one or more errors</fails_when>
    <automated>rc=0; for f in escapeMrkdwn buildApprovalBlocks buildConfirmedBlocks buildDismissedBlocks buildAlreadyScheduledBlocks; do grep -q "export function $f" lib/slack/blocks.ts || { echo "missing export: $f"; rc=1; }; done; grep -q 'action_id: "approve_proposal"' lib/slack/blocks.ts || { echo "missing approve button"; rc=1; }; grep -q 'action_id: "reject_proposal"' lib/slack/blocks.ts || { echo "missing reject button"; rc=1; }; grep -q '@/utils/time' lib/slack/blocks.ts || { echo "HKT formatter not reused"; rc=1; }; test "$rc" = 0</automated>
    <fails_when>non-zero exit with a `missing export: <name>`, `missing approve button`, `missing reject button` or `HKT formatter not reused` line</fails_when>
    <automated>rc=0; b=$(grep -vE '^\s*(\*|//|/\*)' lib/slack/update-proposal-card.ts); echo "$b" | grep -q 'text:' || { echo "no text field"; rc=1; }; echo "$b" | grep -q 'blocks:' || { echo "no blocks field"; rc=1; }; test "$(echo "$b" | grep -c 'chat\.postMessage')" = 0 || { echo "post fallback in update path"; rc=1; }; grep -q 'card coordinates missing' lib/slack/update-proposal-card.ts || { echo "no coordinate guard"; rc=1; }; test "$rc" = 0</automated>
    <fails_when>non-zero exit with a `no text field`, `no blocks field`, `post fallback in update path` or `no coordinate guard` line</fails_when>
    <automated>rc=0; r=$(grep -vE '^\s*(\*|//|/\*)' lib/slack/handlers/reject-proposal.ts); echo "$r" | grep -m1 'await ' | grep -q 'await ack()' || { echo "ack not first"; rc=1; }; test "$(echo "$r" | grep -cE 'body\.channel|body\.message')" = 0 || { echo "click payload coordinates"; rc=1; }; echo "$r" | grep -q 'updateMany' || { echo "no conditional transition"; rc=1; }; echo "$r" | grep -q 'updateProposalCard(' || { echo "no card update"; rc=1; }; test "$rc" = 0</automated>
    <fails_when>non-zero exit with an `ack not first`, `click payload coordinates`, `no conditional transition` or `no card update` line</fails_when>
    <automated>grep -q 'app.action("reject_proposal", handleRejectProposal)' lib/slack/bolt.ts && grep -q 'app.action("approve_proposal", handleApproveProposal)' lib/slack/bolt.ts && test "$(grep -vE '^\s*(\*|//|/\*)' lib/slack/bolt.ts | grep -cE 'chat\.update|prisma\.|buildApprovalBlocks')" = 0</automated>
    <fails_when>non-zero exit: the reject registration is missing, 02-01's approve registration was disturbed, or bolt.ts gained card or database logic</fails_when>
    <automated>grep -n 'app.action(' lib/slack/bolt.ts</automated>
    <fails_when>the printed line numbers are anything other than exactly two distinct lines, one registering approve_proposal and one registering reject_proposal — a third line means a duplicate registration and every click will fire twice</fails_when>
    <human-check>
      <test>With exactly one Bolt process running: (1) post a top-level message with an obvious time-and-participant ask in the watched channel; (2) read the card that appears; (3) click Reject; (4) scroll the channel below the card</test>
      <expected>(2) One card showing the title, a Hong Kong time, a duration in minutes, at least one participant shown as a Slack mention, a confidence value, and both an Approve and a Reject button. (3) Within a second the SAME message loses both buttons and shows a dismissed chip — the header text stays, the card does not collapse to one line of plain text. (4) No new message anywhere in the channel. If the card collapses to plain text, the update call passed text without blocks; if a second message appears, the update path posted instead of editing.</expected>
      <why_human>Only a real Slack user can click the button, and only a human can see that the message was edited in place rather than replaced.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - lib/slack/blocks.ts exports escapeMrkdwn, buildApprovalBlocks, buildConfirmedBlocks, buildDismissedBlocks and buildAlreadyScheduledBlocks, contains both `action_id: "approve_proposal"` and `action_id: "reject_proposal"`, and imports the formatter from `@/utils/time` (third automated command exits 0)
    - lib/slack/update-proposal-card.ts passes both `text` and `blocks`, contains the `card coordinates missing` guard, and contains zero non-comment post-message calls (fourth automated command exits 0)
    - lib/slack/handlers/reject-proposal.ts has `await ack()` as its first awaited non-comment statement, zero references to the click payload's channel or message fields, one conditional status update and one updateProposalCard call (fifth automated command exits 0)
    - lib/slack/bolt.ts contains `app.action("reject_proposal", handleRejectProposal)` alongside 02-01's untouched approve registration, and gained no card or database logic (sixth automated command exits 0)
    - `grep -n 'app.action(' lib/slack/bolt.ts` prints exactly two lines, one per action id (seventh automated command)
    - `bunx tsc --noEmit` and `bunx biome check lib/slack` both exit 0
    - The users.info probe in Step 1 returned a response containing `user.profile.email`, and 02-02-SUMMARY.md records it
    - Slack: the card shows title, HKT time, duration, participants and confidence with both buttons; a Reject click replaces the buttons with a chip on the same message and posts no new message
    - `ps aux | grep -i bolt` shows one Bolt instance
  </acceptance_criteria>
  <done>A real message produces a real approval card with both buttons, clicking Reject turns that same message into a dismissed chip with no new message in the channel, and the users.info scope probe passed.</done>
</task>

<!-- planner-discipline-allow: email -->

<task type="auto" tdd="false">
  <name>Task 2: Approve path onto the same chip machinery, and users.info email resolution</name>
  <reversibility rating="reversible">resolve-email.ts is a new additive module and the approve handler change is two statements; both can be reverted without touching another phase.</reversibility>
  <files>lib/slack/resolve-email.ts, lib/slack/post-proposal-card.ts, lib/slack/handlers/approve-proposal.ts</files>
  <read_first>
    - lib/slack/handlers/approve-proposal.ts (Phase 1's body as 02-01 Task 2 left it — the statement order to preserve)
    - lib/slack/handlers/reject-proposal.ts (Task 1's conditional transition and status spread, the shape to mirror)
    - lib/slack/post-proposal-card.ts (Task 1's loadParticipantLabels, the function being extended)
    - lib/slack/client.ts (the WebClient singleton) and prisma/schema.prisma (Participant.email nullability)
    - .planning/phases/02-slack-surface/02-RESEARCH.md §"resolveParticipantEmail", §"Pitfall 5", §"Orchestrator Review Notes" item 7
    - .planning/phases/02-slack-surface/02-PATTERNS.md §"lib/slack/resolve-email.ts"
  </read_first>
  <action>
**1. `lib/slack/resolve-email.ts` (SLK-08, D-13).**
- Export `resolveParticipantEmail(slackUserId: string): Promise<string | undefined>`. Task 1's probe already proved the installed token carries both required scopes, so this is a wrapper, not an investigation.
- It calls `users.info` through the `slackClient` singleton and returns `user.profile.email`, which is legitimately absent for bot users and for humans who never set one — `undefined` is a normal result, not an error.
- Cache results in a module-level Map keyed by the Slack user id, including the undefined case, so a card with the same participants does not re-call on every update. Put a `ponytail:` comment on it naming the ceiling: in-process only, cleared on restart, which is correct for a process that runs once per demo window.
- Errors propagate to the caller. This module does not decide the fallback.
- TSDoc naming both required scopes.

**2. Wire it into `loadParticipantLabels` in `lib/slack/post-proposal-card.ts` (SLK-08).**
- For each participant row that has a `slack_user_id`, resolve the address inside a try/catch:
  - success with a value: log `participant email resolved slack_user_id=<id> email=<value>`;
  - success with nothing: log the same line with `email=none`;
  - failure: log `email resolution failed slack_user_id=<id> code=<slack error code>` and continue. The card must still post — a resolution failure is never allowed to swallow the card.
- A participant row with no `slack_user_id` is skipped entirely; no call is made for it.
- When the row's stored address is falsy and a value came back, write it onto that Participant row — one guarded update, and the reason Phase 4's invite can read the column directly. Guard for both null and empty string, because the column's nullability is Phase 1's choice. Never modify the schema.
- The returned labels are unchanged: Slack mentions, never addresses. Nothing address-shaped reaches a block builder.

**3. Approve onto the same machinery (SLK-04, SLK-06, D-11, D-12).** Edit `lib/slack/handlers/approve-proposal.ts` minimally — Phase 4 replaces this body, so change only what SLK-06 requires:
- Keep `await ack()` first and the proposal-id read immediately after; do not reorder anything above them.
- Replace the unconditional status write with the same conditional update Task 1 used in the reject handler: where id and status `pending`, set `confirmed`; when the affected count is 0, log `already decided proposal_id=<id> status=<status>` and return without a Slack call.
- Pass the row spread with its new status into `updateProposalCard`, so the confirmed chip renders instead of the approval card. Phase 1's stub passed the row it read before the write, which now renders the wrong branch — this is the line that must change.
- Do not add a calendar call, an organizer claim or a second message. Do not touch the registration in bolt.ts; 02-01 already made it.

**4. Restart Bolt** on the recorded runtime, exactly one process, teeing to `/tmp/ai-secretary-bolt.log`.
  </action>
  <verify>
    <automated>bunx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any `error TS` line in the output</fails_when>
    <automated>bunx biome check lib/slack</automated>
    <fails_when>non-zero exit, or the summary line reports one or more errors</fails_when>
    <automated>rc=0; grep -q 'export async function resolveParticipantEmail' lib/slack/resolve-email.ts || { echo "missing resolver export"; rc=1; }; grep -q 'users.info' lib/slack/resolve-email.ts || { echo "no users.info call"; rc=1; }; grep -q 'resolveParticipantEmail(' lib/slack/post-proposal-card.ts || { echo "resolver not wired into the card path"; rc=1; }; grep -q 'participant email resolved' lib/slack/post-proposal-card.ts || { echo "no resolution log line"; rc=1; }; test "$rc" = 0</automated>
    <fails_when>non-zero exit with a `missing resolver export`, `no users.info call`, `resolver not wired into the card path` or `no resolution log line` line</fails_when>
    <automated>rc=0; a=$(grep -vE '^\s*(\*|//|/\*)' lib/slack/handlers/approve-proposal.ts); echo "$a" | grep -m1 'await ' | grep -q 'await ack()' || { echo "ack not first"; rc=1; }; echo "$a" | grep -q 'updateMany' || { echo "no conditional transition"; rc=1; }; echo "$a" | grep -q 'already decided' || { echo "no already-decided no-op"; rc=1; }; test "$(echo "$a" | grep -cE 'calendar|organizer_user_id')" = 0 || { echo "phase 4 logic leaked in"; rc=1; }; test "$rc" = 0</automated>
    <fails_when>non-zero exit with an `ack not first`, `no conditional transition`, `no already-decided no-op` or `phase 4 logic leaked in` line</fails_when>
    <automated>test "$(grep -vE '^\s*(\*|//|/\*)' lib/slack/blocks.ts | grep -ci 'email')" = 0 && test "$(grep -rnE 'process\.env' lib/slack --include='*.ts' | grep -vcE ':[0-9]+:\s*(\*|//)')" = 0</automated>
    <fails_when>non-zero exit: an address-shaped field reached the pure block builders, or a non-comment direct environment read exists under lib/slack</fails_when>
  </verify>
  <acceptance_criteria>
    - lib/slack/resolve-email.ts exports `resolveParticipantEmail`, calls `users.info`, and is called from lib/slack/post-proposal-card.ts, which contains the `participant email resolved` log line (third automated command exits 0)
    - lib/slack/handlers/approve-proposal.ts still acks first, now uses the conditional pending-only transition with the `already decided` no-op, and contains no calendar or organizer logic (fourth automated command exits 0)
    - lib/slack/blocks.ts contains zero non-comment address references and lib/slack holds no non-comment direct environment read (fifth automated command exits 0)
    - `bunx tsc --noEmit` and `bunx biome check lib/slack` exit 0
    - lib/slack/bolt.ts is byte-identical to its state after Task 1
    - Exactly one Bolt process is running after the restart
  </acceptance_criteria>
  <done>Approve produces a confirmed chip through the same update path Reject uses, a second click on either is a logged no-op, and posting a card logs one resolved address per participant carrying a Slack user id.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Hand-check SLK-05, SLK-06 and SLK-08 in Slack</name>
  <read_first>
    - lib/slack/blocks.ts, lib/slack/update-proposal-card.ts, lib/slack/handlers/{approve,reject}-proposal.ts (what was built)
    - /tmp/ai-secretary-bolt.log (the live Bolt log)
    - .planning/ROADMAP.md §"Phase 2: Slack Surface" Success Criteria 2, 3 and 4
  </read_first>
  <what-built>A real Block Kit approval card with Approve and Reject, a status-driven in-place chat.update addressed by the coordinates stored on the Proposal row, a pending-only conditional status transition on both buttons, and users.info email resolution logged per participant</what-built>
  <how-to-verify>
    1. Run `ps aux | grep -i bolt`. Expect exactly one Bolt instance across every worktree (D-02, success criterion 4).
    2. Post a top-level message with a time-and-participant ask in the watched channel. Expect one card showing title, a Hong Kong time, a duration in minutes, participants as Slack mentions, a confidence value, and both buttons (SLK-05, success criterion 2).
    3. Click **Approve**. Expect that same message to lose both buttons and show a confirmed chip within a second, with no new message anywhere in the channel (SLK-06, success criterion 3). The header must survive — a collapse to one line of plain text means the update passed text without blocks.
    4. Scroll back to the card and click **Approve** again from the stale render (or click **Reject** on it). Expect an `already decided` line in the log, no visible change to the card, and no second update (SLK-06 idempotency).
    5. Post a second message, then click **Reject** on its card. Expect a dismissed chip, visually distinct from step 3's confirmed chip, on that same message.
    6. Run `grep 'participant email resolved' /tmp/ai-secretary-bolt.log`. Expect one line per participant carrying a Slack user id, with an address matching the seeded Slack profile — or `email=none` for a participant who genuinely has none (SLK-08).
    7. Read both cards in Slack. Expect NO address visible anywhere on either card.
    8. Scan the log for `missing_scope`, `channel_not_found`, `message_not_found` or `invalid_blocks`. Expect none. `invalid_blocks` means a block exceeded a length limit or a plain_text block was empty.
  </how-to-verify>
  <resume-signal>Type "approved", or name the failing step and paste its log lines</resume-signal>
  <acceptance_criteria>
    - Step 1 shows one Bolt instance
    - Step 2's card shows all five fields and both buttons
    - Steps 3 and 5 each replace the buttons with a chip on the same message, and no step posts a new message to the channel
    - Step 4 logs `already decided` and changes nothing
    - Step 6 finds a resolved-address line per Slack participant; step 7 finds no address on either card
    - Step 8 finds none of the four error strings
  </acceptance_criteria>
  <done>The human typed "approved" after all eight steps matched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slack channel members to the click handlers | A `block_actions` payload can be produced by any member of the channel the card is in, carrying an attacker-chosen `value` |
| Proposal row to Block Kit | Proposal-derived text (title, and from Phase 5 onward, text lifted from a user's message) is rendered into a message every channel member sees |
| Slack Web API to Postgres | Resolved email addresses cross from Slack's user directory into our rows and our log |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Elevation of privilege | Approve/Reject clicked by any channel member, not only the organizer | low | accept | Two-user demo; a click only moves a Proposal to a terminal status and this phase writes nothing to Calendar. Phase 4's conditional organizer claim (APR-02) is the real mitigation and is out of scope here |
| T-02-09 | Tampering | Double click, or two users clicking the two different buttons, producing two chat.update calls and a flipping chip | medium | mitigate | Both handlers transition the row with a conditional update requiring status `pending`; the loser sees an affected count of 0, logs `already decided` and makes no Slack call. The chip itself removes the buttons, so a second click needs a stale render |
| T-02-10 | Tampering | Proposal-derived text rendered into Block Kit (channel broadcast, false mention, spoofed link) | medium | mitigate | The title renders in a `plain_text` header, which Slack does not parse as mrkdwn; every mrkdwn field passes through `escapeMrkdwn` (`&`, `<`, `>` to entities) and is truncated to 2000 characters, the header to 150 |
| T-02-11 | Information disclosure | Resolved participant email addresses (PII) | medium | mitigate | Addresses appear in exactly one process log line and optionally the Participant row; they never reach card text or any Slack-visible surface, and the block builders contain no address reference at all (grep gate). Log lines are local terminal and `/tmp` only |
| T-02-12 | Denial of service | A `users.info` failure or missing scope aborting the whole card post | medium | mitigate | Resolution is wrapped per participant; the Slack error code is logged and the card still posts with mention labels. Task 1's probe proves both scopes before any code depends on them, so a missing scope surfaces as a stop, not as a silently email-less demo |
| T-02-13 | Tampering | A missing or stale `card_channel`/`card_ts` causing the update path to post a replacement message | medium | mitigate | `updateProposalCard` returns after a `card coordinates missing` log when either is absent, and contains zero post-message calls in any branch (grep gate); the click payload's own coordinates are never substituted (D-11) |
| T-02-14 | Information disclosure | A raw Slack or Prisma error surfacing to the channel from a click handler | low | mitigate | Handlers ack with no response body and never post an error message; failures are logged only, so a channel member learns nothing about internal state from a failed click |

No package installs this phase (Research §Standard Stack), so there is no supply-chain entry.
</threat_model>

<verification>
- `bunx tsc --noEmit` and `bunx biome check lib/slack` exit 0 after Task 2
- Every grep gate in Tasks 1 and 2 exits 0
- All eight Task 3 hand checks pass
- `postProposalCard` and `updateProposalCard` still have Phase 1's parameter order and arity; the only contract change is the widened `channel`/`ts` input type, recorded in 02-02-SUMMARY.md
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 2: the approval card shows title, HKT time, duration, participants, confidence, and both Approve and Reject
- ROADMAP Phase 2 success criterion 3: clicking Approve or Reject replaces the buttons with a status chip on the same message, and no new message appears
- ROADMAP Phase 2 success criterion 4: exactly one Bolt process runs across every worktree
- SLK-08: one resolved address per Slack participant appears in the log, and none appears on a card
- 02-02-SUMMARY.md records the users.info probe result, the widened updateProposalCard input type, and the confidence scale that was found in Phase 1's schema
</success_criteria>

## Artifacts this phase produces

See 02-01-PLAN.md §"Artifacts this phase produces" for the phase-level table; the rows 02-02 is responsible for are:

| Artifact | Symbol(s) | Created by |
|---|---|---|
| `lib/slack/blocks.ts` | `escapeMrkdwn`, `buildDismissedBlocks`, `buildAlreadyScheduledBlocks` (new); `buildApprovalBlocks`, `buildConfirmedBlocks` (real bodies, names unchanged) | 02-02 Task 1 |
| `lib/slack/post-proposal-card.ts` | `postProposalCard` (real body, signature frozen); `loadParticipantLabels` (additive export) | 02-02 Task 1, extended Task 2 |
| `lib/slack/update-proposal-card.ts` | `updateProposalCard(channel, ts, proposal)` (status-driven body, arity frozen, input type widened to accept null coordinates) | 02-02 Task 1 |
| `lib/slack/handlers/reject-proposal.ts` | `handleRejectProposal` | 02-02 Task 1 |
| `lib/slack/handlers/approve-proposal.ts` | `handleApproveProposal` (conditional transition + new-status spread) | 02-02 Task 2 |
| `lib/slack/resolve-email.ts` | `resolveParticipantEmail(slackUserId)` | 02-02 Task 2 |
| `lib/slack/bolt.ts` | `app.action("reject_proposal", handleRejectProposal)` — one added line | 02-02 Task 1 |
| `.planning/phases/02-slack-surface/COVERAGE.md` | Slack Web/Events API coverage matrix | planning |

## Source Coverage Audit

Phase-level, covering both plans: see 02-01-PLAN.md §"Source Coverage Audit (phase 02 plan set)". 02-02's rows there are SLK-04, SLK-05, SLK-06, SLK-08, Research Notes 3, 4 and 7, and CONTEXT D-07 through D-13, D-14, D-15, D-16 and D-19 — all COVERED by the three tasks above. One flagged assumption is recorded in §"Planning assumptions": the spec-less probe's SLK-05 `unclassified` row carried no derivable predicate and stays unresolved rather than being silently dropped.

<output>
Create `.planning/phases/02-slack-surface/02-02-SUMMARY.md` when done. It records the users.info probe result (scopes proven or the dashboard fix needed), the widened `updateProposalCard` channel/ts input type as the one additive contract change, the confidence scale found in Phase 1's schema, and the chip copy chosen for confirmed, dismissed and already_scheduled.
</output>
