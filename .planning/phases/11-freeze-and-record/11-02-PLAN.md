---
phase: 11-freeze-and-record
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [DMO-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "D-09 / ROADMAP SC4: every required README heading (what it is, core functionality, how it differs, problems tackled, architecture, usage, scope and non-goals, production design notes) carries real project-specific prose, and the unfilled-marker grep over README.md prints nothing"
    - "D-10 / ROADMAP SC2: the competitive section names all seven groups (Slackbot; Reclaim/Motion; Clockwise; Slack calendar apps; Fireflies/Otter/Spinach; n8n/Zapier; Relay.app), states Clockwise as shut down 27 March 2026 and Relay.app as winding down with paid access ending 14 September 2026, and the human-in-the-loop comparison names n8n/Zapier before Relay.app"
    - "D-11: batch-first detection is documented as the production design with the ~30-token message vs ~700-token system prompt arithmetic giving ~20x amortization, AND with the reason regex/keyword pre-filtering cannot be the primary filter ('same time as last week', 'after standup', typos, abbreviations, Cantonese-English code-switching, misses that are silent and untunable)"
    - "D-12: a two-sentence note records that the schema is already multi-tenant (Installation keyed by team_id, unique on team_id plus slack_user_id) and that OAuth distribution itself was not built"
    - "D-13: exactly one line of README.md mentions where a graph-level pause would fit — a pause measured in seconds where resumption continues reasoning, not an approval gate spanning unbounded human time"
    - "D-14 / ROADMAP SC3: the literal output of /ponytail-debt, run live during this plan, sits verbatim and unedited inside a fenced text block under the Known shortcuts heading, with any broadened-scan findings kept in a clearly labelled block outside that fence"
    - "D-15: the abandoned stretch work section is written from execution-time evidence (Phase 8/9 SUMMARY and VERIFICATION files, git branch -a, git log --oneline main, STATE.md) and names either the abandon criterion that actually fired (Phase 8 D-02 'not rendering at least two component types by 14:40'; Phase 9 D-04 'nothing merges unless demo-ready') or states plainly that the work was designed and not built because the schedule never opened the gate"
    - "D-16 / ROADMAP cut order: a complete draft README with every required heading filled exists from Task 1 onward, so the recording always outranks README polish; under time pressure the expansion tasks shrink but no section is deleted"
    - "D-01 / D-02: README.md is the only file this plan modifies; the single commit lands directly on main with no new branch, and git show --name-only --format='' HEAD lists exactly one path, README.md"
    - "The Usage section is built from the Phase-1 artifacts actually on disk (root CLAUDE.md, package.json scripts, docker-compose.yml, the .env example file) and documents bun commands only, listing env var NAMES with no value of any kind"
    - "Conditional claims match what is live on main: the core-functionality section claims a per-person knowledge layer only if Phase 9 shipped it, and the conflict wording matches whichever flow is live (CFL-01..04 two reasoned alternatives, or the CFL-05 static warning)"
  artifacts:
    - "README.md at the repo root, carrying every heading listed in 'Artifacts this phase produces' below"
    - ".planning/phases/11-freeze-and-record/11-02-SUMMARY.md"
  key_links:
    - "README Usage section -> the real Phase-1 package.json scripts, docker-compose.yml and .env example file: every command printed must be one that actually runs on this repo"
    - "README conflict wording -> the conflict flow actually merged to main by Phase 7: full CFL alternatives vs the CFL-05 static warning"
    - "README abandoned-stretch wording -> Phase 8/9 artifacts and git history: the stated outcome must be the one that happened"
    - "Known shortcuts verbatim fence -> the live /ponytail-debt run in this window, not a remembered or reconstructed ledger"
  prohibitions:
    - requirement_id: DMO-04
      category: accuracy
      status: unresolved
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT attribute Clockwise's shutdown to Reclaim in any wording. The verified acquirer is Salesforce and it was an acquihire of the team, not a product acquisition; .planning/research/FEATURES.md's Reclaim-attribution claim is superseded by 11-RESEARCH.md:236"
    - requirement_id: DMO-04
      category: accuracy
      status: unresolved
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT present Clockwise or Relay.app as live or current competitors anywhere in README.md; both are cited only with their defunct / winding-down status"
    - requirement_id: DMO-04
      category: privacy
      status: unresolved
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT contain any secret value: env var NAMES only, never a bot or app token, never a Google refresh token, never an API key, never a connection string carrying credentials"
    - requirement_id: DMO-04
      category: scope
      status: unresolved
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT stage any path other than README.md, and MUST NOT use a whole-tree staging command; 11-01's recording and every other untracked file stay out of this commit"
    - requirement_id: DMO-04
      category: accuracy
      status: unresolved
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT claim a per-person knowledge layer exists, and MUST NOT describe a stretch abandonment that did not happen, unless execution-time evidence on disk shows it"
    - requirement_id: DMO-04
      category: fidelity
      status: unresolved
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT paraphrase, trim, reformat or edit anything inside the verbatim /ponytail-debt fence; supplementary findings go in a separate labelled block outside it"
---

# Phase 11 Plan 02: Finalize README.md (DMO-04)

## Phase Goal

**As a** judge or stranger opening this repo after the demo, **I want to** read one README that explains what this is, how it genuinely differs from what already exists, and what was deliberately left out and why, **so that** the project reads as engineering judgement rather than a hackathon stub.

Framed from the ROADMAP Phase 11 Goal line ("A clean run is recorded as the wifi fallback, and the README is complete"), which is not itself in user-story form. The framing adds no scope.

## Acceptance Criteria

- [ ] ROADMAP SC4 / D-09: every required heading present and filled with real prose; the unfilled-marker grep over README.md prints nothing
- [ ] ROADMAP SC2 / D-10: all seven competitor groups named; Clockwise dated 27 March 2026 as shut down; Relay.app dated 14 September 2026 as winding down; n8n/Zapier named before Relay.app; no Reclaim attribution for Clockwise
- [ ] ROADMAP SC3 / D-14: the live `/ponytail-debt` output sits verbatim in a fenced text block
- [ ] D-13: the graph-pause token appears on exactly one line of README.md
- [ ] D-01 / D-02: one commit on `main`; `git show --name-only --format='' HEAD` lists only `README.md`

<objective>
Finalize `README.md` at the repo root so it covers every required DMO-04 section with real content: the corrected competitive comparison, batch-first detection with the ~20x cost reasoning, multi-workspace via OAuth, the one-line note on where a graph-level pause would fit, the verbatim `/ponytail-debt` known-shortcuts block, and the abandoned stretch work with the reason it was abandoned.

Purpose: the README is the artifact that outlives the demo (D-09). Per D-03 this is plan 11-02 of two; it runs in Wave 1 beside 11-01 because it touches only `README.md` while 11-01 touches no repo file at all. Per D-16 the recording outranks README polish, which is why Task 1 is a tracer that lands a complete filled draft before any expansion work begins — if the window collapses, the expansion tasks shrink and no section is ever deleted.
Output: a finalized `README.md` committed directly to `main` in one commit (D-02).

Discretion taken (CONTEXT "Claude's Discretion", default option): the README notes in one line that a screen-recorded fallback run of the demo exists and is deliberately not committed to this repo. The video file itself is never added.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/11-freeze-and-record/11-CONTEXT.md
@.planning/phases/11-freeze-and-record/11-RESEARCH.md
@.planning/phases/11-freeze-and-record/11-PATTERNS.md
@gsd-prompt-ai-secretary.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Complete draft README — every required heading, one real sentence minimum, nothing unfilled</name>
  <precondition>On `main` with Phase 1 done: `git branch --show-current` prints `main` and `package.json` exists at the repo root</precondition>
  <files>README.md</files>
  <read_first>
    - .planning/phases/11-freeze-and-record/11-PATTERNS.md (Content Source Map — every section's source with line anchors; reuse as-is)
    - .planning/phases/11-freeze-and-record/11-RESEARCH.md ("README Content Source Map", "Exact quotable text")
    - .planning/PROJECT.md lines 5, 83-90, 136-146, 148-159 (What This Is; Runtime processes; Agent design; Data model)
    - gsd-prompt-ai-secretary.md lines 31-40 (the required README section list), line 102 (the graph-pause sentence)
    - README.md as it stands (Phase 1's FND-02 skeleton). If it is missing entirely, or was cut to headings only under ROADMAP cut-line row 1, create it outright — same output either way.
  </read_first>
  <action>Write README.md end to end so that every heading below exists and carries at least one complete, project-specific sentence. This is the thin end-to-end pass that satisfies the D-16 floor: after this task a reader can read the whole document and nothing is unfinished. Depth arrives in Tasks 2-4; absence of content does not.

Heading set to create, in this order, with this exact text (level-2 unless noted):
`# AI Secretary` (title, then a one-paragraph lede), `## What it is`, `## The core functionality`, `## How it differs`, `## The problems it tackles`, `## Architecture`, `## Usage`, `## Scope and non-goals`, `## Production design notes`, and under that parent: `### Batch-first detection`, `### Multi-workspace via OAuth`, `### Where a graph-level pause would fit`, `### Known shortcuts`, `### Abandoned stretch work`.

Content for this pass, drawing on the quotes 11-PATTERNS.md and 11-RESEARCH.md already extracted:
1) "What it is": the PROJECT.md:5 paragraph, near-verbatim.
2) "The core functionality": unprompted intent detection in ordinary conversation, plus an approval gate before anything is written to a calendar. Write only those two halves. Do NOT yet claim a per-person knowledge layer — that claim is conditional on Phase 9 and is decided in Task 3.
3) "How it differs": one honest paragraph now; the full competitive treatment lands in Task 2.
4) "The problems it tackles": the gsd-prompt-ai-secretary.md:36 sentence, usable verbatim — commitments made in chat evaporate into scrollback; scheduling costs a round trip of messages; an agent that acts without asking is untrustworthy; an agent that asks about everything is noise, which is why the confidence gate exists.
5) "Architecture": a lede sentence, then the four runtime processes and what each owns (Bolt in Socket Mode as its own process; the Next.js App Router dashboard; Trigger.dev tasks invoking the LangGraph agent; Postgres through Prisma), the data-model block from PROJECT.md:148-159, and one paragraph narrating message to proposal to approval to calendar. Keep it flat — bold labels, not sub-headings.
6) "Usage": prerequisites, env var NAMES, `docker compose` startup, the per-process run commands, and how to install the Slack app. A first pass from PROJECT.md:127-134 is enough here; Task 3 rebuilds it against the real Phase-1 files.
7) "Scope and non-goals": a lede sentence plus a first pass at the REQUIREMENTS.md Out of Scope material; the full table lands in Task 2. Add here the one-line discretion note that a screen-recorded fallback run of the demo exists and is deliberately not committed to this repo.
8) "Production design notes": one sentence of lede under the parent heading, so the parent is not a bare heading.
9) "Batch-first detection" and "Multi-workspace via OAuth": one real sentence each now, expanded in Task 2.
10) "Where a graph-level pause would fit": write the single line now, from gsd-prompt-ai-secretary.md:102 — a pause measured in seconds where the resumption continues reasoning, such as an agent asking a clarifying question mid-analysis; an approval gate spanning unbounded human time is not that case. Per D-13 this is the only line in the whole file that may carry LangGraph's pause-primitive token; do not put that token in the heading.
11) "Known shortcuts": a lede sentence plus the planning-doc-sourced shortcuts in ordinary prose — the Google refresh token sitting in Postgres in plaintext, seeded from env and accepted for a one-laptop demo; `prisma db push` for the whole window with no migration history; only one Bolt process across all worktrees, because Socket Mode load-balances events across every connection on an app token and a second process steals them; no tests, Biome as the only linter and formatter, TSDoc on every function; hand-seeded Slack-user to Google-token linking with no linking UI. Do NOT create the `####` sub-headings yet — Task 4 adds them together with the live ledger.
12) "Abandoned stretch work": write only what is true regardless of outcome — two stretch tracks (S1, per-user preference memory via Graphiti and Neo4j; S2, a CopilotKit commitment ledger) were designed and scoped before the window, each carrying a hard abandon criterion, because stretch work here is abandoned rather than finished late. The actual outcome sentence is added in Task 3 from evidence.

Every heading must be followed by prose before the next heading; the empty-section gate below enforces exactly that. Nothing unfinished, no marker text, no bracketed stand-in anywhere in the file.</action>
  <verify>
    <automated>for h in '^## What it is$' '^## The core functionality$' '^## How it differs$' '^## The problems it tackles$' '^## Architecture$' '^## Usage$' '^## Scope and non-goals$' '^## Production design notes$' '^### Batch-first detection$' '^### Multi-workspace via OAuth$' '^### Where a graph-level pause would fit$' '^### Known shortcuts$' '^### Abandoned stretch work$'; do grep -qE "$h" README.md || { echo "MISSING: $h"; exit 1; }; done; echo HEADINGS-OK</automated>
    <fails_when>non-zero exit with a "MISSING: " line naming the absent heading, or the trailing HEADINGS-OK line never prints</fails_when>
    <automated>awk '/^#+ /{ if (h != "" && b == 0) { print "EMPTY SECTION: " h; bad=1 } h=$0; b=0; next } /[A-Za-z]/ { b=1 } END { if (h != "" && b == 0) { print "EMPTY SECTION: " h; bad=1 } exit bad }' README.md</automated>
    <fails_when>non-zero exit with an "EMPTY SECTION: " line naming a heading that has no prose before the next heading</fails_when>
    <automated>! grep -niE 'TODO|TBD|FIXME|lorem|placeholder|XXX|coming soon' README.md</automated>
    <fails_when>non-zero exit, i.e. grep printed a numbered README.md line still carrying an unfilled marker</fails_when>
    <automated>OUT=$(git diff --name-only HEAD); RC=$?; test "$RC" -eq 0 && test "$OUT" = "README.md"</automated>
    <fails_when>non-zero exit: the working tree has a modification in some file other than README.md, or README.md is not modified at all</fails_when>
  </verify>
  <acceptance_criteria>
    - The heading loop prints HEADINGS-OK and exits 0 — all thirteen required headings present with their exact text
    - The awk empty-section gate exits 0 — no heading in the file is followed straight by another heading
    - The unfilled-marker grep prints nothing
    - `git diff --name-only HEAD` lists exactly `README.md` and nothing else
    - The graph-pause line exists and mentions the seconds-long-pause case, not the approval gate (checked again in Task 5)
  </acceptance_criteria>
  <done>A complete, readable README exists on disk with every required heading filled — the D-16 draft floor is met before any expansion work starts.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the static sections — corrected competitive comparison, architecture depth, scope and non-goals, batch-first, OAuth</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/11-freeze-and-record/11-RESEARCH.md lines 232-241 ("Competitive-comparison dates", web-verified 2026-09-11) and lines 142-177 ("Exact quotable text")
    - .planning/phases/11-freeze-and-record/11-PATTERNS.md lines 41-52 (the correction-required block) and 67-84
    - .planning/REQUIREMENTS.md lines 123-140 (Out of Scope table)
    - .planning/PROJECT.md line 142 (batch-first paragraph), line 212 (multi-tenant Key Decisions row), lines 83-90 and 136-146
    - gsd-prompt-ai-secretary.md line 35 (the ORIGINAL "how it differs" paragraph — reuse its structure, correct its facts)
  </read_first>
  <action>Deepen four sections written thin in Task 1. Add no new top-level headings.

A) "How it differs" (D-10, ROADMAP SC2). Reuse the structure of the source paragraph at gsd-prompt-ai-secretary.md:35, but correct its facts — that paragraph lists both Clockwise and Relay.app as live competitors, and both are wrong as of this demo date. Name all seven groups:
  - Slackbot: extracts action items and checks availability, but you invoke it; it is workspace-scoped and gated to paid plans.
  - Reclaim and Motion: schedule from natural language, but do not read conversations to find intent.
  - Clockwise: shut down 27 March 2026 — the team joined Salesforce in an acquihire and the product itself was discontinued. Cite it as prior art in this space, never as a live competitor. Clockwise's shutdown must never be attributed to Reclaim in any wording; the verified acquirer is Salesforce, and .planning/research/FEATURES.md's Reclaim-attribution claim is superseded by 11-RESEARCH.md:236.
  - Slack calendar apps: command-driven.
  - Fireflies, Otter and Spinach: extract action items from meeting audio, not chat.
  - n8n and Zapier: generic human-in-the-loop approval steps with no understanding of your calendar or your habits. Per D-10 and the PROJECT.md Key Decision, these LEAD the human-in-the-loop comparison — name them before Relay.app in the text, because they are the two still active.
  - Relay.app: winding down; free access ended 15 August 2026 and paid access ends 14 September 2026, two days after this demo. Cite it only with that status.
Close with the gap: nothing in that list watches an ordinary conversation and decides, unprompted, that something needs doing.

B) "Architecture". Expand the Task 1 pass into the full picture: each runtime process and what it owns, why Bolt is a separate process (a long-lived WebSocket does not fit request-scoped Next.js route handlers), the data model, and the message to proposal to approval to calendar flow. State the re-derive-don't-resume design: the agent run ends when the proposal is created, and the Approve handler is ordinary backend code that reads the row, re-checks the calendar as it is now, and creates the event. Mention idempotency at both levels — one Proposal per intent via `dedupe_key`, one calendar event via a deterministic event id with a 409 fallback to `events.get`.

C) "Scope and non-goals". Reproduce the REQUIREMENTS.md:123-140 Out of Scope material as a Feature/Reason table, then a short paragraph on what that says about the product's thesis. Two wording constraints: write the package-manager row's four tool names as inline code separated by slashes with no bare name followed by a word, so the bun-only gate in Task 3 stays meaningful; and write the LangGraph pause row without LangGraph's pause-primitive token, so D-13's one-line rule holds across the whole file — describe it as "LangGraph's pause-and-resume primitive for the approval gate" and give the reason (approval waits are unbounded; re-derive from the row instead).

D) "Batch-first detection" (D-11). Use the PROJECT.md:142 paragraph, quoted in 11-RESEARCH.md:144-145. Both halves are mandatory: the cost arithmetic — a message is about 30 tokens against an about-700-token system prompt, so per-message calls pay the prompt every time and batching a window amortizes it for roughly 20x savings — and the reason regex/keyword pre-filtering cannot be the primary filter: it fails on "same time as last week", "after standup", typos, abbreviations and Cantonese-English code-switching, and its misses are silent and untunable. Then the production shape: a cheap local router for time-urgent messages only (poor recall allowed), a per-channel batch sweep over a bounded window as the workhorse, full extraction plus conflict reasoning on candidates only, with `dedupe_key` absorbing overlapping windows.

E) "Multi-workspace via OAuth" (D-12). Exactly two sentences, from PROJECT.md:212: the schema is multi-tenant from the start — `Installation` is keyed by `team_id` and users are unique on the pair of `team_id` and `slack_user_id`, because Slack user ids are workspace-scoped — and the public OAuth install flow that would populate `Installation` was not built for a one-workspace demo. Do not expand past two sentences.</action>
  <verify>
    <automated>for n in Slackbot Reclaim Motion Clockwise Fireflies Otter Spinach n8n Zapier Relay.app 'calendar apps'; do grep -qiF "$n" README.md || { echo "MISSING COMPETITOR: $n"; exit 1; }; done; echo COMPETITORS-OK</automated>
    <fails_when>non-zero exit with a "MISSING COMPETITOR: " line naming the absent name, or the trailing COMPETITORS-OK line never prints</fails_when>
    <automated>grep -qE '27 (Mar|March) 2026' README.md && grep -qE '14 (Sep|September) 2026' README.md && grep -i 'Clockwise' README.md | grep -qi 'shut down' && grep -iF 'Relay.app' README.md | grep -qiE 'winding down|wind-down'</automated>
    <fails_when>non-zero exit: one of the four clauses found nothing — no 27 March 2026 date, no 14 September 2026 date, no Clockwise line that also says shut down, or no Relay.app line that also says winding down</fails_when>
    <automated>! grep -niE '(folded into|absorbed (by|into)|acquired by|merged into) Reclaim' README.md</automated>
    <fails_when>non-zero exit, i.e. grep printed a README.md line attributing a competitor's shutdown to Reclaim</fails_when>
    <automated>test "$(grep -n 'n8n' README.md | head -1 | cut -d: -f1)" -lt "$(grep -nF 'Relay.app' README.md | head -1 | cut -d: -f1)"</automated>
    <fails_when>non-zero exit: the first Relay.app line is at or above the first n8n line, or either grep printed nothing and test reports "integer expression expected"</fails_when>
    <automated>grep -qE '20 ?[x×]|20-fold|twentyfold' README.md && grep -qiE '700' README.md && grep -qi 'code-switching' README.md && grep -qiE 'silent and untunable|silently|untunable' README.md</automated>
    <fails_when>non-zero exit: the batch-first section is missing the ~20x amortization claim, the 700-token prompt figure, the code-switching failure mode, or the statement that regex misses are silent/untunable</fails_when>
    <automated>grep -q 'team_id' README.md && grep -qi 'slack_user_id' README.md && grep -qi 'OAuth' README.md</automated>
    <fails_when>non-zero exit: the multi-tenant note is missing the team_id key, the slack_user_id pairing, or the word OAuth</fails_when>
  </verify>
  <acceptance_criteria>
    - The competitor loop prints COMPETITORS-OK; all seven D-10 groups appear
    - Both corrected dates appear, each on a line that also carries the correct status word
    - The Reclaim-attribution grep prints nothing
    - The first n8n line number is strictly lower than the first Relay.app line number
    - The batch-first gate exits 0 — cost arithmetic AND the regex-failure reasoning are both present
    - The multi-tenant gate exits 0, and the OAuth note is two sentences by hand count
  </acceptance_criteria>
  <done>The four window-independent sections carry full, fact-corrected content; D-10, D-11 and D-12 are satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Execution-time truth pass — real Usage section, conditional claims, abandoned stretch outcome</name>
  <precondition>Phase 1's env example file is on disk: `ls .env*.example` prints at least one path</precondition>
  <files>README.md</files>
  <read_first>
    - README.md as written by Tasks 1-2
    - Root `CLAUDE.md`, `package.json` (the `scripts` block), `docker-compose.yml`, and the `.env*.example` file(s) — all Phase-1 artifacts, read now, not assumed
    - .planning/PROJECT.md lines 127-134 ("Services, connections, environments") and line 79 (the Slack app's installed scopes and events)
    - .planning/phases/08-optional-s2-commitment-ledger/08-CONTEXT.md (D-02, the abandon criterion) and .planning/phases/09-optional-s1-graphiti-preference-memory/09-CONTEXT.md (D-03/D-04, the abandon rule)
    - .planning/STATE.md lines 76-77 (stretch phases realistically README-only)
    - .planning/REQUIREMENTS.md CFL-01..CFL-05
  </read_first>
  <action>Three edits, each grounded in something read off disk in this window. Never assume an outcome.

A) Rebuild "Usage" from the real Phase-1 artifacts. Read `package.json`'s `scripts` block, `docker-compose.yml` and the `.env*.example` file, and document what is actually there: prerequisites (bun, Docker, a Slack app in Socket Mode, a Google Cloud project with the Calendar API and a consenting test user, an Kilo Gateway key, a Trigger.dev project); starting Postgres with `docker compose`; pushing the schema with `bunx prisma db push`; and the per-process run commands — `bun run dev` for Next.js, `bun lib/slack/bolt.ts` for the Bolt process (or the `bunx tsx` fallback if that is what Phase 1 settled on), and `bunx trigger.dev@4.5.16 dev` for the Trigger.dev CLI. Note that exactly one Bolt process may run at a time. bun commands only — no other package manager anywhere in the file. List env var NAMES exactly as the example file spells them, grouped by service, each with a one-line description of what it points at; never print a value, never print a credential-bearing connection string, and point the reader at the example file for shapes. Finish with how to install the Slack app: Socket Mode on, the `message.channels` event plus the `channels:history`, `users:read.email`, `chat:write` and `commands` scopes, then paste the bot and app tokens into the env file under the names already listed.

B) Fix the conditional claims against what is actually live on `main`. Run the evidence scan below first, then:
  - "The core functionality": add the per-person knowledge layer as a third capability ONLY if Phase 9 shipped and merged (a `09-*-SUMMARY.md` exists and its work is on `main`). If it did not, leave the two-capability wording from Task 1 untouched and let the knowledge layer appear only under "Abandoned stretch work".
  - The conflict wording in "What it is" and "Architecture": describe whichever flow is live. If Phase 7 landed CFL-01..04, say the agent proposes two concrete alternative slots each with a one-line stated reason. If it degraded to CFL-05, say the card shows a static conflict warning naming the clashing block, and say plainly that the reasoned-alternatives version is the designed form that the window did not reach. Do not describe a flow that is not on `main`.

C) Write the real "Abandoned stretch work" outcome (D-15), replacing nothing from Task 1's design paragraph — append to it. Branch the wording on the evidence:
  - Attempted, then abandoned: name the criterion that actually fired. Phase 8's D-02 is "the ledger is not rendering at least two different component types by 14:40". Phase 9's D-03 is "the first episodes-to-preferences round trip has not succeeded within 30 minutes", and its D-04 is "nothing merges to main unless demo-ready".
  - Never started: say so plainly — designed and scoped, not built, because the track was gated on Phase 7 finishing materially early and the schedule never opened that gate. Cite the honest arithmetic, not a fictional abandonment.
  Say in one sentence why abandoning beats finishing late here: unfinished stretch work on `main` risks the core demo, and the demo is the deliverable.</action>
  <verify>
    <automated>LOG=$(git log --oneline -40 main); test -n "$LOG"; RC=$?; echo "$LOG"; git branch -a --list 'gsd/phase-8-*' 'gsd/phase-9-*'; ls .planning/phases/08-optional-s2-commitment-ledger/08-*-SUMMARY.md .planning/phases/08-optional-s2-commitment-ledger/08-*-VERIFICATION.md .planning/phases/09-optional-s1-graphiti-preference-memory/09-*-SUMMARY.md .planning/phases/09-optional-s1-graphiti-preference-memory/09-*-VERIFICATION.md 2>/dev/null; echo EVIDENCE-SCAN-DONE; exit "$RC"</automated>
    <fails_when>non-zero exit because the `git log main` capture came back empty (an errored or unknown `main`), or the trailing EVIDENCE-SCAN-DONE line never prints — in either case there is no execution-time evidence and the stretch wording would be invented</fails_when>
    <automated>! grep -nE '(xox[baprs]-[A-Za-z0-9-]|xapp-1-|sk-[A-Za-z0-9]{16}|ya29\.|AIza[A-Za-z0-9_-]{10}|1//[A-Za-z0-9]|://[^ /]+:[^ /@]+@)' README.md</automated>
    <fails_when>non-zero exit, i.e. grep printed a numbered README.md line carrying a token-shaped string or a connection string with embedded credentials</fails_when>
    <automated>! grep -nE '\b(npm|npx|yarn|pnpm)[[:space:]]+[a-z@.-]' README.md</automated>
    <fails_when>non-zero exit, i.e. grep printed a README.md line pairing a non-bun package manager with a subcommand</fails_when>
    <automated>grep -q 'docker compose' README.md && grep -q 'bun run dev' README.md && grep -qE 'bun(x tsx)? lib/slack/bolt\.ts' README.md && grep -q 'prisma db push' README.md && grep -qi 'trigger\.dev' README.md</automated>
    <fails_when>non-zero exit: Usage is missing the compose startup, the Next.js run command, the Bolt run command, the schema push, or the Trigger.dev CLI</fails_when>
    <automated>grep -q 'DATABASE_URL' README.md && grep -q 'SLACK_WATCH_CHANNEL_IDS' README.md && grep -q 'AI_API_KEY' README.md</automated>
    <fails_when>non-zero exit: at least one of the three anchor env var names is absent from the Usage section</fails_when>
    <!-- planner-discipline-allow: knowledge layer -->
    <!-- planner-discipline-allow: preference memory -->
    <!-- planner-discipline-allow: Graphiti -->
    <!-- The three literals above are named in Task 1 and Task 3 actions on purpose: the action must
         tell the executor to WITHHOLD the knowledge-layer claim from the core-functionality section,
         and must name the S1 track by its real name when writing the abandoned-stretch paragraph.
         The gate below is scoped to the core-functionality section, so those names remain legal
         everywhere else in README.md. -->
    <automated>test -n "$(ls .planning/phases/09-optional-s1-graphiti-preference-memory/09-*-SUMMARY.md 2>/dev/null)" || ! sed -n '/^## The core functionality$/,/^## /p' README.md | grep -qiE 'knowledge layer|preference memory|Graphiti'</automated>
    <fails_when>non-zero exit: no Phase 9 SUMMARY exists on disk, yet the core-functionality section still claims a knowledge layer, preference memory or Graphiti</fails_when>
    <automated>grep -qiE 'two (concrete |reasoned )?alternative' README.md || grep -qi 'conflict warning' README.md</automated>
    <fails_when>non-zero exit: neither the two-alternatives wording nor the static conflict-warning wording appears, so the conflict beat is undocumented</fails_when>
  </verify>
  <acceptance_criteria>
    - EVIDENCE-SCAN-DONE prints and the git log section lists commits; the abandoned-stretch wording cites something from that output
    - The secret-shape grep prints nothing; every env var appears as a NAME with no value
    - The non-bun package-manager grep prints nothing
    - Every command in Usage is one that exists in the real `package.json` / `docker-compose.yml` read this task
    - The conditional core-functionality gate exits 0 (scoped to the core-functionality section by design — the knowledge layer may legitimately be named under Abandoned stretch work)
    - The conflict wording present in the file is the one that is live on `main`, not the other one
  </acceptance_criteria>
  <done>Usage is reproducible from the real repo, and every conditional claim in the README matches what shipped (D-15 and the Task 3 conditional rules).</done>
</task>

<task type="auto">
  <name>Task 4: Known shortcuts — live /ponytail-debt output pasted verbatim, plus the broadened scan and the design-level shortcuts</name>
  <precondition>The `ponytail` plugin is available in this session, so `/ponytail-debt` can be invoked (PROJECT.md:79 records it as installed before the window)</precondition>
  <files>README.md</files>
  <read_first>
    - .planning/phases/11-freeze-and-record/11-RESEARCH.md lines 187-193 (Pitfall 2, the empty-ledger trap and the broadened-scan note)
    - .planning/phases/11-freeze-and-record/11-PATTERNS.md lines 78-84 and 90-103 (the verbatim-block shape)
    - .planning/phases/10-seed-and-rehearse/10-impeccable-audit-notes.md, if it exists — its P0-P3 findings marked as deferred feed this section
    - README.md's "Known shortcuts" section as written in Task 1
  </read_first>
  <action>Restructure the "Known shortcuts" section into two clearly separated sub-sections and fill the first from a live run.

1) Invoke `/ponytail-debt` now, in this session, against this repo. Do not reconstruct or recall a previous result.
2) Add the sub-heading `#### /ponytail-debt output (verbatim)` and paste the skill's literal output underneath, inside a fenced block whose info string is `text` so Markdown does not reflow it. Paste it unedited: no trimming, no reordering, no rewrapping, no added commentary inside the fence. D-14 is satisfied by literal fidelity, not by completeness — if the skill reports a clean ledger, that one line is what goes in the fence.
3) Before trusting a clean or empty result, run the broadened scan in the verify block below. The skill's default pattern anchors on hash and double-slash comment prefixes, so it misses TSDoc continuation lines and JSX comments — and this repo mandates TSDoc on every function, so a marker written as a doc-comment continuation would be silently under-reported. The scan below drops the prefix anchor entirely and matches the bare marker text across every tracked non-markdown file, which is deliberately broader than the skill's pattern; markdown is excluded so planning-doc mentions of the marker do not create noise. If that scan surfaces markers the skill missed, add them in a separate, clearly labelled block OUTSIDE the verbatim fence — a short paragraph saying the default pattern under-reports doc-comment markers, then the extra hits. Never merge them into the verbatim fence.
4) Add the sub-heading `#### Other known shortcuts (design decisions, not ponytail markers)` and move the Task 1 prose under it, unchanged in substance. Keep it design-level: what was simplified and why it was acceptable for a one-day one-laptop build. Do not name a host, a port, a path to a credential, or anything that reads as an exploit recipe — these statements stay at the same altitude the planning docs already use in public.
5) If `.planning/phases/10-seed-and-rehearse/10-impeccable-audit-notes.md` exists, fold its deferred findings (the ones Phase 10 recorded as reviewed and not fixed) into that same prose sub-section, attributed to the audit, in one or two sentences. If the file does not exist, add nothing and say nothing about it.</action>
  <verify>
    <automated>FENCE=$(printf '\140\140\140'); grep -qE '^#### /ponytail-debt output \(verbatim\)$' README.md && grep -q "^${FENCE}text" README.md && grep -qE '^#### Other known shortcuts' README.md</automated>
    <fails_when>non-zero exit: the verbatim sub-heading is missing, no fenced block with the text info string exists, or the design-decisions sub-heading is missing</fails_when>
    <automated>git grep -n 'ponytail:' -- ':!*.md'; S=$?; echo "BROAD-SCAN-DONE exit=$S"</automated>
    <fails_when>the trailing BROAD-SCAN-DONE line never prints, or it reports exit=128 (git grep itself errored) — in either case the broadened scan did not run and a clean ledger would be trusted unverified. exit=1 means no markers matched in any non-markdown tracked file, which is a valid clean result</fails_when>
    <automated>grep -qi 'refresh token' README.md && grep -q 'db push' README.md && grep -qi 'one Bolt process' README.md && grep -qi 'Biome' README.md</automated>
    <fails_when>non-zero exit: the design-level shortcuts list is missing the plaintext refresh token, the db-push-no-migrations note, the single-Bolt-process rule, or the Biome-only rule</fails_when>
    <automated>OUT=$(git diff --name-only HEAD); RC=$?; test "$RC" -eq 0 && test "$OUT" = "README.md"</automated>
    <fails_when>non-zero exit: the working tree has a modification outside README.md, or README.md is not modified</fails_when>
  </verify>
  <acceptance_criteria>
    - Both `####` sub-headings exist and a fenced block with the `text` info string sits under the first
    - The fenced block contains exactly what `/ponytail-debt` printed in this session — nothing added, nothing removed
    - BROAD-SCAN-DONE prints; any markers it found that the skill missed appear outside the fence in a labelled block
    - The design-level shortcut prose covers all four anchor shortcuts and names no host, port or credential path
    - `git diff --name-only HEAD` still lists only `README.md`
  </acceptance_criteria>
  <done>The known-shortcuts section carries a live, verbatim ledger plus real design-level content, so it reads as engineering judgement rather than an unfilled heading (D-14, ROADMAP SC3).</done>
</task>

<task type="auto">
  <name>Task 5: Final gate pass, human read-through, and one scoped commit on main</name>
  <files>README.md</files>
  <read_first>
    - README.md in full, top to bottom
    - .planning/phases/11-freeze-and-record/11-RESEARCH.md lines 206-209 (Pitfall 5, staging discipline)
    - .planning/ROADMAP.md §"Phase 11: Freeze-and-Record" Success Criteria 2, 3 and 4
  </read_first>
  <action>Run the full gate set below, then commit.

1) Run every automated check in this task and fix any failure by editing README.md — never by weakening a check and never by deleting a section (D-16: under time pressure sections get shorter, not fewer).
2) One known exception, and only this one: if the verbatim ledger block from Task 4 itself contains LangGraph's pause-primitive token, the count gate will read 2. Leave the fence untouched — D-14 outranks the count — and record the resulting count and the reason in 11-02-SUMMARY.md.
3) Hand the file to the human for one read-through against ROADMAP Success Criteria 2, 3 and 4 while 11-01's recording work continues. This is a read, not a rewrite.
4) Stage by explicit path only: `git add README.md`. Never a whole-tree stage — 11-01's recording lives outside the repo, but any other untracked file in the tree would be swept in (11-RESEARCH.md Pitfall 5). Before committing, confirm by hand that `git diff --cached --name-only HEAD` prints exactly the one line `README.md`; if it prints anything else, unstage and investigate rather than committing. This check lives here rather than in the verify block because the verify commands run after the commit, when the staged set is empty by construction — the post-commit path gate below is what proves the property.
5) Commit directly on `main` with no new branch and no merge (D-02). Message: `docs(11): finalize README (DMO-04)`, ending with the attribution trailer lines given verbatim in the executing session's own system-reminder (the `Co-Authored-By:` line naming whichever model is actually executing, plus any session line it specifies). Do not copy a model name from this plan — the executing session's reminder is the only source.
6) Confirm after the fact that the commit carries exactly one path.</action>
  <reversibility rating="reversible">A README commit on `main` is revertible with a single `git revert`; no schema, no interface, no external state changes.</reversibility>
  <verify>
    <automated>for h in '^## What it is$' '^## The core functionality$' '^## How it differs$' '^## The problems it tackles$' '^## Architecture$' '^## Usage$' '^## Scope and non-goals$' '^## Production design notes$' '^### Batch-first detection$' '^### Multi-workspace via OAuth$' '^### Where a graph-level pause would fit$' '^### Known shortcuts$' '^#### /ponytail-debt output \(verbatim\)$' '^#### Other known shortcuts' '^### Abandoned stretch work$'; do grep -qE "$h" README.md || { echo "MISSING: $h"; exit 1; }; done; echo FINAL-HEADINGS-OK</automated>
    <fails_when>non-zero exit with a "MISSING: " line naming the absent heading, or the trailing FINAL-HEADINGS-OK line never prints</fails_when>
    <automated>! grep -niE 'TODO|TBD|FIXME|lorem|placeholder|XXX|coming soon' README.md</automated>
    <fails_when>non-zero exit, i.e. grep printed a numbered README.md line still carrying an unfilled marker</fails_when>
    <automated>test "$(grep -cF 'interrupt()' README.md)" -eq 1</automated>
    <fails_when>non-zero exit: the count is 0 (the D-13 line is missing) or 2 or more (the token leaked into a second line, most often the scope-and-non-goals table)</fails_when>
    <automated>test "$(git branch --show-current)" = main</automated>
    <fails_when>non-zero exit, i.e. HEAD is on a branch other than main, or is detached and the command prints nothing</fails_when>
    <automated>OUT=$(git show --name-only --format='' HEAD); RC=$?; test "$RC" -eq 0 && test "$OUT" = "README.md"</automated>
    <fails_when>non-zero exit: `git show` itself errored, HEAD touches more than one path (the variable carries a multi-line value), or the single path it touches is not README.md</fails_when>
    <human-check>Read once, top to bottom: every heading has real content; Clockwise and Relay.app both read as defunct or winding down, never as live competitors; the known-shortcuts fence looks like raw tool output; the abandoned-stretch paragraph matches what actually happened today; no secret value anywhere.</human-check>
  </verify>
  <acceptance_criteria>
    - FINAL-HEADINGS-OK prints; all fifteen headings including both `####` sub-headings are present
    - The unfilled-marker grep prints nothing (ROADMAP SC4)
    - The pause-primitive count is exactly 1, or the Task 5 step 2 exception is recorded in the SUMMARY
    - `git branch --show-current` prints main
    - The pre-commit hand check showed `git diff --cached --name-only HEAD` printing exactly one line, `README.md`
    - `git show --name-only --format='' HEAD` returns exactly `README.md` — the commit carried one path
    - The human confirms the read-through against ROADMAP Success Criteria 2, 3 and 4
  </acceptance_criteria>
  <done>README.md is final and committed in one scoped commit on `main`; DMO-04 and ROADMAP Success Criteria 2, 3 and 4 are met (D-01, D-02, D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo README to the public | `README.md` is committed and read by judges and anyone who opens the repo; everything in it is published and permanent |
| working tree to the commit | An unscoped stage would sweep untracked files — including any stray copy of 11-01's recording — into a public commit |
| local tool output to the published doc | `/ponytail-debt` output is pasted verbatim and unreviewed-by-edit into a public file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-06 | Information disclosure | README Usage section | high | mitigate | Env var NAMES only, sourced from the `.env*.example` file, never a value; Task 3's secret-shape grep rejects bot/app/API/refresh-token shapes and any connection string with embedded credentials; PROJECT.md's local `DATABASE_URL` example is referenced by name, never pasted |
| T-11-07 | Information disclosure | git staging area | medium | mitigate | `git add README.md` by explicit path, never a whole-tree stage (11-RESEARCH.md Pitfall 5); Task 5 gates the staged set with `git diff --cached --name-only` and the commit with `git show --name-only --format=''` |
| T-11-08 | Information disclosure | Known shortcuts section | medium | mitigate | Shortcuts stated at design level only — what was simplified and why — with no host, port, credential path or exploit recipe beyond what the planning docs already state publicly (Task 4 step 4) |
| T-11-09 | Tampering | Verbatim `/ponytail-debt` fence | low | accept | D-14 mandates literal output and prohibition P6 forbids editing inside the fence, so the content is whatever the local tool emitted. Accepted: the tool runs locally against this repo only, and the human read-through in Task 5 sees the fence before the commit lands |
| T-11-10 | Spoofing | Competitive claims about third parties | low | mitigate | Dates and statuses come from 11-RESEARCH.md's web-verified table (2026-09-11, multiple independent sources plus Relay.app's own notice); Task 2's Reclaim-attribution grep blocks the one claim research found unsupported |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase — 11-RESEARCH.md's Package Legitimacy Audit is skipped for exactly that reason, and the bun-only grep in Task 3 would flag any install command that appeared in the README |
</threat_model>

<assumption_delta_decision>
noun: none (no data or identity model is touched)
decision: no-change
rationale: false positive on the prose terms "fallback" (the recorded wifi fallback the README mentions in one line) and "alongside" (the phase's "Runs alongside: Nothing" row). This plan edits only README.md and adds no code, no schema and no identity surface.
</assumption_delta_decision>

## Flagged assumptions

- **Edge probe DMO-04 (unclassified, unresolved):** what "actual content, not a placeholder" means per heading. Assumed: at least one complete, project-specific sentence, enforced by the awk empty-section gate in Task 1 plus the unfilled-marker grep and the Task 5 human read-through. No stricter automated definition exists.
- **Prohibitions P1 to P6 (unresolved, flagged-unverified):** P1 (no Reclaim attribution), P2 (no live-competitor framing — partly), P3 (no secret values) and P4 (scoped staging) are automated. P5 (no invented knowledge layer or fictional abandonment) is automated only for the core-functionality section; the abandoned-stretch half rests on the executor honouring the Task 3 evidence scan. P6 (nothing edited inside the verbatim fence) is unverifiable after the fact by construction and rests on executor discipline plus the human read-through.
- **`README.md` is a Phase-1 artifact that does not exist in today's tree.** Task 1 assumes it exists at execution time as the FND-02 skeleton (Phase 1 D-20). If Phase 1 cut it to headings only under ROADMAP cut-line row 1, or it is missing entirely, Task 1 writes it outright — the output is identical either way.
- **Phase-1 Usage artifacts read at execution time:** the exact env example filename is whatever Phase 1 produced. PROJECT.md:129 names `.env.local.example` and `.env.cloud.example`; Task 3's precondition globs `.env*.example` rather than pinning one name.
- **11-RESEARCH.md competitive dates (MEDIUM-HIGH, verified 2026-09-11)** are not re-verified at execution time: D-10 locks the wording and the window has no room for a second verification pass.
- **`/ponytail-debt` may return a clean ledger.** 11-RESEARCH.md Pitfall 2 applies: the verbatim fence stays whatever the tool printed, and the separate design-level prose carries the section's substance.
- **Region-scoped gate:** the conditional knowledge-layer check in Task 3 is scoped to the core-functionality section with `sed`, because the same phrases may legitimately appear under "Abandoned stretch work". This is deliberate, not an oversight.
- **Runtime:** WSL Ubuntu with POSIX `grep`, `sed`, `awk` and `git`, all commands run from the repo root.

## Artifacts this phase produces

README section headings this plan creates or fills in `README.md` at the repo root:

- `# AI Secretary` (title and lede)
- `## What it is`
- `## The core functionality`
- `## How it differs`
- `## The problems it tackles`
- `## Architecture`
- `## Usage`
- `## Scope and non-goals`
- `## Production design notes`
- `### Batch-first detection`
- `### Multi-workspace via OAuth`
- `### Where a graph-level pause would fit`
- `### Known shortcuts`
- `#### /ponytail-debt output (verbatim)`
- `#### Other known shortcuts (design decisions, not ponytail markers)`
- `### Abandoned stretch work`

Also produced: `.planning/phases/11-freeze-and-record/COVERAGE.md` (written at planning time only if the API-coverage detector fired). No code symbols — this phase adds no code (D-01).

<verification>
- Every required heading is present and filled; the unfilled-marker grep over README.md prints nothing (ROADMAP SC4, D-09)
- Clockwise is cited as shut down 27 March 2026 and Relay.app as winding down with paid access ending 14 September 2026, neither as a live competitor, with no Reclaim attribution (ROADMAP SC2, D-10)
- The live `/ponytail-debt` output appears verbatim inside a fenced text block (ROADMAP SC3, D-14)
- LangGraph's pause-primitive token appears on exactly one line (D-13)
- One commit on `main` whose `git show --name-only --format='' HEAD` lists only `README.md` (D-01, D-02)
</verification>

<success_criteria>
DMO-04 is met: the final README covers every required section with real content — the corrected competitive comparison, batch-first detection with the ~20x cost reasoning and why regex pre-filtering fails, multi-workspace via OAuth, the one-line graph-pause note, the verbatim `/ponytail-debt` known-shortcuts block, and the abandoned stretch work with the reason — committed in one scoped commit directly on `main`.
</success_criteria>

<output>
Create `.planning/phases/11-freeze-and-record/11-02-SUMMARY.md` when done. Record: which conflict flow the README describes (full CFL alternatives or CFL-05), whether the per-person knowledge layer claim was included and the evidence for that call, the exact abandoned-stretch outcome written and which criterion fired (or that neither track started), whether `/ponytail-debt` returned a clean ledger, whether the broadened scan found anything the skill missed, the pause-primitive line count (and the Task 5 step 2 exception if it read 2), and the commit sha with the one path it touched.
</output>

