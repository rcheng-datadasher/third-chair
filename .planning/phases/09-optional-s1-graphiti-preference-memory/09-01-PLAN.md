---
phase: 09-optional-s1-graphiti-preference-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "${GRAPH_SERVICE_DIR:-../graph-service}/requirements.txt"
  - "${GRAPH_SERVICE_DIR:-../graph-service}/Dockerfile"
  - "${GRAPH_SERVICE_DIR:-../graph-service}/probe.py"
  - "${GRAPH_SERVICE_DIR:-../graph-service}/main.py"
  - "${GRAPH_SERVICE_DIR:-../graph-service}/.gitignore"
  - "${GRAPH_SERVICE_DIR:-../graph-service}/.env.example"
  - docker-compose.yml
autonomous: true
requirements:
  - STR-01
  - STR-02

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01 start gate held: work began only with Phase 7's full dry run passed on main, the HKT clock at or before 13:15, and Phase 8 skipped"
    - "D-11/D-09/D-07: Graphiti built with Kilo-pointed LLM, embedder and cross-encoder clients (every value from env) writes one JSON episode to local Neo4j without an exception, and Episodic nodes appear"
    - "D-13/D-06 (STR-01, STR-02): POST /episodes with one closed-vocabulary fact, then GET /preferences?user_id=, returns exactly that fact"
    - "D-12: a key outside working_hours, default_meeting_duration, buffer_between_meetings, no_meeting_days, preferred_slot_of_day gets HTTP 422 before Graphiti is called"
    - "D-06: the service has exactly two routes, and the compose healthcheck probes GET /preferences?user_id=healthcheck"
    - "D-08: one Neo4j driver per process, created in the FastAPI lifespan and closed on shutdown, TLS from the URI scheme only, explicit pool size and acquisition timeout (or a ponytail comment recording driver defaults as a known shortcut)"
    - "D-05/D-18/D-19: all Python lives in the sibling repo at ${GRAPH_SERVICE_DIR:-../graph-service}; this repo's only change is additive keys in the docker-compose.yml graph-service block, and compose still interpolates with no graph vars set"
    - "D-03/D-02/D-04: the round trip passed within 30 minutes of Task 1's recorded start on branch gsd/phase-9-s1-graphiti, or work stopped and the SUMMARY records the abandonment for the Phase 11 README"
    - "D-10/D-20/D-21: Python functions carry summary/Args/Returns/Raises docstrings; no test files exist; this is plan 1 of 2"
  artifacts:
    - path: "${GRAPH_SERVICE_DIR:-../graph-service}/main.py"
      provides: "FastAPI app: lifespan (one Graphiti + one Neo4j driver), POST /episodes, GET /preferences"
      contains: '@app.get("/preferences")'
    - path: "${GRAPH_SERVICE_DIR:-../graph-service}/probe.py"
      provides: "Abandon-clock probe: one Kilo-pointed Graphiti JSON episode, run before any route exists"
      contains: "OpenAIRerankerClient"
    - path: "${GRAPH_SERVICE_DIR:-../graph-service}/requirements.txt"
      provides: "Exactly two pinned Python requirements"
      contains: "graphiti-core==0.30.2"
    - path: "${GRAPH_SERVICE_DIR:-../graph-service}/Dockerfile"
      provides: "python:3.12-slim image running uvicorn main:app on port 8000"
      contains: "python:3.12-slim"
    - path: "${GRAPH_SERVICE_DIR:-../graph-service}/.env.example"
      provides: "Documented env keys with empty secret slots"
      contains: "GRAPHITI_EMBEDDING_MODEL"
    - path: "${GRAPH_SERVICE_DIR:-../graph-service}/.gitignore"
      provides: "Keeps .env out of the sibling repo"
      contains: ".env"
    - path: "docker-compose.yml"
      provides: "graph-service env passthrough, loopback port 8000, GET /preferences healthcheck"
      contains: "AI_API_KEY"
  key_links:
    - from: "docker-compose.yml"
      to: "${GRAPH_SERVICE_DIR:-../graph-service}/Dockerfile"
      via: "graph-service build.context interpolated from GRAPH_SERVICE_DIR (Phase 1 D-17)"
      pattern: 'GRAPH_SERVICE_DIR:-\.\./graph-service'
    - from: "docker-compose.yml"
      to: "${GRAPH_SERVICE_DIR:-../graph-service}/main.py"
      via: "healthcheck opens GET /preferences?user_id=healthcheck with python urllib"
      pattern: 'preferences\?user_id=healthcheck'
    - from: "${GRAPH_SERVICE_DIR:-../graph-service}/main.py"
      to: "Neo4j Episodic nodes"
      via: "graphiti.driver.execute_query with a params dict, never string interpolation"
      pattern: 'execute_query\(READ_PREFERENCES, params='
    - from: "${GRAPH_SERVICE_DIR:-../graph-service}/main.py"
      to: "Graphiti.add_episode"
      via: "POST /episodes awaits a JSON episode with group_id = user_id"
      pattern: 'EpisodeType\.json'
---

# Phase 9, Plan 01: Graphiti preference service round trip (sibling repo) [throwaway]

## Phase Goal

ROADMAP `**Goal:**` (verbatim): A self-hosted Graphiti/Neo4j service demonstrably changes a proposal after learning a preference.

MVP note: the ROADMAP Goal line is not in "As a / I want to / so that" form, and no story is invented here. `/gsd mvp-phase 9` would convert it. The plan shape does not depend on it: 09-01 is the tracer, 09-02 is the before/after slice (D-21).

<objective>
Stand up the S1 Graphiti service in a SEPARATE Python repo at `${GRAPH_SERVICE_DIR:-../graph-service}` (D-05, never inside this repo). It exposes exactly `POST /episodes` and `GET /preferences?user_id=` against local Neo4j 5 community behind the compose `graph` profile (D-06, D-07). The observable result is a literal round trip: POST one distilled closed-vocabulary JSON fact, GET returns that fact (D-11, D-12, D-13; STR-01, STR-02).

**Start gate (D-01), enforced as Task 1's precondition, not as a task.** Start only if all of these hold:
- Phase 7's full dry run has passed on `main`.
- The HKT clock reads 13:15 or earlier.
- Phase 8 is skipped in favour of this phase.

Together these mean the build is at least 60 minutes ahead. ROADMAP "Honest arithmetic" says that normally does not happen, so on schedule this phase is README-only: Neo4j and graph-service stay declared in compose but are not exercised.

**Budget and abandon rule (D-02, D-03).**
- Budget is 60 to 90 minutes. 20 to 30 of those are the first Graphiti round trip.
- The abandon clock starts when Task 1 starts; Task 1 records the HKT start time.
- If Task 2's round trip is not green by start + 30 minutes, stop. Do not run Task 3 or plan 09-02, and write the SUMMARY as abandoned with the exact failure signal.
- Abandoned work is noted for the Phase 11 README, never finished late (D-04).

**Branch and label (D-04).** All work in this repo happens on branch `gsd/phase-9-s1-graphiti`, labelled [throwaway]. Nothing merges to `main` unless 09-02 is demo-ready by 14:40. The sibling repo is its own throwaway git repo.

**Task shape (tracer-first, MVP).**
- Task 1 is the orchestrator-mandated abandon-clock probe (RESEARCH Abandon Clock risk #1). It proves Kilo Gateway + Graphiti + Neo4j with no FastAPI in the way.
- Task 2 is the D-21 tracer: the compose → FastAPI → Graphiti → Neo4j → Cypher read-back round trip.
- Task 3 hardens the driver (D-08) and documents env.

**Reversibility:** every decision in this plan is reversible. It is a throwaway branch, a throwaway sibling repo, and additive compose keys. No `checkpoint:decision` is inserted.

**No tests of any kind (D-20).** Every check below is a hand check under a minute.

Output:
- sibling-repo files `requirements.txt`, `Dockerfile`, `probe.py`, `main.py`, `.gitignore`, `.env.example`, committed in THAT repo;
- additive `graph-service` keys in this repo's `docker-compose.yml`.
</objective>

## Artifacts this phase produces

Full list for both plans. This plan produces the rows marked 09-01.

**Files**

| Path | Repo | Plan / task | Kind |
|---|---|---|---|
| `${GRAPH_SERVICE_DIR:-../graph-service}/probe.py` | sibling repo, committed there | 09-01 T1 | new |
| `${GRAPH_SERVICE_DIR:-../graph-service}/requirements.txt` | sibling repo, committed there | 09-01 T1 | new |
| `${GRAPH_SERVICE_DIR:-../graph-service}/Dockerfile` | sibling repo, committed there | 09-01 T1 | new |
| `${GRAPH_SERVICE_DIR:-../graph-service}/main.py` | sibling repo, committed there | 09-01 T2, T3 | new |
| `${GRAPH_SERVICE_DIR:-../graph-service}/.gitignore` | sibling repo, committed there | 09-01 T2 | new |
| `${GRAPH_SERVICE_DIR:-../graph-service}/.env.example` | sibling repo, committed there | 09-01 T3 | new |
| `docker-compose.yml` (`graph-service` block only) | this repo | 09-01 T1, T2 | additive edit |
| `lib/agent/graph-preferences.ts` | this repo | 09-02 T1 | new |
| duration-default call site, expected `lib/agent/graph.ts` (confirmed by grep) | this repo | 09-02 T1 | one additive edit |
| `graph/queries.cypher` | this repo | 09-02 T1 | new |
| known-preferences call site, expected `lib/agent/nodes/propose.ts` (confirmed by grep) | this repo | 09-02 T3 (time-gated stretch, may be skipped) | additive edit |

**Python symbols (`main.py`, one file by design; RESEARCH Q8's three-file layout collapses into it)**
- `PreferenceKey`: `Literal` alias of exactly the five D-12 keys.
- `PreferenceEpisode`: Pydantic request model with fields `user_id`, `key`, `value`.
- `READ_PREFERENCES`: module constant holding the parameterized Cypher read-back.
- `lifespan`: async context manager. It builds the three Kilo Gateway clients, one `Neo4jDriver` with a tuned client, and one `Graphiti`; calls `build_indices_and_constraints`; and `close`s on shutdown.
- `app`: the `FastAPI(lifespan=lifespan)` instance.
- `post_episode` → `POST /episodes`.
- `get_preferences` → `GET /preferences?user_id=`.
- `probe.py`: one `async def main()` run by `asyncio.run`.

**TypeScript symbol (09-02)**
- `fetchGraphPreferences(userId: string): Promise<Record<string, unknown> | null>` exported from `lib/agent/graph-preferences.ts`, plus a module-private `GraphPreferencesSchema`.

**Env vars**

The `graph-service` container receives these via compose passthrough. Values live in this repo's untracked root `.env`.
- `NEO4J_URI` (existing, `bolt://neo4j:7687`)
- `NEO4J_USER`
- `NEO4J_PASSWORD`
- `AI_API_KEY`
- `AI_BASE_URL` (new)
- `GRAPHITI_LLM_MODEL` (new)
- `GRAPHITI_EMBEDDING_MODEL` (new)
- `GRAPHITI_TELEMETRY_ENABLED` (new, fixed `"false"`)

On the app side, `GRAPH_SERVICE_URL` is an existing Phase 1 key. Its value is `http://localhost:8000`, set in the untracked `.env` for 09-02.

**Compose additions (`graph-service` block only)**
- environment passthrough of the vars above, each with a `${VAR:-default}` form;
- `ports: "127.0.0.1:8000:8000"`;
- a healthcheck running python urllib against `GET /preferences?user_id=healthcheck` (interval 10s, timeout 5s, retries 5, start_period 30s).

Conditional `neo4j`-block fallbacks, applied only if the named failure actually happens:
- `NEO4J_PLUGINS: '["apoc"]'`
- swapping the healthcheck test

**Explicitly not produced**
- no third route;
- no Postgres `Preference` write-back, no `prisma/schema.prisma` change, no npm dependency;
- no test files;
- no Python file in this repo.

## Source Coverage Audit (plan set 09-01 + 09-02)

| Source | Item | Covered by |
|---|---|---|
| GOAL | Graphiti/Neo4j service demonstrably changes a proposal after learning a preference | 09-01 T2 (service), 09-02 T1 + T2 (the change) |
| REQ | STR-01 two-endpoint FastAPI service, one driver per process, TLS via URI scheme | 09-01 T2, T3 |
| REQ | STR-02 closed-vocabulary JSON facts ingested and read back | 09-01 T1, T2 |
| REQ | STR-03 same input, different better proposal; graph before/after via `graph/queries.cypher` | 09-02 T1, T2 |
| RESEARCH | Abandon risk 1: structured output on the chosen Kilo Gateway model | 09-01 T1 probe + env-only model swap |
| RESEARCH | Abandon risk 2: embedder silently no-ops (Episodic but no Entity) | 09-01 T1 fix list, T3 Browser label check |
| RESEARCH | Abandon risk 3: cold Docker start eats the clock | 09-01 T1 step 1 (start pulls first) |
| RESEARCH | Abandon risk 4: APOC | 09-01 T1 fallback |
| RESEARCH | Q3 deterministic read-back on `Episodic.content` | 09-01 T2 |
| RESEARCH | Q6 await `add_episode` inline | 09-01 T2 |
| RESEARCH | Q7 duration lever primary, known-preferences lever stretch | 09-02 T1, T3 |
| RESEARCH | Q8 python:3.12-slim, pip, telemetry off | 09-01 T1 |
| RESEARCH | Q9 `graph/queries.cypher` | 09-02 T1 |
| RESEARCH | Correction 1: no `Neo4jDriver(driver=)`; synchronous client swap; keep explicit index build | 09-01 T2, T3 |
| RESEARCH | Correction 2: pass the cross-encoder explicitly | 09-01 T1, T2 |
| RESEARCH | Correction 3: `execute_query(QUERY, params=...)` | 09-01 T2 |
| RESEARCH | Correction 4: exactly two routes; healthcheck and TS gate on `GET /preferences` | 09-01 T2, 09-02 T1 |
| RESEARCH | Correction 5: no Postgres write-back, stated explicitly | 09-02 objective + must_haves |
| RESEARCH | Security domain: Cypher injection, `.env` leakage, unbounded fetch, unauthenticated :8000 | 09-01 T-09-01, T-09-02, T-09-04; 09-02 T-09-07 |
| CONTEXT | D-01..D-04 gate, budget, abandon, branch | 09-01 objective + T1 precondition; 09-02 objective, T2, T3 |
| CONTEXT | D-05..D-13 service shape and preference facts | 09-01 T1..T3 |
| CONTEXT | D-14..D-16 before/after, queries, gated additive seam | 09-02 T1..T3 |
| CONTEXT | D-17 Postgres `Preference` rows | 09-02 objective: deliberate, documented simplification (direct GET at proposal time) |
| CONTEXT | D-18..D-21 ownership, must-not-touch, no tests, two plans | both plans |

Excluded as Deferred Ideas: STR-08 `GET /graph` + in-dashboard render, STR-06 relationship brief, SCL-01 batch-sweep distillation, Aura / FalkorDB Cloud, Trigger.dev-specific transport.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-optional-s1-graphiti-preference-memory/09-CONTEXT.md
@.planning/phases/09-optional-s1-graphiti-preference-memory/09-RESEARCH.md
@.planning/phases/01-foundation-hardcoded-round-trip/01-RESEARCH.md
@docker-compose.yml

Read `09-RESEARCH.md` in this order: "Orchestrator Review Corrections" first (it overrides Q5 and the Q8 extra-route snippet), then §Abandon Clock, §Q1, §Q2, §Q3, §Code Examples. In `01-RESEARCH.md`, only §"docker-compose.yml skeleton" matters here. No application code from this repo is needed for 09-01.

<interfaces>
Verified against getzep/graphiti main source (RESEARCH Q1 + Orchestrator correction 1). Use these, not memory:

```python
class Graphiti:
    def __init__(self, uri=None, user=None, password=None, llm_client=None, embedder=None,
                 cross_encoder=None, store_raw_episode_content=True, graph_driver=None, ...): ...
    async def add_episode(self, name: str, episode_body: str, source_description: str,
                          reference_time: datetime, source: EpisodeType = EpisodeType.message,
                          group_id: str | None = None, ...) -> AddEpisodeResults: ...
    async def build_indices_and_constraints(self, delete_existing: bool = False): ...
    async def close(self): ...
    driver: GraphDriver   # Neo4jDriver; .execute_query(cypher, params={...}) -> EagerResult (.records)

class Neo4jDriver:          # graphiti_core.driver.neo4j_driver
    def __init__(self, uri, user, password, database='neo4j'): ...   # NO driver= kwarg
    client: neo4j.AsyncDriver  # built internally with no pool kwargs; swappable attribute

# Defaults when a client is omitted: OpenAIClient(), OpenAIEmbedder(), OpenAIRerankerClient(),
# all aimed at api.openai.com with OPENAI_API_KEY. All three must be passed explicitly.
# LLMConfig(api_key, model, small_model, base_url)          graphiti_core.llm_client.config
# OpenAIGenericClient(config=LLMConfig)                     graphiti_core.llm_client.openai_generic_client
# OpenAIEmbedder(config=OpenAIEmbedderConfig(api_key, embedding_model, base_url))  graphiti_core.embedder.openai
# OpenAIRerankerClient(config=LLMConfig)                    graphiti_core.cross_encoder.openai_reranker_client
# EpisodeType.json                                          graphiti_core.nodes
```

Phase 1 `docker-compose.yml` skeleton, the existing shape to extend (additive only):
- `neo4j`: `neo4j:5-community`, `profiles: ["graph"]`, `NEO4J_AUTH: ${NEO4J_USER:-neo4j}/${NEO4J_PASSWORD:-changeme}`, heap 512m, pagecache 256m, ports 7474/7687, wget healthcheck.
- `graph-service`: `profiles: ["graph"]`, `build.context: ${GRAPH_SERVICE_DIR:-../graph-service}`, `depends_on: neo4j: condition: service_healthy`, `environment: NEO4J_URI: bolt://neo4j:7687`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Abandon-clock probe: one Kilo-pointed Graphiti JSON episode into local Neo4j, before any route exists</name>
  <precondition>D-01 start gate: `TZ=Asia/Hong_Kong date +%H:%M` prints 13:15 or earlier; a Phase 7 SUMMARY under .planning/phases/07-integrate-conflict-counter-proposal/ records the full dry run passed on main; `git branch --list 'gsd/phase-8-*'` prints nothing (Phase 8 skipped); this repo's root .env has a non-empty AI_API_KEY.</precondition>
  <files>${GRAPH_SERVICE_DIR:-../graph-service}/requirements.txt (new, sibling repo), ${GRAPH_SERVICE_DIR:-../graph-service}/Dockerfile (new, sibling repo), ${GRAPH_SERVICE_DIR:-../graph-service}/probe.py (new, sibling repo), docker-compose.yml (this repo, graph-service block, additive)</files>
  <read_first>
    - docker-compose.yml (the Phase 1 graph-service and neo4j blocks being extended)
    - .planning/phases/09-optional-s1-graphiti-preference-memory/09-RESEARCH.md §"Orchestrator Review Corrections" items 1 and 2, §"Abandon Clock: Top Risks", §Q1, §Q2, §Q8
    - .planning/phases/09-optional-s1-graphiti-preference-memory/09-CONTEXT.md D-01..D-05, D-07, D-09, D-11
    - .planning/phases/01-foundation-hardcoded-round-trip/01-CONTEXT.md D-16 (one untracked root .env) and D-17 (compose interpolates the whole file)
  </read_first>
  <action>
The 30-minute abandon clock starts now (D-03). Record the output of `TZ=Asia/Hong_Kong date +%H:%M` as the clock start for the SUMMARY. Task 2's round trip must be green by start + 30 minutes.

1. First action, before writing anything (RESEARCH Abandon Clock risk #3). From this repo's root, start `docker compose --profile graph up -d neo4j`. In a second shell, run `docker pull python:3.12-slim`. Let both run while you write the files below.

2. Create the sibling repo directory at `${GRAPH_SERVICE_DIR:-../graph-service}`, resolved relative to this repo's root and never inside this repo (D-05), and `git init` it. Every Python file in this phase is committed in THAT repo only.

3. `requirements.txt` has exactly two lines: `fastapi[standard]==0.141.1` and `graphiti-core==0.30.2`.
   - FastAPI's own `standard` extra supplies uvicorn. So no top-level package outside RESEARCH's Package Legitimacy Audit is installed (see T-09-SC).
   - The neo4j driver and the openai SDK arrive transitively through graphiti-core. Do not hand-pin them.

4. `Dockerfile`, in this order:
   - `FROM python:3.12-slim`
   - `WORKDIR /app`
   - copy `requirements.txt`
   - `pip install --no-cache-dir -r requirements.txt`
   - copy the rest of the source last, so a code edit rebuilds only the final layer
   - exec-form CMD running `uvicorn main:app --host 0.0.0.0 --port 8000`

   As soon as these two files exist, start `docker compose --profile graph build graph-service` in the background.

5. `docker-compose.yml`, `graph-service` block only.
   - Never touch `postgres`. Touch `neo4j` only for the named fallbacks below.
   - Keep the existing `profiles`, `build.context`, `depends_on` and `NEO4J_URI: bolt://neo4j:7687`.
   - Add these environment entries:
     - `NEO4J_USER: ${NEO4J_USER:-neo4j}`
     - `NEO4J_PASSWORD: ${NEO4J_PASSWORD:-changeme}` (these two defaults match the neo4j service's NEO4J_AUTH, so unset values still authenticate)
     - `AI_API_KEY: ${AI_API_KEY:-}`
     - `AI_BASE_URL: ${AI_BASE_URL:-https://api.kilo.ai/api/gateway}`
     - `GRAPHITI_LLM_MODEL: ${GRAPHITI_LLM_MODEL:-}`
     - `GRAPHITI_EMBEDDING_MODEL: ${GRAPHITI_EMBEDDING_MODEL:-}`
     - `GRAPHITI_TELEMETRY_ENABLED: "false"` (RESEARCH A3: no telemetry egress on venue wifi)
   - Every value has a default form, so an unset var can never break `docker compose up postgres` (D-09, Phase 1 D-17).
   - Values come from this repo's untracked root `.env`, which Compose reads for interpolation. Add two keys to that `.env` (untracked, never committed):
     - `GRAPHITI_LLM_MODEL`: reuse the pre-window-verified MODEL_FAST id (RESEARCH Pitfall 2).
     - `GRAPHITI_EMBEDDING_MODEL`: `openai/text-embedding-3-small`.

6. `probe.py`: imports plus about five statements inside one `async def main()`, run by `asyncio.run(main())`.
   - Build one `LLMConfig(api_key=AI_API_KEY, model=GRAPHITI_LLM_MODEL, small_model=GRAPHITI_LLM_MODEL, base_url=AI_BASE_URL)`.
   - Construct `Graphiti` with uri/user/password from NEO4J_URI, NEO4J_USER and NEO4J_PASSWORD, and pass all three clients explicitly:
     - `llm_client=OpenAIGenericClient(config=that LLMConfig)`. Use the generic client, not OpenAIClient, which ignores custom base URLs (RESEARCH Q2).
     - `embedder=OpenAIEmbedder(config=OpenAIEmbedderConfig(api_key=AI_API_KEY, embedding_model=GRAPHITI_EMBEDDING_MODEL, base_url=AI_BASE_URL))`.
     - `cross_encoder=OpenAIRerankerClient(config=that LLMConfig)`. The default reranker needs an OpenAI key and fails at construction (RESEARCH correction 2).
   - `await graphiti.build_indices_and_constraints()`.
   - Make one call: `await graphiti.add_episode(name="probe", episode_body=json.dumps({"default_meeting_duration": 45}), source=EpisodeType.json, source_description="abandon-clock probe", reference_time=datetime.now(timezone.utc), group_id="probe")`. This is a D-11 JSON episode in a throwaway group.
   - `result = await graphiti.driver.execute_query("MATCH (n) RETURN labels(n) AS labels, count(*) AS n")`, then print each record in `result.records`.
   - `await graphiti.close()`.
   - Read every value with `os.environ[...]`: fail loud on a missing key, and put no defaults or hostnames in code (D-09).
   - Import paths are listed in the plan's interfaces block. If one errors, inspect the installed package with `pip show -f graphiti-core` inside the container instead of guessing.

7. Run the probe with the second verify command. Its `--build` flag re-copies probe.py into the image, and the pip layer stays cached. The episode takes seconds, not milliseconds.

Env-only or tiny fixes, tried in this order, each for at most a couple of minutes:
- Kilo Gateway returns 400/422 naming response_format or unsupported parameters: set GRAPHITI_LLM_MODEL in the root `.env` to a model listed at https://api.kilo.ai/api/gateway/models, then rerun.
- A 401 names api.openai.com: a client was not passed explicitly. Recheck all three.
- The neo4j container never turns healthy because wget is missing (RESEARCH A5): replace only the neo4j healthcheck test with `cypher-shell -u ${NEO4J_USER:-neo4j} -p ${NEO4J_PASSWORD:-changeme} 'RETURN 1'`.
- `build_indices_and_constraints` raises a procedure-not-found error (RESEARCH A1): add `NEO4J_PLUGINS: '["apoc"]'` to the neo4j environment, then run `docker compose --profile graph up -d --force-recreate neo4j`.
- Output shows `Episodic` but no `Entity`: the read-back will still work because it bypasses extraction, but D-15's new-edge demo is at risk. Try one other GRAPHITI_EMBEDDING_MODEL, then move on and note it for 09-02.

If no fix lands and the clock passes start + 30 minutes, stop. Do not write main.py. Write the SUMMARY as abandoned, with the exact failure signal, for the Phase 11 README (D-03, D-04).
  </action>
  <verify>
    <automated>docker compose --env-file /dev/null config --quiet</automated>
    <fails_when>non-zero exit, or stderr contains "invalid interpolation format" or "required variable" (an unset graph var would break docker compose up postgres, Phase 1 D-17)</fails_when>
    <automated>docker compose --profile graph run --rm --build graph-service python probe.py</automated>
    <fails_when>non-zero exit, a Python traceback on stderr (for example openai.BadRequestError mentioning response_format, openai.AuthenticationError, neo4j.exceptions.ServiceUnavailable), or no printed record line containing Episodic</fails_when>
  </verify>
  <acceptance_criteria>
    - Probe stdout contains a record whose labels include `Episodic` with a count of at least 1. Whether `Entity` also appears is recorded in the SUMMARY.
    - `${GRAPH_SERVICE_DIR:-../graph-service}/probe.py` contains `OpenAIGenericClient`, `OpenAIEmbedder`, `OpenAIRerankerClient`, `EpisodeType.json` and `AI_BASE_URL`.
    - `grep -c . "${GRAPH_SERVICE_DIR:-../graph-service}/requirements.txt"` prints 2, and the two lines are `fastapi[standard]==0.141.1` and `graphiti-core==0.30.2`.
    - The `graph-service` block in `docker-compose.yml` contains `AI_API_KEY: ${AI_API_KEY:-}`, `GRAPHITI_LLM_MODEL`, `GRAPHITI_EMBEDDING_MODEL`, `AI_BASE_URL` and `GRAPHITI_TELEMETRY_ENABLED`.
    - `git diff -U0 docker-compose.yml` shows only added lines, except that a documented neo4j fallback replaces its single test line.
    - `docker compose --env-file /dev/null config --quiet` exits 0.
    - `test -z "$(git ls-files '*.py')"` exits 0 in this repo, so no Python lives here (D-05).
    - The HKT clock start is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>One Graphiti JSON episode, written through Kilo-pointed LLM, embedder and cross-encoder clients, lands as Episodic node(s) in local Neo4j with the clock start recorded, or the phase is abandoned with the failure signal recorded.</done>
</task>

<task type="tracer">
  <name>Task 2 (tracer): POST /episodes then GET /preferences round trip through compose, FastAPI, Graphiti and Neo4j</name>
  <files>${GRAPH_SERVICE_DIR:-../graph-service}/main.py (new, sibling repo), ${GRAPH_SERVICE_DIR:-../graph-service}/.gitignore (new, sibling repo), docker-compose.yml (this repo, graph-service block, additive)</files>
  <read_first>
    - ${GRAPH_SERVICE_DIR:-../graph-service}/probe.py (the client construction that already works; copy it)
    - .planning/phases/09-optional-s1-graphiti-preference-memory/09-RESEARCH.md §"Orchestrator Review Corrections" items 1, 3, 4; §Q3; §Q6; §"Code Examples" (POST /episodes and GET /preferences handlers)
    - .planning/phases/09-optional-s1-graphiti-preference-memory/09-CONTEXT.md D-06, D-08, D-10, D-12, D-13
    - docker-compose.yml graph-service block as left by Task 1
  </read_first>
  <action>
This is the phase tracer (D-21): the thinnest real path from compose through FastAPI, Graphiti and Neo4j to the Cypher read-back. It is production quality and lives in one file, `main.py`. There is no separate client or preferences module (ponytail: fewest files).

main.py, top to bottom:

1. A one-line module docstring.
   - `PreferenceKey = Literal["working_hours", "default_meeting_duration", "buffer_between_meetings", "no_meeting_days", "preferred_slot_of_day"]`, exactly these five keys (D-12).

2. `class PreferenceEpisode(BaseModel)` with three fields:
   - `user_id: str` constrained by `Field(pattern=r"^[A-Za-z0-9_-]+$", max_length=64)`. This matches Graphiti's group_id character rule [ASSUMED; harmless if the rule does not exist] and keeps the partition key boring.
   - `key: PreferenceKey`.
   - `value: str | int | list[str]`.

   A key outside the five makes FastAPI answer 422 before any handler code runs, which is D-12's rejection at the service boundary.

3. Module constant `READ_PREFERENCES` holding exactly `MATCH (e:Episodic {group_id: $user_id}) RETURN e.content AS content ORDER BY e.created_at ASC` (RESEARCH Q3). The user id only ever travels as a query parameter (T-09-01).

4. `@asynccontextmanager async def lifespan(app: FastAPI)`:
   - Build the same three Kilo-pointed clients as probe.py, from env.
   - `graph_driver = Neo4jDriver(uri, user, password)` from `graphiti_core.driver.neo4j_driver`. It accepts only these values; there is no parameter for a pre-built driver (RESEARCH correction 1).
   - `graphiti = Graphiti(graph_driver=graph_driver, llm_client=..., embedder=..., cross_encoder=...)`.
   - `await graphiti.build_indices_and_constraints()`. This is explicit on purpose, so a missing-procedure failure surfaces at startup (correction 1). Only if startup logs show an index-creation race error from the driver's own scheduled build, drop this one line and note it.
   - `app.state.graphiti = graphiti`, then `yield`, then `await graphiti.close()`. That gives one driver per process, closed on shutdown (D-08).
   - Pool tuning is Task 3. Do not spend the abandon clock on it here.

5. `app = FastAPI(lifespan=lifespan)`.

6. `@app.post("/episodes") async def post_episode(body: PreferenceEpisode, request: Request) -> dict[str, str]`:
   - Call `await request.app.state.graphiti.add_episode(name=f"preference-{body.key}", episode_body=json.dumps({body.key: body.value}), source=EpisodeType.json, source_description="distilled scheduling preference fact", reference_time=datetime.now(timezone.utc), group_id=body.user_id)`.
   - Await it inline, not in the background (RESEARCH Q6): the demo must know ingestion finished.
   - Return `{"status": "ok"}`.

7. `@app.get("/preferences") async def get_preferences(request: Request, user_id: str = Query(pattern=r"^[A-Za-z0-9_-]+$", max_length=64)) -> dict[str, Any]`:
   - `result = await request.app.state.graphiti.driver.execute_query(READ_PREFERENCES, params={"user_id": user_id})` (RESEARCH correction 3).
   - Start from an empty dict and `update` it with `json.loads(record["content"])` for each record in `result.records`, in the returned ascending order, so a later fact for the same key wins.
   - Return the dict. An unknown user returns `{}` with status 200, which is exactly what the compose healthcheck and the 09-02 TS gate need.

8. No other route of any kind: exactly these two `@app.` decorators (D-06, RESEARCH correction 4). No `if __name__` block; uvicorn is the entry point.

9. D-10 docstrings on `PreferenceEpisode`, `lifespan`, `post_episode` and `get_preferences`: a one-line summary, then Args, Returns and Raises sections.
   - Raises names the real behaviour. For example, `post_episode` lets Kilo Gateway or Neo4j errors propagate as HTTP 500, and an invalid body is a 422 that FastAPI raises before the handler runs.

`.gitignore` in the sibling repo contains `.env` and `__pycache__/` (T-09-02). No `.env` file is created anywhere in the sibling repo; under compose, every value comes from this repo's untracked root `.env`.

`docker-compose.yml`, `graph-service` block, additive:
- A ports entry `"127.0.0.1:8000:8000"`. This is the requested 8000:8000 narrowed to loopback, so the unauthenticated service is not reachable from venue wifi (T-09-04). If WSL genuinely cannot reach it, fall back to `"8000:8000"` and record that.
- A healthcheck:
  - exec-form test running `python -c` with the one-liner `import urllib.request; urllib.request.urlopen('http://localhost:8000/preferences?user_id=healthcheck', timeout=4)`;
  - interval 10s, timeout 5s, retries 5, start_period 30s.

  The gate is the real read route, not an extra one (RESEARCH correction 4). Nothing depends on graph-service health; only neo4j's health gates it.

Run the round trip:
- `docker compose --profile graph up -d --build graph-service`.
- Wait until `docker compose --profile graph ps graph-service` shows healthy.
- Run the verify commands.
- Use group_id `tracer` here, NOT demo user A's Slack id. A's partition must stay empty so 09-02 can show the before state (D-14, D-15).

On green:
- Commit in the sibling repo only: `git -C "${GRAPH_SERVICE_DIR:-../graph-service}" add -A`, then commit with the message `feat: graphiti preference service round trip`.
- This repo's commit holds only docker-compose.yml.
- Record the HKT time the round trip first passed, next to Task 1's clock start.

If start + 30 minutes passes before these verifies are green, stop. Skip Task 3 and plan 09-02, and write the SUMMARY as abandoned for the Phase 11 README (D-03, D-04).
  </action>
  <verify>
    <automated>curl -sf --max-time 120 -X POST http://localhost:8000/episodes -H 'content-type: application/json' -d '{"user_id":"tracer","key":"default_meeting_duration","value":45}' && curl -sf --max-time 5 'http://localhost:8000/preferences?user_id=tracer'</automated>
    <fails_when>curl exits non-zero (7 connection refused, 22 HTTP 4xx/5xx under -f, 28 timeout), or the final response body does not contain "default_meeting_duration":45</fails_when>
    <automated>curl -s --max-time 10 -o /dev/null -w '%{http_code}\n' -X POST http://localhost:8000/episodes -H 'content-type: application/json' -d '{"user_id":"tracer","key":"favourite_colour","value":"red"}'</automated>
    <fails_when>the printed status code is anything other than 422 (a 200 means the closed vocabulary is not enforced; 000 means the service is not listening)</fails_when>
    <automated>test "$(grep -cE '^@app\.' "${GRAPH_SERVICE_DIR:-../graph-service}/main.py")" = 2</automated>
    <fails_when>non-zero exit, meaning main.py declares a route count other than exactly two</fails_when>
  </verify>
  <acceptance_criteria>
    - `curl -sf 'http://localhost:8000/preferences?user_id=tracer'` returns a JSON body equal to `{"default_meeting_duration":45}`. This is D-13's literal round trip.
    - POSTing an unknown key returns 422 (D-12).
    - `main.py` has exactly two `@app.` decorator lines, `@app.post("/episodes")` and `@app.get("/preferences")` (D-06).
    - `docker compose --profile graph ps graph-service` shows `healthy`.
    - `main.py` contains `READ_PREFERENCES`, `$user_id` and `params={"user_id": user_id}`, and `EpisodeType.json`.
    - `grep -c '"""' "${GRAPH_SERVICE_DIR:-../graph-service}/main.py"` prints at least 8 (D-10 docstrings on four symbols).
    - `git -C "${GRAPH_SERVICE_DIR:-../graph-service}" ls-files` lists `main.py`, `probe.py`, `requirements.txt`, `Dockerfile` and `.gitignore`, and no `.env`.
    - `test -z "$(git ls-files '*.py')"` exits 0 in this repo.
    - The SUMMARY records the round-trip pass time, at or before clock start + 30 minutes.
  </acceptance_criteria>
  <done>POSTing one closed-vocabulary fact and GETting it back works end to end through compose-run FastAPI, Graphiti and Neo4j, with an invalid key rejected, committed in the sibling repo inside the 30-minute abandon window.</done>
</task>

<task type="auto">
  <name>Task 3: Tune the single Neo4j driver pool (D-08), document env, record the Browser label check for 09-02</name>
  <files>${GRAPH_SERVICE_DIR:-../graph-service}/main.py (sibling repo), ${GRAPH_SERVICE_DIR:-../graph-service}/.env.example (new, sibling repo)</files>
  <read_first>
    - ${GRAPH_SERVICE_DIR:-../graph-service}/main.py (lifespan as written in Task 2)
    - .planning/phases/09-optional-s1-graphiti-preference-memory/09-RESEARCH.md §"Orchestrator Review Corrections" item 1
    - .planning/phases/09-optional-s1-graphiti-preference-memory/09-CONTEXT.md D-08, D-09
    - gsd-prompt-ai-secretary.md lines 134-139 (Neo4j: the driver is a pool, one per process, small explicit pool, TLS in the URI scheme)
  </read_first>
  <action>
Time box: 5 minutes for the pool swap, then move on either way. D-03 outranks D-08 (RESEARCH correction 1).

1. D-08 pool settings in `lifespan`. Keep exactly this order, with no await between steps a and b:
   - a. `graph_driver = Neo4jDriver(uri, user, password)`.
   - b. Keep a reference to the original `graph_driver.client`, then assign `graph_driver.client = AsyncGraphDatabase.driver(uri, auth=(user, password), max_connection_pool_size=10, connection_acquisition_timeout=10.0)`. Import AsyncGraphDatabase from `neo4j`.
   - c. `await` close on the kept original client.
   - d. Pass `graph_driver=graph_driver` to Graphiti exactly as before.

   Why no await between a and b: `Neo4jDriver.__init__` schedules an index-build task that reads `self.client` only when it first runs, and it first runs at the next await. Swapping first means that task and every later query use the tuned pool, and the discarded client never has in-flight work.

   TLS comes from the URI scheme only (bolt:// locally, neo4j+s:// for Aura). Pass no encryption or certificate-trust keyword arguments to the driver (D-08). The pool is small on purpose, with an explicit acquisition timeout (governing doc).

2. If the swap errors or overruns 5 minutes, revert to the plain `Neo4jDriver(uri, user, password)` from Task 2.
   - Above it, add a `# ponytail:` comment naming the ceiling (driver defaults: pool 100, acquisition timeout 60 s) and the upgrade path (this client swap, or a Neo4jDriver subclass).
   - Record it in the SUMMARY as a known shortcut for the Phase 11 README.

3. `.env.example` (committed, no real values, T-09-02):
   - `NEO4J_URI=bolt://localhost:7687`
   - `NEO4J_USER=neo4j`
   - `NEO4J_PASSWORD=` (empty)
   - `AI_API_KEY=` (empty)
   - `AI_BASE_URL=https://api.kilo.ai/api/gateway`
   - `GRAPHITI_LLM_MODEL=` (empty)
   - `GRAPHITI_EMBEDDING_MODEL=openai/text-embedding-3-small`
   - `GRAPHITI_TELEMETRY_ENABLED=false`

   Add a leading comment saying two things. Under `docker compose --profile graph`, these values come from the app repo's untracked root .env through the graph-service environment passthrough. Inside compose, NEO4J_URI is bolt://neo4j:7687 (D-09).

4. Rebuild and restart: `docker compose --profile graph up -d --build graph-service`, wait for healthy, then run the verify commands. The first verify re-reads Task 2's tracer fact. That proves the tuned lifespan starts, the data survived the restart in the neo4j volume, and the read path still works.

5. Browser label check for 09-02 (RESEARCH Abandon Clock risk #2):
   - Open http://localhost:7474 and log in with NEO4J_USER/NEO4J_PASSWORD.
   - Run `MATCH (n) RETURN labels(n), count(*)`.
   - Record in the SUMMARY whether `Entity` appears beside `Episodic`. If only `Episodic` appears, 09-02 still demos a new node for the user, but not a new edge.

6. Commit in the sibling repo: `git -C "${GRAPH_SERVICE_DIR:-../graph-service}" add -A`, then commit with the message `feat: tune neo4j pool and document env`. This task changes nothing in this repo.
  </action>
  <verify>
    <automated>curl -sf --max-time 5 'http://localhost:8000/preferences?user_id=tracer'</automated>
    <fails_when>curl exits non-zero (7 connection refused or 22 HTTP error, e.g. the lifespan crashed after the swap and the container is restarting), or the body does not contain "default_meeting_duration":45</fails_when>
    <automated>test "$(grep -cE 'max_connection_pool_size|ponytail:' "${GRAPH_SERVICE_DIR:-../graph-service}/main.py")" -ge 1</automated>
    <fails_when>non-zero exit, meaning main.py has neither the tuned pool nor the documented driver-default shortcut</fails_when>
    <automated>test "$(grep -v '^\s*#' "${GRAPH_SERVICE_DIR:-../graph-service}/main.py" | grep -cE 'encrypted=|trust=')" = 0</automated>
    <fails_when>non-zero exit, meaning a TLS keyword argument appears in code outside comments (D-08 says scheme-only)</fails_when>
  </verify>
  <acceptance_criteria>
    - After a rebuild and restart, `curl -sf 'http://localhost:8000/preferences?user_id=tracer'` still returns `{"default_meeting_duration":45}`.
    - Either `main.py` contains `AsyncGraphDatabase.driver(`, `max_connection_pool_size=10` and `connection_acquisition_timeout=10.0`, or it contains a `# ponytail:` comment naming the driver-default pool and timeout, and the SUMMARY lists the shortcut.
    - No TLS keyword arguments in code outside comments (the verify test exits 0).
    - `.env.example` lists all eight keys, and `grep -cE '^(AI_API_KEY|NEO4J_PASSWORD|GRAPHITI_LLM_MODEL)=$' "${GRAPH_SERVICE_DIR:-../graph-service}/.env.example"` prints 3 (secret and model slots are empty).
    - The SUMMARY records the Browser label result (Episodic, with or without Entity).
    - `git -C "${GRAPH_SERVICE_DIR:-../graph-service}" log --oneline | wc -l` prints at least 2.
  </acceptance_criteria>
  <done>The service runs on one tuned (or documented-default) Neo4j driver created in the lifespan, with TLS from the scheme only. Env is documented with no secrets, the service is restart-safe, and the Browser label result is recorded for 09-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| laptop / venue network → graph-service :8000 | Unauthenticated HTTP. `user_id`, `key` and `value` are caller-controlled |
| graph-service → Neo4j bolt | Cypher that carries request data |
| graph-service → Kilo Gateway | API key egress; every POST triggers paid LLM and embedding calls |
| sibling repo → git history | `.env` or keys could be committed |
| PyPI → container image | `pip install` supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | `get_preferences` Cypher read-back | high | mitigate | `READ_PREFERENCES` is a module constant using `$user_id`. It is executed only via `graphiti.driver.execute_query(READ_PREFERENCES, params={"user_id": user_id})`. `user_id` is also constrained by `Query(pattern=r"^[A-Za-z0-9_-]+$", max_length=64)` on GET and by the same `Field` on POST. Task 2 acceptance greps for the params call. |
| T-09-02 | Information Disclosure | `AI_API_KEY` / `NEO4J_PASSWORD` in the sibling repo | high | mitigate | No `.env` is ever created in the sibling repo; values live only in this repo's gitignored root `.env` and reach the container via compose passthrough. The sibling `.gitignore` lists `.env`. `.env.example` has empty secret slots (Task 3 acceptance greps for them). Task 2 acceptance confirms `git ls-files` shows no `.env`. |
| T-09-03 | Tampering | `POST /episodes` body | medium | mitigate | `PreferenceKey` is a `Literal` of exactly the five D-12 keys, so anything else gets 422 before Graphiti runs (Task 2 verify). |
| T-09-04 | Elevation of Privilege | unauthenticated service on :8000 | medium | accept | Known shortcut for a one-day, two-user local demo: any local caller can read or write any `user_id`. Exposure is reduced by publishing on loopback only (`127.0.0.1:8000:8000`). Recorded in the SUMMARY for the Phase 11 README known shortcuts. |
| T-09-05 | Denial of Service | `POST /episodes` burning Kilo Gateway credits | low | accept | Loopback binding limits callers; demo volume is a handful of episodes. |
| T-09-06 | Information Disclosure | API key in the image or logs | medium | mitigate | The key is passed as runtime `environment`, never as a build arg or copied file. `probe.py` prints only labels and counts; no code prints env. |
| T-09-SC | Tampering | `pip install -r requirements.txt` in the Dockerfile | high | mitigate | Exactly two exact-pinned top-level requirements, `fastapi[standard]==0.141.1` and `graphiti-core==0.30.2`, both `Approved` in 09-RESEARCH §Package Legitimacy Audit. uvicorn arrives through FastAPI's own `standard` extra, and the neo4j and openai packages through graphiti-core's metadata. No `[ASSUMED]` or `[SUS]` top-level package means no legitimacy checkpoint is required. |
</threat_model>

<verification>
Phase-level checks for this plan, each a hand check under a minute (D-20):
- The D-13 round trip: POST `{"user_id":"tracer","key":"default_meeting_duration","value":45}`, then GET `/preferences?user_id=tracer` returns `{"default_meeting_duration":45}`, green by clock start + 30 minutes (D-03).
- `docker compose --env-file /dev/null config --quiet` exits 0: compose still interpolates with every graph var unset (Phase 1 D-17).
- `test -z "$(git ls-files '*.py')"` exits 0 in this repo. All Python is in the sibling repo (D-05).
- `test "$(git -C "${GRAPH_SERVICE_DIR:-../graph-service}" ls-files | grep -ci test)" = 0` exits 0. No tests exist (D-20).
- `git diff --name-only main` in this repo lists only `docker-compose.yml` (D-18, D-19).
</verification>

<success_criteria>
- ROADMAP Phase 9 success criterion 1 holds: `POST /episodes` with one distilled preference fact, then `GET /preferences?user_id=`, returns that fact.
- STR-01: a separate-repo FastAPI service with exactly two endpoints, one Neo4j driver per process, and TLS from the URI scheme.
- STR-02: a closed-vocabulary JSON fact is ingested via `add_episode(source=EpisodeType.json)` and read back deterministically from `Episodic.content`.
- The SUMMARY records the clock start, the round-trip pass time, the Browser label result and the D-08 pool status. Or the phase is abandoned, with the failure signal noted for the Phase 11 README (D-03, D-04).
</success_criteria>

<output>
Create `.planning/phases/09-optional-s1-graphiti-preference-memory/09-01-SUMMARY.md` when done (or when abandoned). It must include:
- HKT clock start and round-trip pass time;
- the `GRAPHITI_LLM_MODEL` and `GRAPHITI_EMBEDDING_MODEL` ids that worked;
- the Browser label result (Episodic with or without Entity);
- the D-08 pool status (tuned, or ponytail shortcut);
- the port binding used;
- the sibling-repo commit hashes;
- known shortcuts for the Phase 11 README.
</output>
