---
phase: 07-integrate-conflict-counter-proposal
plan: 03
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - lib/agent/graph.ts
  - lib/slack/scan-channel.ts
  - lib/slack/handlers/secretary-command.ts
autonomous: false
requirements:
  - OPT-01

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "[OPT-01, D-20] Typing `/secretary scan` in the watched channel reads that channel's last ~50 messages, makes ONE batch `extractIntents` call over them, and leaves real Proposal and Decision rows behind — visible in the dashboard without a reload"
    - "[D-20, D-25] No second `/secretary` command is registered anywhere: the scan is a `text.trim() === \"scan\"` branch added at the `Branch point:` marker inside Phase 2's existing handler, because Bolt runs every matching listener and a second registration would double-ack"
    - "[SLK-02 privacy boundary] `/secretary scan` invoked in a channel outside `SLACK_WATCH_CHANNEL_IDS` reads no history, extracts nothing, writes nothing, and answers the caller ephemerally that the channel is not watched"
    - "[SLK-04] `ack()` is still the first awaited statement of the `/secretary` handler, before the branch is even evaluated, so the scan's slow work never violates the three-second budget"
    - "[edge OPT-01/empty] `/secretary scan` in a watched channel whose recent history holds no human messages makes no model call at all and answers ephemerally with a zero count"
    - "[edge OPT-01/idempotency, D-19 rehearsal safety] Running `/secretary scan` twice in a row over the same history posts no duplicate card and creates no duplicate Proposal or Decision: messages that already carry a Decision row are skipped before extraction, and `dedupe_key` catches anything that slips through"
    - "[edge OPT-01/encoding] Message text reaches `extractIntents` exactly as Slack stored it, mention markup included; bot messages and every edit/delete/join/file subtype are dropped before extraction, and messages are ordered oldest-first so the numbered lines the extractor renders are chronological"
    - "[edge OPT-01/ordering] A capped scan processes the most recent actionable intents first and logs how many it skipped, so a re-scan picks up the remainder instead of silently losing them"
    - "[AGT-01] The scan adds no new model-call site: its one batch call goes through `extractIntents`, which goes through `lib/ai/provider.ts`"
    - "[AGT-06, AGT-07] `decision.create` remains a single write site in `lib/agent/graph.ts`: the scan reuses an extracted `recordDecision` rather than adding a second Decision write anywhere in the repo"
    - "[D-19, DMO-05] This whole plan is the first thing cut. Dropping it costs no requirement other than OPT-01, because DMO-05 was signed off by 07-02's review on the full merged diff; the review here is scoped to the scan diff and is additional, never the sign-off"
    - "[D-23, D-24] Only `lib/agent/graph.ts` and `lib/slack/**` change; `app/**`, `components/**` and `lib/calendar/**` are untouched"
  artifacts:
    - path: "lib/slack/scan-channel.ts"
      provides: "Channel history fetch, allowlist guard, already-decided filter, one batch extraction, capped proposal creation"
      exports: ["scanChannel", "SCAN_MESSAGE_LIMIT", "SCAN_PROPOSAL_CAP"]
      contains: "conversations.history"
    - path: "lib/slack/handlers/secretary-command.ts"
      provides: "The scan branch appended at Phase 2's Branch point marker; the handler is still the only /secretary registration"
      contains: "scan"
    - path: "lib/agent/graph.ts"
      provides: "recordDecision extracted from runAgent so the scan writes Decisions through the same single site"
      exports: ["runAgent", "recordDecision", "proposeNode", "bucketConfidence"]
      contains: "recordDecision"
  key_links:
    - from: "lib/slack/handlers/secretary-command.ts"
      to: "lib/slack/scan-channel.ts"
      via: "the scan branch calls scanChannel after ack"
      pattern: "scanChannel\\("
    - from: "lib/slack/scan-channel.ts"
      to: "lib/agent/extract-intents.ts"
      via: "one batch extractIntents call over the fetched messages"
      pattern: "extractIntents\\("
    - from: "lib/slack/scan-channel.ts"
      to: "lib/agent/graph.ts"
      via: "reuses proposeNode and recordDecision rather than a parallel writer"
      pattern: "proposeNode\\(|recordDecision\\("
    - from: "lib/slack/scan-channel.ts"
      to: "lib/config.ts"
      via: "the watched-channel allowlist guard before any history read"
      pattern: "watchChannelIds"
  prohibitions:
    - statement: "MUST NOT read or store message content from a channel outside SLACK_WATCH_CHANNEL_IDS: the allowlist guard runs before the history call, so an operator cannot point the scan at a private channel the agent was never meant to see"
      status: "resolved"
      verification: "test"
    - statement: "MUST NOT register a second /secretary command: Bolt runs every matching listener, so a second registration double-acks and can post duplicate cards"
      status: "resolved"
      verification: "test"
    - statement: "MUST NOT add a second Decision write site or a second model-call site: the scan reuses recordDecision and extractIntents"
      status: "resolved"
      verification: "test"
    - statement: "MUST NOT put anything on a calendar: the scan creates pending proposals and posts cards, and a human Approve click is still the only path to a calendar write"
      status: "resolved"
      verification: "test"
---

## Phase Goal

The full demo path runs live end to end, and a clash produces two reasoned alternative slots (ROADMAP Phase 7 `**Goal:**`, verbatim; see 07-01-PLAN.md for the MVP user-story note).

<objective>
Optional plan 3 of 3 (D-01). `/secretary scan` sweeps the watched channel's recent history through one batch `extractIntents` call and turns whatever it finds into real Proposals and Decisions (OPT-01) — the batch-first detection design the README argues for, demonstrated live rather than only described.

Purpose: it is the cheapest way to show that detection is not tied to a single live message, and it makes the dashboard's Decision log fill with real considered-and-ignored rows on demand. It is also the only place in the build where `extractIntents`' array signature (AGT-02) is exercised with more than one message.

**This plan is the first thing cut (D-19).** Build it only after 07-02 is demo-ready. Dropping it costs exactly one requirement, OPT-01, and nothing else: DMO-05 was already signed off by 07-02's `/ponytail-review` on the full merged diff (D-22), and the review in Task 2 here is scoped to the scan diff and is additional. If the wall clock is past 14:35 when this plan would start, do not start it — record OPT-01 as not built and let Phase 10 begin its rehearsal on time at 14:45.

Output: `lib/slack/scan-channel.ts`, a scan branch inside Phase 2's existing `/secretary` handler, and `recordDecision` extracted in `lib/agent/graph.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-integrate-conflict-counter-proposal/07-CONTEXT.md
@.planning/phases/07-integrate-conflict-counter-proposal/07-RESEARCH.md
@.planning/phases/07-integrate-conflict-counter-proposal/07-PATTERNS.md
@.planning/phases/07-integrate-conflict-counter-proposal/07-02-SUMMARY.md
@.claude/CLAUDE.md

<interfaces>
Fixed upstream. **Read the real merged files first; they win over any sketch here.**

```typescript
// types/slack.ts (Phase 1)
interface SlackMessage { teamId: string; channelId: string; ts: string; threadTs?: string; userId: string; text: string }
// lib/agent/extract-intents.ts (Phase 5): ONE batch call, numbered lines, message_index mapped back to a real ts
function extractIntents(messages: SlackMessage[], ctx: { now: Date }): Promise<LocatedIntent[]>   // LocatedIntent = ExtractedIntent & { ts: string }
// lib/agent/graph.ts (Phase 5 + 07-01/07-02)
function bucketConfidence(intent: LocatedIntent | null): "high" | "medium" | "low"
function proposeNode(state): Promise<Partial<AgentState>>   // participants, dedupe_key, proposal.create (P2002-safe), card post, card_channel/card_ts, ActionItems
function runAgent(input: RunAgentInput): Promise<RunAgentResult>   // holds the repo's ONLY decision.create call
// AgentState fields: message, now, intent, band, reason, conflicts, proposalId, replayed
// lib/slack/handlers/secretary-command.ts (Phase 2): handleSecretaryCommand — ack() first, logs, dispatches
//   nothing, and carries a `Branch point:` marker put there for exactly this plan (02-01-PLAN.md D-17)
// lib/slack/bolt.ts (Phase 2): registration only; `/secretary` is registered EXACTLY ONCE
// lib/config.ts (Phase 1): config.slack.watchChannelIds, config.slack.teamId — the only env reader
```

Prisma (Phase 1, snake_case): `Decision(id, team_id, source_ts, source_channel, message_text, verdict, confidence, reason, proposal_id?)`; `DecisionVerdict = acted | ignored`; `Proposal.dedupe_key @unique`.

**No schema change in this plan.**
</interfaces>
</context>

## Artifacts this phase produces (07-03)

| Artifact | Kind | Task |
|---|---|---|
| `lib/slack/scan-channel.ts` | new file | T1 |
| `scanChannel(args): Promise<ScanResult>` | new export — the whole sweep, returning counts | T1 |
| `SCAN_MESSAGE_LIMIT` (50) and `SCAN_PROPOSAL_CAP` (5) | new exported constants | T1, cap tightened in T2 |
| `ScanResult { scanned, skipped, extracted, proposals, decisions, cappedOut }` | new exported type | T1 |
| `recordDecision(args): Promise<string>` in `lib/agent/graph.ts` | extracted from `runAgent`'s existing single Decision write; `runAgent` now calls it, so the repo still has one write site | T1 |
| Scan branch inside `handleSecretaryCommand` at Phase 2's `Branch point:` marker | additive branch, no new registration (D-20, D-25) | T1 |
| Log tags `[scan] channel=`, `[scan] not watched`, `[scan] skipped-already-decided=`, `[scan] capped-out=`, `[scan] done` | log contract used by verify commands | T1 |
| `/ponytail-review` output scoped to the scan diff | recorded in the SUMMARY as known-shortcut candidates | T2 |

### Cross-phase additive contract changes

1. **`lib/agent/graph.ts` gains `recordDecision`** as a named export. `runAgent`'s behaviour is unchanged — the same statements now live behind the new function and `runAgent` calls it. The `decision.create` call site count in that file stays at exactly one, which is the gate Phase 5 established.
2. **`lib/slack/handlers/secretary-command.ts` gains one branch.** No registration changes anywhere; `lib/slack/bolt.ts` is not modified by this plan at all.
3. **Nothing else.** Every other signature is consumed unchanged.

## Flagged assumptions

- **FA-1:** the `Branch point:` marker Phase 2 promised is present in `handleSecretaryCommand`. If it is not, add the branch as the first statement after the existing ack and log, and record that the marker was missing.
- **FA-2:** `conversations.history` on the watched channel works with the installed scopes (`channels:history` was a pre-window checklist item). If it returns `missing_scope`, that is a checklist miss, not a code problem — record it and cut the plan rather than adding a scope at 14:40.
- **FA-3:** scanned proposals get an empty conflicts list, so they never produce conflict cards. This is a deliberate shortcut for an optional bulk feature: the conflict path is for the live message path, and running a freebusy query per scanned intent would add latency and quota to the thing most likely to be cut. Recorded as a known shortcut for Phase 11's `/ponytail-debt` section.
- **FA-4:** a full scan writes one Decision row per scanned message, so the dashboard's Decision log will jump by up to 50 rows. That is the intended DSH-02 demo asset, not a defect — but say so in the SUMMARY so Phase 10's rehearsal is not surprised by it.
- **FA-5:** `SCAN_PROPOSAL_CAP` of 5 is a demo-hygiene number, not a product decision. Intents beyond the cap are logged and left for a re-scan.

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): End-to-end "/secretary scan sweeps the watched channel into real rows" — one path only</name>
  <precondition>07-02 is demo-ready (its dry run passed), the wall clock is before 14:35, `/secretary` is registered exactly once by Phase 2, the bot is a member of the watched channel with `channels:history` on the installed token, and the watched channel holds at least a handful of recent human messages including at least one obvious scheduling ask.</precondition>
  <reversibility rating="reversible">A new file plus one branch plus a same-file function extraction; reverting restores Phase 2's ack-and-log `/secretary` behaviour exactly.</reversibility>
  <files>lib/agent/graph.ts, lib/slack/scan-channel.ts, lib/slack/handlers/secretary-command.ts</files>
  <read_first>
    - `lib/slack/handlers/secretary-command.ts` (the existing ack, the log line, and the `Branch point:` marker — the branch goes there and nothing else moves)
    - `lib/slack/bolt.ts` (confirm `/secretary` is registered exactly once; this plan must not add a registration)
    - `lib/agent/graph.ts` (`runAgent`'s single Decision write to extract, `proposeNode`'s parameter shape, `bucketConfidence`, the `AgentState` field names)
    - `lib/agent/extract-intents.ts` (the real `extractIntents` signature, its context parameter, and what `LocatedIntent` carries)
    - `lib/slack/handlers/watched-channel-message.ts` (Phase 2's subtype/bot/empty drop guard — mirror the same filter rules rather than inventing new ones)
    - `lib/slack/client.ts` and `lib/config.ts` (the WebClient singleton and the allowlist)
    - `prisma/schema.prisma` (Decision columns; read only)
    - `.planning/phases/07-integrate-conflict-counter-proposal/07-RESEARCH.md` §"`/secretary scan` (OPT-01) — reuse, don't fork" including its orchestrator note about not registering a second command
    - `.planning/phases/02-slack-surface/02-RESEARCH.md` §"Orchestrator Review Notes" item 5 (why `/secretary` currently dispatches nothing)
  </read_first>
  <action>
Build one path: slash command → allowlist guard → history fetch → filter → one batch extraction → rows → ephemeral answer. Repo rules apply: kebab-case, named exports, TSDoc on every function, `@/` imports, no `any`, Biome before done, no test files.

1. **Extract `recordDecision` in `lib/agent/graph.ts` (AGT-06, AGT-07).** Move the statements of `runAgent`'s existing Decision write into a new exported async function taking the fields it already writes (team id, source channel, source ts, message text, verdict, confidence, reason, optional proposal id) and returning the new row's id. Have `runAgent` call it. This is a mechanical move: same statements, same order, no behaviour change, no new guard. The repo keeps exactly one Decision write site, which is the gate Phase 5 established and which the scan must not break by writing its own.

2. **`lib/slack/scan-channel.ts` — the sweep.** Export `SCAN_MESSAGE_LIMIT` as 50 and `SCAN_PROPOSAL_CAP` as 5, a `ScanResult` type carrying the counts named in the artifacts table, and one async `scanChannel(args: { teamId: string; channelId: string; client: WebClient })`:
   a. **Allowlist guard, first statement, before any network call.** If the channel id is not in `config.slack.watchChannelIds`, log a line tagged `[scan] not watched` with the channel id only, and return a result marked as not-watched. No history is read, nothing is stored. This is the same privacy boundary SLK-02 draws on the live path, and it is what stops an operator pointing the scan at a channel the agent was never meant to see.
   b. Log a line tagged `[scan] channel=` with the channel id and the limit. Call `conversations.history` on the WebClient singleton for that channel with the limit — one page, no pagination.
   c. Filter exactly as Phase 2's live handler does: drop anything with a subtype, anything carrying a bot id, and anything whose text is empty after trimming. Then reverse the array — Slack answers newest-first and the extractor renders numbered lines, so chronological order is what makes those numbers mean anything. Map each survivor to the `SlackMessage` shape, passing the text through unmodified: mention markup and link markup are the extractor's problem, not this file's.
   d. **Skip what was already decided (re-run safety).** Read the existing Decision rows for this team and channel whose source ts is in the fetched set, and drop those messages before extraction. Log a line tagged `[scan] skipped-already-decided=` with the count. This makes a second scan over the same history cheap and side-effect-free, which matters because Phase 10 rehearses the demo three times.
   e. If nothing survives, return zeroed counts without calling the model at all.
   f. **One batch call (D-20, AGT-02):** `extractIntents(messages, { now: new Date() })` — a single call over the whole array, never one call per message. This is the only place in the build that exercises the array signature with more than one message.
   g. For each returned intent, find the message whose ts it maps to (drop an intent whose ts matches nothing) and bucket it with `bucketConfidence`. For the non-low ones, most recent first, up to `SCAN_PROPOSAL_CAP`: call `proposeNode` with a state object built from that message — the intent, the band, the reason, an empty conflicts list, a null proposal id and the same `now`. Reusing `proposeNode` is what gives the scan dedupe safety, participant resolution, the card post and the ActionItem rows for free; do not write a parallel creator. Non-low intents beyond the cap are logged with a line tagged `[scan] capped-out=` and left for a re-scan. Add a `ponytail:` comment naming the ceiling: the cap is demo hygiene so one scan cannot flood the channel, and the upgrade path is paging plus a queued poster.
   h. **Conflicts are deliberately empty for scanned intents** (FA-3). Add a `ponytail:` comment saying so and naming the upgrade: run the check-conflicts step per scanned intent when latency and quota allow.
   i. Write exactly one Decision per scanned message through `recordDecision`: verdict acted with the new proposal id where one was created, otherwise ignored with the intent's confidence and reason, or a zero confidence and a reason naming that no scheduling intent was extracted.
   j. Log a line tagged `[scan] done` with every count and return the `ScanResult`.

3. **The branch (D-20, D-25).** In `handleSecretaryCommand`, at the `Branch point:` marker, add a branch that fires only when the command text trimmed equals `scan`. Everything above the marker — including the ack, which stays the first awaited statement — is untouched. The branch calls `scanChannel` and then answers the caller ephemerally with the counts (or, for a non-watched channel, with a one-line explanation that the channel is not being watched). Wrap the call so a thrown error is logged and answered ephemerally rather than crashing the listener.
   **Register nothing.** Phase 2 already binds this command once, and Bolt runs every listener that matches, so a second binding would acknowledge the same invocation twice and can produce duplicate cards. The whole point of the marker is that this plan appends inside the existing handler.

4. **Restart Bolt** (the sole instance — check `ps aux` first) and fire the one path: type `/secretary scan` in the watched channel. Expect the `[scan]` log lines, an ephemeral reply with the counts, up to five new cards, and new Proposal and Decision rows visible in the dashboard within one polling interval.

5. **Triage by signal.** A `missing_scope` error is a pre-window checklist miss (FA-2) — record it and cut the plan, do not add a scope now. Two acks or two ephemeral replies for one invocation means a second `/secretary` registration exists — find and remove it rather than making the branch idempotent. Cards appearing for obviously non-actionable chatter means the band thresholds drifted, which is a Phase 5 concern; record it, do not retune at 14:40.

6. Run `bunx --bun @biomejs/biome check --write lib/agent lib/slack`, then the verify commands.
  </action>
  <verify>
    <automated>grep -nE 'decision\.create\(' lib/agent/graph.ts && grep -nE 'export async function recordDecision' lib/agent/graph.ts && ! grep -rnE 'decision\.create\(' lib/slack --include=*.ts</automated>
    <fails_when>non-zero exit; no Decision write line is printed from `lib/agent/graph.ts`; `recordDecision` is not exported; or a Decision write appeared under `lib/slack`, which is the second write site this task exists to avoid. Read the printed line numbers from the first grep: there must be exactly one, and it must sit inside `recordDecision` — two means the extraction copied the statements instead of moving them.</fails_when>
    <automated>grep -nE 'app\.command\("/secretary"' lib/slack/bolt.ts && ! grep -rnE 'app\.command\(' lib/slack/scan-channel.ts lib/slack/handlers/secretary-command.ts && OUT="$(git status --porcelain lib/slack/bolt.ts)"; echo "$OUT"; test -z "$OUT"</automated>
    <fails_when>non-zero exit; no `/secretary` registration is printed; a registration call appeared in the scan or handler file; or the command echoes `lib/slack/bolt.ts`, meaning this plan modified it, which it must not (D-20: append inside the existing handler). Read the printed line numbers from the first grep: more than one means a second binding exists and Bolt will run both, double-acking.</fails_when>
    <automated>grep -n 'watchChannelIds' lib/slack/scan-channel.ts && grep -nE 'extractIntents\(' lib/slack/scan-channel.ts && ! grep -nE 'for .*extractIntents|\.map\(.*extractIntents' lib/slack/scan-channel.ts && grep -nE 'conversations\.history' lib/slack/scan-channel.ts && ! grep -rnE 'from "openai|events\.insert|getCalendarClient' lib/slack/scan-channel.ts</automated>
    <fails_when>non-zero exit; no allowlist reference is printed (the privacy boundary is missing); no extraction call is printed; the extraction call sits inside a loop or a map, which means one call per message instead of one batch (against D-20 and the batch-first design); no history call is printed; or a direct SDK or calendar import exists in the scan file. Read the printed extraction line numbers: there must be exactly one, and the run's log must show exactly one `[ai]` line per scan.</fails_when>
    <automated>bun -e 'const { prisma } = await import("./lib/db.ts"); const { config } = await import("./lib/config.ts"); const ch = config.slack.watchChannelIds[0]; const since = new Date(Date.now() - 15 * 60000); const decisions = await prisma.decision.count({ where: { source_channel: ch } }); const props = await prisma.proposal.findMany({ where: { source_channel: ch, created_at: { gte: since } }, select: { id: true, title: true, status: true, card_ts: true, dedupe_key: true } }); const keys = new Set(props.map((p) => p.dedupe_key)); console.log(JSON.stringify({ decisions, recentProposals: props, distinctKeys: keys.size }, null, 2)); const ok = decisions > 0 && props.length > 0 && props.every((p) => p.card_ts && p.status === "pending") && keys.size === props.length; process.exit(ok ? 0 : 1)'</automated>
    <fails_when>non-zero exit, or the printed JSON shows zero Decision rows for the watched channel (the scan wrote nothing), zero recent Proposals, a recent Proposal with a null `card_ts` (a row was created but no card posted), a status other than `pending`, or `distinctKeys` smaller than the Proposal count (two rows share a dedupe key, meaning the reuse of `proposeNode` was bypassed)</fails_when>
    <automated>test -z "$(git status --porcelain app components lib/calendar)" && bunx tsc --noEmit && bunx --bun @biomejs/biome check lib/agent lib/slack</automated>
    <fails_when>non-zero exit; `git status` prints a path under `app/`, `components/` or `lib/calendar/` (D-23/D-24); any `error TS` line; or Biome reports one or more errors</fails_when>
    <human-check>
      <test>Type `/secretary scan` in the watched channel, read the ephemeral reply and the `[scan]` terminal lines, then open the dashboard. Then type `/secretary scan` once more in a channel that is NOT in the allowlist.</test>
      <expected>The first invocation answered ephemerally with counts, posted at most five cards, and the dashboard's Decision log filled with real considered-and-ignored rows carrying reasons — visible without a manual reload. Exactly one ephemeral reply appeared, not two. The second invocation, in the unwatched channel, answered ephemerally that the channel is not watched, posted no card, and printed a `[scan] not watched` line with no history read after it.</expected>
      <why_human>OPT-01's check is a hand check under a minute (dashboard rows appearing, cards appearing), and "exactly one ephemeral reply" is the only way to see a duplicate registration from the user's side</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `lib/agent/graph.ts` exports `recordDecision`, still contains exactly one Decision write call, and no Decision write exists anywhere under `lib/slack`
    - `lib/slack/bolt.ts` carries exactly one `/secretary` registration and was not modified by this plan
    - `lib/slack/scan-channel.ts` reads the allowlist, makes exactly one `extractIntents` call, calls `conversations.history`, and imports no SDK client and no calendar module
    - The row probe exits 0: Decision rows exist for the watched channel, recent Proposals all have a card ts and `pending` status, and every recent Proposal has a distinct dedupe key
    - Running `/secretary scan` twice in a row produces no second card for the same message — the second run's `[scan] skipped-already-decided=` count is greater than zero and its proposal count is zero
    - `/secretary scan` in a non-allowlisted channel printed `[scan] not watched` and made no history call
    - `git status --porcelain app components lib/calendar` prints nothing; `bunx tsc --noEmit` and Biome both exit clean
  </acceptance_criteria>
  <done>`/secretary scan` in the watched channel sweeps its recent history through one batch extraction and leaves real Proposals and Decisions behind, a second scan is a near no-op, and the command refuses to read any channel outside the allowlist.</done>
</task>

<task type="auto">
  <name>Task 2: Re-scan safety check and a `/ponytail-review` scoped to the scan diff</name>
  <precondition>Task 1 is green and at least one scan has run against the watched channel.</precondition>
  <files>lib/slack/scan-channel.ts</files>
  <read_first>
    - `lib/slack/scan-channel.ts` (from Task 1)
    - `lib/agent/graph.ts` (the P2002 reuse path 05-02 added inside `proposeNode` — the scan rides on it)
    - `07-02-SUMMARY.md` (`/ponytail-review` findings already recorded, so this review does not repeat them)
    - `.planning/phases/10-seed-and-rehearse/10-CONTEXT.md` if present (what Phase 10's reset expects to clear — a scan that leaves rows the reset misses would pollute the rehearsal)
  </read_first>
  <action>
1. **Prove the re-run is safe (D-19 rehearsal safety, Phase 10 depends on it).** Run `/secretary scan` a second time over the same history with nothing new posted in between. Expect the already-decided filter to skip every message, a zero proposal count, no new card, and no new Decision row. If a duplicate card appears, the filter in Task 1 step 2d is reading the wrong key — fix the filter, not the dedupe logic in `proposeNode`, which is Phase 5's and must not be touched.

2. **Tighten the cap only if the first scan flooded the channel.** If Task 1 posted five cards and the channel now looks noisy for a demo, lower `SCAN_PROPOSAL_CAP` to 3 and record the value used. Do not add paging, a queue, or a rate limiter — the cap is the whole mechanism and this plan is the first thing cut.

3. **Record the rehearsal note.** Write into the SUMMARY how many Decision rows a full scan adds and what a scan leaves behind, so Phase 10's reset script (DMO-01) and its three rehearsal runs account for it. A scan run immediately before a rehearsal would otherwise leave a Decision log Phase 10 did not expect.

4. **`/ponytail-review` scoped to this plan's diff.** Run it over only the files this plan changed. This is additional review, not the DMO-05 sign-off — that was satisfied by 07-02's review of the full merged diff (D-22), which is exactly why dropping this plan never drops the requirement. Apply only one-line findings; paste the rest into the SUMMARY as known shortcuts for Phase 11's `/ponytail-debt` section.

5. Run `bunx --bun @biomejs/biome check --write lib/slack`, then the verify commands.
  </action>
  <verify>
    <automated>bun -e 'const { prisma } = await import("./lib/db.ts"); const { config } = await import("./lib/config.ts"); const ch = config.slack.watchChannelIds[0]; const rows = await prisma.proposal.findMany({ where: { source_channel: ch }, select: { dedupe_key: true, card_ts: true } }); const keys = rows.map((r) => r.dedupe_key); const dupes = keys.filter((k, i) => keys.indexOf(k) !== i); const noCard = rows.filter((r) => !r.card_ts).length; console.log(JSON.stringify({ total: rows.length, duplicateKeys: dupes, proposalsWithoutCard: noCard }, null, 2)); process.exit(dupes.length === 0 && noCard === 0 ? 0 : 1)'</automated>
    <fails_when>non-zero exit, or the printed JSON lists any duplicate dedupe key (a second scan created a parallel Proposal instead of skipping) or any proposal without a card ts (a row was created and no card followed, which Phase 10's rehearsal would surface as a silent no-op)</fails_when>
    <automated>grep -nE 'SCAN_PROPOSAL_CAP|SCAN_MESSAGE_LIMIT' lib/slack/scan-channel.ts && ! grep -rnE 'setTimeout|setInterval|while \(true\)' lib/slack/scan-channel.ts</automated>
    <fails_when>non-zero exit; the two constants are absent (the cap and limit were inlined as bare numbers); or a timer or unbounded loop appeared, meaning paging or rate limiting was added against step 2's instruction</fails_when>
    <automated>test -z "$(git status --porcelain app components lib/calendar lib/slack/bolt.ts)" && bunx tsc --noEmit && bunx --bun @biomejs/biome check lib/agent lib/slack</automated>
    <fails_when>non-zero exit; `git status` prints a path under `app/`, `components/`, `lib/calendar/` or `lib/slack/bolt.ts`; any `error TS` line; or Biome reports errors</fails_when>
  </verify>
  <acceptance_criteria>
    - No Proposal for the watched channel shares a dedupe key with another, and none lacks a card ts
    - The second scan reported zero new proposals and a non-zero already-decided skip count
    - `SCAN_MESSAGE_LIMIT` and `SCAN_PROPOSAL_CAP` are named constants, and the scan file contains no timer and no unbounded loop
    - `07-03-SUMMARY.md` records the cap value used, the number of Decision rows a full scan adds, and the `/ponytail-review` findings
    - The SUMMARY states explicitly that DMO-05 was signed off in 07-02 and that this review is additional
    - `git status --porcelain app components lib/calendar lib/slack/bolt.ts` prints nothing; `bunx tsc --noEmit` and Biome both exit clean
  </acceptance_criteria>
  <done>A second `/secretary scan` over the same history is a no-op, the scan cannot flood the demo channel, Phase 10 knows what a scan leaves behind, and the scan diff has been reviewed.</done>
</task>

</tasks>

## Edge Coverage (spec-less probe fallback)

No SPEC.md exists for this phase; edge predicates were authored against OPT-01 and the Phase 2 handler contract.

| Probe item | Status | Disposition |
|---|---|---|
| OPT-01 empty (no human messages in range) | resolved (explicit) | truth: no model call, ephemeral zero count |
| OPT-01 idempotency (scan run twice) | resolved (explicit) | truth: already-decided filter plus `dedupe_key`; asserted by Task 2's duplicate-key probe |
| OPT-01 ordering (cap and chronology) | resolved (explicit) | truth: oldest-first for numbering, most-recent-first for the cap, skipped count logged |
| OPT-01 encoding (mention/link markup, subtypes, bot posts) | resolved (explicit) | truth: text passed through unmodified, Phase 2's drop rules mirrored |
| OPT-01 authorization (channel outside the allowlist) | resolved (explicit) | truth + prohibition: guard runs before the history call |
| OPT-01 adjacency (a second `/secretary` registration) | resolved (explicit) | truth + registration-count gate: exactly one binding, branch appended inside it |
| OPT-01 unclassified ("review manually") | unresolved | Flagged assumption FA-3 (scanned proposals get no conflict detection) — never auto-resolved |

No-silent-drop equality: 7 surfaced = 6 authored into `must_haves.truths` + 1 flagged assumption.

Prohibition recall (Stage 1 → Stage 2): four kept prohibitions (no reading outside the allowlist, no second command registration, no second Decision or model-call site, no calendar write from a scan), authored in `must_haves.prohibitions`. Canon referral, not minted here: prompt injection through scanned message text is canon injection handling, carried by Phase 5's T-05-01 and re-stated as T-07-18 below.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slash command invoker → channel history | Any workspace member can type the command in any channel the bot can see |
| Slack channel history → extraction prompt | Up to 50 untrusted messages reach the model in one call |
| Scan → Proposal/Decision rows and Slack cards | One command can create many rows and post many cards |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-17 | Information Disclosure | `/secretary scan` pointed at a non-watched channel | high | mitigate | The allowlist guard is the first statement of `scanChannel`, before any history call, so message content from a channel outside `SLACK_WATCH_CHANNEL_IDS` is never read, never sent to the model, and never stored. This is the same boundary SLK-02 draws on the live path, and the registration-count gate keeps it single-entry |
| T-07-18 | Tampering | prompt injection across 50 messages in one batch call | medium | mitigate | Inherited from Phase 5 (T-05-01): output is validated through the extraction schema, participant ids are filtered to those literally present in the message, and every proposal still needs a human Approve click. The blast radius of a successful injection is a card, never a calendar write |
| T-07-19 | Denial of Service | one command flooding the channel with cards and the log with rows | medium | mitigate | `SCAN_MESSAGE_LIMIT` caps the history read at one page of 50, `SCAN_PROPOSAL_CAP` caps cards per scan, the already-decided filter makes a repeat scan free, and there is no pagination or retry loop (asserted by the no-timer gate) |
| T-07-20 | Spoofing | a second `/secretary` registration acknowledging the same invocation twice | medium | mitigate | The branch is appended inside Phase 2's single handler and `lib/slack/bolt.ts` is not modified at all; the registration-count gate and the `git status` gate both assert it |
| T-07-21 | Repudiation | which messages a scan considered | low | mitigate | One Decision row per scanned message with verdict, confidence and reason, written through the single `recordDecision` site, plus the `[scan] done` counts in the log |
| T-07-22 | Elevation of Privilege | a scan reaching Google Calendar | high | mitigate | The scan creates `pending` proposals and posts cards only; the scan file imports no calendar module (asserted by grep), so a human Approve click remains the only path to a calendar write |

No package is installed in this plan, so no supply-chain row applies.
</threat_model>

<verification>
- Every automated verify command in Tasks 1-2 exits 0.
- `lib/slack/bolt.ts` is unmodified and carries exactly one `/secretary` registration.
- `lib/agent/graph.ts` still holds exactly one Decision write call, now behind `recordDecision`.
- The scan makes exactly one `extractIntents` call per invocation and reads the allowlist before any history call.
- A second scan over the same history creates no duplicate Proposal, Decision or card.
- Scope guard (D-23, D-24): `git status --porcelain app components lib/calendar` prints nothing.
- DMO-05 is NOT gated on this plan: 07-02's review of the full merged diff is the sign-off, recorded in `07-02-SUMMARY.md`.
</verification>

<success_criteria>
- OPT-01: `/secretary scan` runs `extractIntents` over the last ~50 messages of the current channel and produces real Proposals and Decisions, checkable by hand in under a minute (ephemeral counts, new cards, dashboard rows).
- The scan refuses any channel outside the watched allowlist.
- Running the scan twice back to back changes nothing the second time, so Phase 10's rehearsals are unaffected.
- If this plan is cut, the only requirement lost is OPT-01.
</success_criteria>

<output>
Create `.planning/phases/07-integrate-conflict-counter-proposal/07-03-SUMMARY.md` when done. It MUST include:
- the wall clock at which this plan started, and whether it was attempted at all
- the counts from the first scan (scanned, skipped, extracted, proposals, capped-out, decisions) and from the second scan
- the `SCAN_PROPOSAL_CAP` value actually used
- how many Decision rows a full scan adds, as a note for Phase 10's reset script and rehearsals
- whether the Phase 2 `Branch point:` marker was present
- `/ponytail-review` findings, and an explicit statement that DMO-05 was signed off in 07-02 and this review is additional
- if the plan was cut: the wall clock, and OPT-01 recorded as not built
</output>
