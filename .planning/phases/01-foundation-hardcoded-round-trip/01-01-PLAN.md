---
phase: 01-foundation-hardcoded-round-trip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - README.md
  - .gitignore
  - package.json
  - bun.lock
  - biome.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - components.json
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - lib/utils.ts
autonomous: false
requirements: [FND-01, FND-02, FND-03, FND-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`CLAUDE.md` exists at the repo root and contains a section for every one of the 13 required rule topics (folder structure, reuse-before-writing, TSDoc, single sources of truth, Biome-only, no tests, Next.js conventions, Zustand-not-Context, React Hook Form + Zod, naming, Prisma, run commands, one Bolt process)"
    - "No required `CLAUDE.md` or `README.md` section is heading-only: every `## ` heading is followed by at least one non-blank body line"
    - "The commit that first adds `CLAUDE.md` contains zero files under `app/`, `lib/` or `components/` — the rules land before feature code"
    - "`README.md` contains all 8 required headings in the source document's order: What it is, The core functionality, How it differs from what exists today, The problems it tackles, Architecture, Usage, Scope and non-goals, Production design notes"
    - "`bunx --bun @biomejs/biome check .` exits 0 on the scaffolded tree"
    - "`bun run dev` serves HTTP 200 at http://localhost:3000"
    - "No `tailwind.config.js` and no `tailwind.config.ts` exists anywhere in the repo (Tailwind v4 is CSS-first)"
    - "Every dependency in `package.json` is an exact version — no `^` and no `~` range survives anywhere in the file"
    - "`bun.lock` is tracked by git and `package-lock.json` is git-ignored"
    - "`gsd-prompt-ai-secretary.md` is git-ignored and never committed (listed in `.gitignore`)"
    - statement: "`CLAUDE.md` contains no two rule statements that contradict each other (e.g. one section permitting a tool another section forbids)"
      verification: backstop
  artifacts:
    - "CLAUDE.md"
    - "README.md"
    - "biome.json"
    - "package.json"
    - "bun.lock"
    - "next.config.ts"
    - "postcss.config.mjs"
    - "components.json"
    - "lib/utils.ts"
  key_links:
    - "FND-01 → every required rule topic greps as a `## ` heading in `CLAUDE.md`, and the CLAUDE.md-adding commit touches no source dir"
    - "FND-02 → all 8 README headings grep in order, none heading-only"
    - "FND-03 → `bun run dev` returns 200 at :3000, `bunx --bun @biomejs/biome check .` exits 0, no `tailwind.config.*` exists"
    - "FND-04 → zero `^`/`~` ranges in package.json, `bun.lock` tracked, `package-lock.json` ignored"
  prohibitions:
    - statement: "No formatter or linter other than Biome is configured in this repo (no Prettier config, no ESLint config, no competing format-on-save toolchain)"
      status: unverified
      verification: flagged
---

<objective>
Lay the repo's rules down first, then scaffold the toolchain the rest of the phase builds on.

**User story (MVP mode):** **As a** solo developer racing a 40-minute scaffold box, **I want to** have the repo's non-negotiable rules and a working bun/Next.js/Biome toolchain on disk before any feature file exists, **so that** every later phase and every parallel track inherits the same conventions instead of inventing its own.

Purpose: FND-01 and FND-02 are the cheapest, highest-leverage artifacts in the build — they constrain ten later phases. FND-03/FND-04 make the toolchain real and pinned so no parallel track ever re-resolves `package.json`.

Output: root `CLAUDE.md`, `README.md` skeleton, Next.js 16 App Router + shadcn + Tailwind v4 + Biome running under bun with every core-window dependency installed at an exact pin.

**Tracer ordering note.** Phase 1 is the project's Walking Skeleton and its end-to-end tracer is the SLK-07 Slack round trip in `01-03-PLAN.md`. That tracer cannot lead the phase: `create-next-app@16.3.4` refuses to scaffold into a non-empty directory and is therefore run into a temp dir and copied over the repo root (01-RESEARCH.md Pitfall 1), which would clobber any hand-authored `package.json` written before it. The substrate plans (01-01, 01-02) are forced to precede the tracer by that tool constraint, not by horizontal-layer habit. See `01-SKELETON.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-hardcoded-round-trip/01-CONTEXT.md
@.planning/phases/01-foundation-hardcoded-round-trip/01-RESEARCH.md
@gsd-prompt-ai-secretary.md
</context>

<assumption_delta_decision>
decision: no-change
rationale: The three detector signals ("alongside", "also", "second") are ROADMAP prose from the Phase 1 section ("**Runs alongside**: Nothing", "Also: `bunx prisma db push` succeeds", "Second: `bunx prisma generate` hangs under bun") — no entity in Phase 1's scope changes singular→plural identity.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Git baseline + CLAUDE.md (FND-01) + README skeleton (FND-02)</name>
  <precondition>The repo working copy is on the native WSL filesystem — `pwd` does not start with `/mnt/c`. If it does, stop: re-clone to a native path before writing anything (ROADMAP "most likely time-eating failure").</precondition>
  <files>CLAUDE.md, README.md</files>
  <read_first>
    - `gsd-prompt-ai-secretary.md` §"Phase 1, first task: `CLAUDE.md` and `README.md`" — the authoritative list of required CLAUDE.md rules and README sections
    - `.planning/PROJECT.md` §"Repo rules (become `CLAUDE.md`)" and §"What This Is" (source of the README "What it is" paragraph)
    - `.planning/phases/01-foundation-hardcoded-round-trip/01-CONTEXT.md` D-18, D-20, D-22, D-24, D-26, D-27
  </read_first>
  <action>
Rename the local branch `master` to `main` and create and check out `gsd/phase-1-foundation` from it (per D-26; there is no remote, so no upstream work is needed). Stage `gsd-prompt-ai-secretary.md` so it becomes tracked (per D-27 — PROJECT.md names it authoritative for README contents and untracked files do not exist in GSD worktrees).

Write root `CLAUDE.md` (per D-18; `.claude/CLAUDE.md` is GSD-managed and must not be edited). Use one `## ` heading per rule topic, with these exact heading texts so downstream greps are deterministic: `## Folder structure`, `## Reuse before writing`, `## Documentation`, `## Single sources of truth`, `## Formatting and linting`, `## Tests`, `## Next.js conventions`, `## State management`, `## Forms`, `## Naming and style`, `## Prisma`, `## Run commands`, `## Processes`. Content per topic:
- Folder structure is not negotiable: `app/`, `components/`, `components/ui/`, `hooks/`, `stores/`, `lib/` with `lib/slack/`, `lib/calendar/`, `lib/ai/`, `lib/agent/`, `utils/`, `types/`, `prisma/`. A file goes in exactly one of these. State (per D-24) that `hooks/` and `stores/` carry no placeholder file and appear when first used. State (per D-22) that `lib/utils.ts` is shadcn-owned (it holds `cn`) and is the one exception to the "domain logic in `lib/<area>/`" rule.
- Reuse before writing: search `lib/` and `utils/` before adding a helper; never a second helper doing an existing job under a different name.
- Documentation: TSDoc on every function — summary, `@param`, `@returns`, `@throws`, internal helpers included; no ponytail pass may remove a doc comment; if a cleanup pass and a comment conflict, the comment wins.
- Single sources of truth: all model calls through `lib/ai/provider.ts`; all colours through shadcn CSS variables in `app/globals.css` with no colour literals in components and no `tailwind.config` file (Tailwind v4 is CSS-first); all environment access through the one typed config module `lib/config.ts` — no scattered direct reads of the runtime environment object anywhere else in the repo.
- Formatting and linting: Biome is the only formatter and linter; run `bun run check` before a phase is considered done; no Prettier, no ESLint, no competing config.
- Tests: no test files and no test framework. This is a repo rule, not an oversight.
- Next.js conventions: Server Components by default, `"use client"` only where interactivity demands it, no secrets in client components, thin route handlers with logic in `lib/`, TanStack Query for client fetching rather than ad-hoc effect-based fetching.
- State management: Zustand, never React Context, for shared client state; stores live in `stores/`, one per domain, with selectors so components subscribe narrowly; Context only for static injection (theme/Copilot provider).
- Forms: React Hook Form with a Zod resolver, always; the same Zod schema validates on the server.
- Naming and style: kebab-case files, PascalCase components, camelCase functions, named exports over default, no `any`, Zod at every external boundary.
- Prisma: one shared client per process from `lib/db.ts` behind a `globalThis` singleton — never construct a client inside a request handler, Bolt listener or background task body; `bunx prisma db push` during the window and never `prisma migrate dev`; a track merges the latest `develop` before it pushes schema.
- Run commands (per D-18, which supersedes `.claude/CLAUDE.md`'s generated "Bolt under Node" text): Next.js is `bun run dev`; Bolt is `bun lib/slack/bolt.ts` with fallback `bunx tsx lib/slack/bolt.ts`; Prisma is `bunx prisma …`; bun only, never npm/npx/yarn/pnpm; never pass `--bun` to any of these.
- Processes: exactly one Bolt process runs at a time across all worktrees — kill any other worktree's Bolt before starting one (SLK-01).

Write `README.md` as a real open-source README skeleton (per D-20) with these exact `## ` headings in this order: `## What it is`, `## The core functionality`, `## How it differs from what exists today`, `## The problems it tackles`, `## Architecture`, `## Usage`, `## Scope and non-goals`, `## Production design notes`. Fill in two sections for real: "What it is" (one paragraph a stranger understands, from PROJECT.md §"What This Is") and "Usage" (prerequisites; `cp .env.local.example .env`; `docker compose up postgres`; the `dev`, `bolt`, `db:push`, `db:seed`, `check` scripts; how the Slack app is installed). Every other heading gets a single-line `TODO:` body naming what Phase 11 will write there — no heading may be left with an empty body. Under cut pressure the fallback is headings-plus-TODO only; never drop a heading.
  </action>
  <verify>
    <automated>for h in "Folder structure" "Reuse before writing" "Documentation" "Single sources of truth" "Formatting and linting" "Tests" "Next.js conventions" "State management" "Forms" "Naming and style" "Prisma" "Run commands" "Processes"; do grep -q "^## $h\$" CLAUDE.md || echo "MISSING-CLAUDE: $h"; done; for h in "What it is" "The core functionality" "How it differs from what exists today" "The problems it tackles" "Architecture" "Usage" "Scope and non-goals" "Production design notes"; do grep -q "^## $h\$" README.md || echo "MISSING-README: $h"; done</automated>
    <fails_when>any line starting with `MISSING-CLAUDE:` or `MISSING-README:` is printed</fails_when>
    <automated>awk '/^## /{if(h&&!n)print "EMPTY: "FILENAME" "h; h=$0; n=0; next} NF&&h{n=1} END{if(h&&!n)print "EMPTY: "FILENAME" "h}' CLAUDE.md README.md</automated>
    <fails_when>any line starting with `EMPTY:` is printed (a required section is heading-only)</fails_when>
    <automated>grep -n '^## ' README.md | cut -d: -f2- | tr -d '#' | sed 's/^ //' | tr '\n' '|'</automated>
    <fails_when>the printed pipe-joined string is not exactly `What it is|The core functionality|How it differs from what exists today|The problems it tackles|Architecture|Usage|Scope and non-goals|Production design notes|`</fails_when>
    <automated>git add CLAUDE.md README.md && git commit -q -m "docs(01-01): repo rules and README skeleton"</automated>
    <fails_when>non-zero exit (nothing staged, or the commit was rejected)</fails_when>
    <automated>git show --name-only --format= HEAD > /tmp/rules-commit.txt; awk '/^(app|lib|components)\//{n++} END{print n+0}' /tmp/rules-commit.txt</automated>
    <fails_when>prints a number greater than 0 (feature-code files landed in the same commit as the rules)</fails_when>
    <automated>git branch --show-current; git check-ignore -q gsd-prompt-ai-secretary.md && ! git ls-files --error-unmatch gsd-prompt-ai-secretary.md >/dev/null 2>&1 && echo SOURCE_DOC_UNTRACKED</automated>
    <fails_when>the branch line is not `gsd/phase-1-foundation`, or `SOURCE_DOC_UNTRACKED` is absent (the source doc must stay git-ignored and uncommitted)</fails_when>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` has all 13 `## ` headings listed above, each with a non-blank body.
    - `README.md` has all 8 `## ` headings in the stated order, each with a non-blank body; "What it is" and "Usage" carry real prose, the rest carry a one-line `TODO:`.
    - The commit that adds `CLAUDE.md` touches zero paths under `app/`, `lib/` or `components/`.
    - Current branch is `gsd/phase-1-foundation`; `gsd-prompt-ai-secretary.md` is git-ignored and not tracked.
    - `.claude/CLAUDE.md` is unmodified (`git status --short .claude/` prints nothing).
  </acceptance_criteria>
  <done>FND-01 and FND-02 are satisfied and committed before any feature file exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Package legitimacy gate — three [SUS] packages before any install</name>
  <read_first>
    - `.planning/phases/01-foundation-hardcoded-round-trip/01-RESEARCH.md` §"Package Legitimacy Audit" (the verdict table and the false-positive analysis)
  </read_first>
  <action>
Pause before the first `bun add`. The Phase 1 legitimacy audit returned `SUS` for three packages — `@slack/web-api@8.1.1`, `@prisma/adapter-pg@7.10.0`, `@types/pg@8.23.1` — all three flagged solely by the checker's "too-new" heuristic, which reads the latest-publish date rather than package age. Present the three packages with their audited weekly download counts (7.08M / 3.09M / 31.39M) and source repos (slackapi/node-slack-sdk, prisma/prisma, DefinitelyTyped), and ask the developer to confirm each on `npmjs.com/package/<name>` before installing. This checkpoint is never auto-approvable — `workflow.auto_advance` does not apply.
  </action>
  <what-built>
Nothing is built yet. `CLAUDE.md` and the `README.md` skeleton are committed (Task 1) and the repo is on branch `gsd/phase-1-foundation`. No dependency has been installed: this gate stands between the rules commit and the first `bun add`.
  </what-built>
  <how-to-verify>
Open each of these and confirm the listed repository matches:
1. https://www.npmjs.com/package/@slack/web-api — expect repo `slackapi/node-slack-sdk`, version 8.1.1, the exact version `@slack/bolt@5.1.0` itself depends on.
2. https://www.npmjs.com/package/@prisma/adapter-pg — expect repo `prisma/prisma`, version 7.10.0, matching the `prisma`/`@prisma/client` pin.
3. https://www.npmjs.com/package/@types/pg — expect repo `DefinitelyTyped/DefinitelyTyped`, version 8.23.1.
`pg@8.23.0` and `dotenv@17.4.2` audited `OK` and need no check.
  </how-to-verify>
  <resume-signal>
Reply `approved` to install all three, or name the package to drop and what to use instead.
  </resume-signal>
  <acceptance_criteria>
    - Developer has replied with an explicit approval (or a named exclusion) for all three `SUS` packages.
    - No `bun add` has run before this reply.
  </acceptance_criteria>
  <done>The three flagged packages are human-confirmed as the official upstream packages, or excluded by explicit instruction.</done>
</task>

<task type="auto">
  <name>Task 3: Next.js + shadcn + Tailwind v4 + Biome scaffold under bun, all deps pinned (FND-03, FND-04)</name>
  <files>package.json, bun.lock, biome.json, tsconfig.json, next.config.ts, postcss.config.mjs, components.json, app/layout.tsx, app/page.tsx, app/globals.css, lib/utils.ts, .gitignore</files>
  <read_first>
    - `.planning/phases/01-foundation-hardcoded-round-trip/01-RESEARCH.md` §"Common Pitfalls" Pitfall 1 (create-next-app conflict list and the temp-dir recipe) and Pitfall 2 (shadcn flag list is unversioned — run `--help` first)
    - `.planning/phases/01-foundation-hardcoded-round-trip/01-RESEARCH.md` §"Standard Stack" (the five new package pins) and §"Recommended Project Structure"
    - `.claude/CLAUDE.md` §"Technology Stack" — the exact version pin table for every core-window dependency
    - `.planning/phases/01-foundation-hardcoded-round-trip/01-CONTEXT.md` D-19, D-21, D-22, D-23, D-24
  </read_first>
  <action>
Scaffold Next.js into a sibling temp directory and move the generated tree into the repo root — do not run the CLI against the repo root. `create-next-app@16.3.4`'s conflict checker allowlists `.claude` but not `.planning`, `.remember` or the loose `gsd-prompt-ai-secretary.md`, and the CLI has no override flag (01-RESEARCH.md Pitfall 1 gives the exact command and the `rsync -a --exclude=.git` merge form). Scaffold flags: TypeScript, App Router, Tailwind, Biome, no `src/` directory, import alias `@/*`, bun as the package manager, Turbopack, git disabled. After the merge, run `git status --short` and confirm `.planning/`, `.claude/` and the source doc are untouched.

Initialise shadcn only — no components (D-22). Run the pinned CLI's `--help` first to confirm the flag names actually exist at 4.21.0 before scripting the init call (01-RESEARCH.md Pitfall 2: the docs site is unversioned). After init, confirm no `tailwind.config.js` or `tailwind.config.ts` was written; if the CLI produced one, delete it — Tailwind v4 is CSS-first and `postcss.config.mjs` carries the `@tailwindcss/postcss` plugin while `app/globals.css` starts with the Tailwind import. `cn` stays where shadcn puts it, at `lib/utils.ts`.

Install every core-window dependency at an exact pin (D-21) — everything in the stack table except CopilotKit and the Python side, plus the five Phase 1 additions. Runtime: `next@16.3.4`, `react@19.3.0`, `react-dom@19.3.0`, `tailwindcss@4.3.3`, `@tailwindcss/postcss@4.3.3`, `postcss@8.5.28`, `@slack/bolt@5.1.0`, `@slack/web-api@8.1.1`, `googleapis@180.0.0`, `@langchain/langgraph@1.4.14`, `@langchain/core@1.2.10`, `openai@7.15.0`, `zod@4.6.2`, `@hookform/resolvers@5.9.1`, `react-hook-form@7.87.0`, `@tanstack/react-query@5.102.8`, `zustand@5.0.15`, `@prisma/client@7.10.0`, `@prisma/adapter-pg@7.10.0`, `pg@8.23.0`, `@trigger.dev/sdk@4.5.16`. Dev: `prisma@7.10.0` (pin explicitly — the npm `latest` tag resolves to an 8.0.0 release candidate), `@trigger.dev/build@4.5.16`, `@biomejs/biome@2.5.13`, `@types/pg@8.23.1`, `dotenv@17.4.2`. Do not install CopilotKit (Phase 8 owns it) and do not create `trigger.config.ts` (Phase 4 owns it, per D-04). Strip every `^` and `~` from `package.json` so each dependency is an exact version, then re-run the install so `bun.lock` matches.

Write `package.json` scripts as the only written-down run commands (D-19): `dev` → `next dev`; `bolt` → `bun lib/slack/bolt.ts`; `db:push` → `prisma db push`; `db:seed` → `bun prisma/seed.ts`; `check` → `biome check --write .`; `postinstall` → `prisma generate` (D-12, so every worktree's `bun install` regenerates the gitignored client).

Write `biome.json` (D-23): exclude `components/ui/**`, `prisma/generated/**` and `.next` from both formatter and linter, and set the CSS parser's Tailwind-directive option so the `@theme`/`@apply` at-rules Tailwind v4 uses do not lint as unknown.

Extend `.gitignore` with `package-lock.json` (D-21), `prisma/generated/` (D-12), `.env`, and `.env.*` with an explicit negation for `!.env.local.example` and `!.env.cloud.example` (D-16). Create no placeholder file in `hooks/` or `stores/` (D-24) — those directories are documented in `CLAUDE.md` and appear when first used.
  </action>
  <verify>
    <automated>bunx --bun @biomejs/biome check .</automated>
    <fails_when>non-zero exit, or the summary line reports one or more errors (`Found N errors`)</fails_when>
    <automated>test ! -f tailwind.config.js && test ! -f tailwind.config.ts && echo TAILWIND_CONFIG_ABSENT</automated>
    <fails_when>`TAILWIND_CONFIG_ABSENT` is not printed (a v3-style Tailwind config survived the shadcn init)</fails_when>
    <automated>grep -cE '": *"[\^~]' package.json</automated>
    <fails_when>prints a number greater than 0 (a caret or tilde range survived, so a dependency is not exactly pinned)</fails_when>
    <automated>git ls-files --error-unmatch bun.lock >/dev/null && echo LOCK_TRACKED; git check-ignore -q package-lock.json && echo PKGLOCK_IGNORED; git check-ignore -q .env && echo DOTENV_IGNORED; git check-ignore -q prisma/generated && echo GENERATED_IGNORED</automated>
    <fails_when>any of `LOCK_TRACKED`, `PKGLOCK_IGNORED`, `DOTENV_IGNORED`, `GENERATED_IGNORED` is absent from the output</fails_when>
    <automated>node -e 'const s=require("./package.json").scripts;const m=["dev","bolt","db:push","db:seed","check","postinstall"].filter(k=>!s[k]);console.log(m.length?"MISSING-SCRIPTS: "+m.join(","):"SCRIPTS_OK")'</automated>
    <fails_when>output starts with `MISSING-SCRIPTS:` instead of printing `SCRIPTS_OK`</fails_when>
    <automated>(bun run dev >/tmp/next-dev.log 2>&1 &) ; sleep 12 ; curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3000 ; pkill -f "next dev"</automated>
    <fails_when>prints `000` (connection refused) or any 5xx code instead of `200`</fails_when>
    <automated>git status --short .planning .claude gsd-prompt-ai-secretary.md</automated>
    <fails_when>any line is printed (the scaffold copy modified or deleted planning/GSD files it must not touch)</fails_when>
  </verify>
  <acceptance_criteria>
    - `bunx --bun @biomejs/biome check .` exits 0.
    - `curl http://localhost:3000` returns HTTP 200 while `bun run dev` is up.
    - Neither `tailwind.config.js` nor `tailwind.config.ts` exists.
    - `grep -cE '": *"[\^~]' package.json` prints `0`.
    - `bun.lock` tracked; `package-lock.json`, `.env`, `prisma/generated` all git-ignored.
    - `package.json` defines all six scripts: `dev`, `bolt`, `db:push`, `db:seed`, `check`, `postinstall`.
    - No `trigger.config.ts` exists and no CopilotKit package is installed.
    - `hooks/` and `stores/` contain no placeholder file.
  </acceptance_criteria>
  <done>FND-03 and FND-04 are satisfied: the toolchain runs under bun, every dependency is exactly pinned, and the lockfile is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local `node_modules` | Third-party code executes at install (`postinstall`) and at runtime |
| temp scaffold dir → repo root | A bulk file copy can overwrite or delete tracked repo state |
| developer shell env → tracked files | Secrets must reach the process through untracked `.env` only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.gitignore` / secret files | high | mitigate | `.gitignore` lists `.env` and `.env.*` with explicit negations for the two example files; verified by `git check-ignore -q .env` in Task 3 |
| T-01-SC | Tampering | npm installs (`bun add`) | high | mitigate | Package Legitimacy Audit ran in 01-RESEARCH.md; Task 2 is a blocking human checkpoint for the three `[SUS]` packages before any install; no `[SLOP]` package is installed |
| T-01-02 | Tampering | temp-dir scaffold copy over repo root | medium | mitigate | Copy excludes `.git`; Task 3 verifies `git status --short .planning .claude gsd-prompt-ai-secretary.md` is empty after the merge |
| T-01-03 | Tampering | `postinstall` script running `prisma generate` | low | accept | Script is authored in this repo, not inherited from a dependency; its only effect is regenerating the gitignored client |
</threat_model>

## Assumptions

Deterministic edge-probe rows that came back `unclassified` for this plan's requirements stay unresolved and are recorded here as flagged assumptions rather than silently dropped. Neither is auto-dismissed; each is a live question for execution.

| # | Requirement | Probe row | Assumption taken | Risk if wrong |
|---|-------------|-----------|------------------|---------------|
| A-01-01 | FND-03 | unclassified | "runs under bun" is taken to mean the dev server responds 200 and Biome exits clean when invoked through bun/bunx — not that every process uses the bun *runtime* (PROJECT.md keeps Trigger.dev tasks on Node and allows a `bunx tsx` fallback for Bolt) | Low — a stricter reading would only add runtime assertions this phase's exit criteria do not name |
| A-01-02 | FND-04 | unclassified | "pinned exactly per STACK.md" is taken as: no `^`/`~` range survives in `package.json`, and the five Phase 1 additions in 01-RESEARCH.md §Standard Stack count as in-scope pins | Low — a mismatch surfaces immediately as an install error or a Biome/TS failure, inside the window |

## Artifacts this phase produces (plan 01-01 share)

Files created by this plan:

- `CLAUDE.md` — repo rules, 13 `## ` sections (the list in Task 1)
- `README.md` — 8 `## ` sections in source-doc order
- `biome.json`, `next.config.ts`, `postcss.config.mjs`, `components.json`, `tsconfig.json`
- `package.json` scripts: `dev`, `bolt`, `db:push`, `db:seed`, `check`, `postinstall`
- `bun.lock` (tracked), `.gitignore` entries for `package-lock.json`, `prisma/generated/`, `.env`, `.env.*` (with `!.env.local.example`, `!.env.cloud.example`)
- `app/layout.tsx`, `app/page.tsx` (scaffold default — replaced by 01-02), `app/globals.css`
- `lib/utils.ts` — shadcn-owned, exports `cn`

Deliberately NOT produced here (owned elsewhere): `trigger.config.ts` and `lib/agent/tasks/` (Phase 4, per D-04); CopilotKit (Phase 8); shadcn components and theme tokens (Phase 6). The full cross-phase symbol contract is listed in `01-02-PLAN.md` (data/config layer) and `01-03-PLAN.md` (Slack/agent/calendar seams).

<verification>
- All four Task 1 verify commands pass, and all seven Task 3 verify commands pass.
- `git log --oneline` shows the rules commit strictly before the scaffold commit.
- `.claude/CLAUDE.md` is byte-identical to its pre-phase state.
</verification>

<success_criteria>
- ROADMAP success criterion 1 is met: `CLAUDE.md` and `README.md` exist with every required heading before any other file is opened.
- ROADMAP success criterion 2 is half met here: `bun run dev` serves a page at :3000 and `bunx biome check` exits clean.
- The toolchain is pinned so no later track needs to re-resolve `package.json` or hand-merge `bun.lock`.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-hardcoded-round-trip/01-01-SUMMARY.md` when done.
</output>
